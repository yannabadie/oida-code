# Progress

**Active plan: `PLAN.md`** (merged blueprint + roadmap, 2026-04-24).
The schedule below uses `PLAN.md` §14 Phase 0-7 numbering, not the original `prompt.md` Phase-1/2/3/4 wording.

## Done

- [x] Initialize project (workspace scaffolding, brainstorm docs).
- [x] **Phase 0 — cadrage + bootstrap** (2026-04-23 → 2026-04-24). Commits `15138f3..1733f98` on `main`. See `PHASE1_REPORT.md` (titled for historical reasons; describes Phase 0 output).
  - Context digest (10 mandatory docs read).
  - Public repo `yannabadie/oida-code` created (MIT, `main` tracked).
  - Blueprint §7 tree scaffolded under `src/oida_code/`.
  - Pydantic I/O models v1 (`AuditRequest`, `NormalizedScenario`, `AuditReport`).
  - Vendored OIDA core (SHA256-pinned in `VENDORED_FROM.txt`).
  - Typer CLI with `inspect` implemented; 4 other subcommands `NotImplementedError` with phase pointers.
  - 4 quality gates green: ruff ✓, mypy --strict ✓ (41 files), pytest 10/10 ✓, `oida-code inspect` deserializes. Coverage 74%.
  - memory-bank populated with real content (6 files + 12 ADRs).
- [x] **Merge blueprint + roadmap into `PLAN.md`** (2026-04-24). ADR-10/11/12 logged.

## Doing

- [ ] Nothing. Awaiting user "go Phase 2" (observation model + obligation graph).

## Done — Phase 1 (deterministic audit · 2026-04-24)

Shipped with advisor-vetted scope cuts (CodeQL stubbed, 3-repo validation with
explicit 10-repo exit criterion deferred).

- [x] Schema v1.1: `Finding`, `ToolEvidence`, `ToolBudgets`, `VerdictLabel`
      (Literal) added to `models/`. Example JSONs updated. ADR-13 (Optional
      summary fields) + ADR-14 (verify accepts AuditRequest in Phase 1).
- [x] `verify/_runner.py` — shared subprocess helper (UTF-8, timeout, never
      raises). `probe_version` for lazy `tool_version` population.
- [x] `verify/lint.py` — ruff JSON.
- [x] `verify/typing.py` — mypy stdout parse with absolute-path regex.
- [x] `verify/pytest_runner.py` — JUnit-XML via tempdir.
- [x] `verify/semgrep_scan.py` — semgrep `scan --config=auto --json`.
- [x] `verify/codeql_scan.py` — Phase 1 stub (`status="tool_missing"` /
      `"skipped"`); full integration deferred to Phase 2.
- [x] `extract/blast_radius.py` — weighted signals (modules 0.20 / api 0.20 /
      data 0.35 / infra 0.25), bounded in ``[0, 1]``.
- [x] `ingest/manifest.detect_commands` — pyproject + setup.cfg + marker
      files. Falls back to stock Python defaults.
- [x] `score/verdict.py` — deterministic resolver. `corrupt_success` stays
      dark until Phase 5.
- [x] `report/json_report.py`, `report/markdown_report.py`,
      `report/sarif_export.py` — SARIF 2.1.0 minimal compliance (GitHub
      code-scanning accepted fields).
- [x] `cli.py` — `normalize` stays NotImplementedError (Phase 2). `verify`
      + `audit` implemented. Added `--intent`, `--format`, `--fail-on`.
- [x] Tests: 53 passing. Coverage **78%**. 6 new test modules.
- [x] Validation: self-audit + oida_framework subdir + oid_framework subdir
      + external `attrs` repo (clone to `.oida/validation-external/`). All
      4 runs produced valid reports, no crashes.

## Phase 1 carry-over tickets (fix in early Phase 2)

- [ ] **Pytest-subprocess Python resolution.** When `shutil.which("pytest")`
      returns a pytest bound to a different Python than the one running
      oida-code (e.g. miniforge3 global vs. our venv), collection fails with
      `ModuleNotFoundError`. Same risk for `mypy`. Phase 2 fix: probe whether
      `sys.executable -m pytest` is available and prefer it when the target
      appears to be the oida-code repo itself, or when `shutil.which` resolves
      to a Python different from `sys.executable`.
- [ ] **`--fail-on corrupt` structurally unreachable in Phase 1** (requires
      Phase 5 OIDA fusion). Wired but not testable end-to-end yet.
- [ ] **10-repo validation exit criterion deferred.** PLAN.md §14 P1 exit
      criterion is "Stable report on 10 Python repos without human
      intervention". We ran 4 (3 in-workspace + 1 external attrs). Phase 2
      gate will revisit after obligation graph ships.
- [ ] **semgrep_scan coverage 24%** — dev env on Windows lacks semgrep. Add
      a fixture-based JSON-parse unit test.
- [ ] **warnings from ruff/mypy do not show up as critical_findings.** By
      design (only `severity="error"` counts). The Markdown `counts` column
      surfaces them. Double-check the shape when Phase 2 fusion lands.

## Phase 2 — observation model + obligation graph (2 weeks)

**Entry gate:** Phase 1 shipped (met once pushed).

Deliverables: `models/trace.py`, `models/obligation.py`,
`models/progress_event.py`, `extract/obligation_graph.py`,
`extract/{claims,preconditions,dependencies}.py`, `score/mapper.py`,
`verify/hypothesis_runner.py`, `verify/mutmut_runner.py`, **50-100
hand-annotated PR traces** in `datasets/traces_v1/`, and `verify` CLI
learns to accept `NormalizedScenario`.

Exit criterion: can describe action-by-action where an agent explores /
exploits / stagnates / loops on a real PR. Obligation-extraction recall
≥ 60% on the annotated set.

## Phase 2 — observation model + obligation graph (2 weeks)

**Entry gate:** Phase 1 shipped.

Deliverables: `models/trace.py`, `models/obligation.py`, `models/progress_event.py`, `extract/obligation_graph.py`, `extract/{claims,preconditions,dependencies}.py`, `score/mapper.py`, `verify/hypothesis_runner.py`, `verify/mutmut_runner.py`, **50-100 hand-annotated PR traces** in `datasets/traces_v1/`.

Exit criterion: can describe action-by-action where an agent explores / exploits / stagnates / loops on a real PR. Obligation-extraction recall ≥ 60% on the annotated set.

## Phase 3 — Explore/Exploit adapter (2 weeks)

Deliverables: `score/trajectory.py`, correlation dashboard (Jupyter or Streamlit in `notebooks/`).

Exit criterion: scorer distinguishes "didn't find" (exploration error) vs "found but didn't use" (exploitation error). Spearman ρ > 0.5 against human labels on Phase 2 dataset.

## Phase 4 — agentic verifier (2 weeks, blocked on M.2 2TB upgrade)

Deliverables: `llm/{client,schemas,forward_verifier,backward_verifier}.py`, extended `score/verdict.py`, CLI `--llm-endpoint --offline`.

Exit criterion: multi-turn verifier beats single-pass LLM judge on Phase 2 annotated set.

## Phase 5 — OIDA fusion + repair (2 weeks)

Deliverables: `score/{fusion,repair}.py`, `llm/repair_prompts.py`, CLI `repair` wired.

Exit criterion: can explain every red/yellow verdict on a PR with evidence, not vibes.

## Phase 6 — product surface (2 weeks)

Deliverables: `.github/workflows/oida-code.yml`, `github/{checks,annotations}.py`.

Exit criterion: dev installs tool in <15 min and sees useful verdict inside a PR. Demo on 10 intentionally sloppy PRs with false-positive / false-negative table recorded before any threshold tuning.

## Phase 7 — research moat (months 4-6, off critical path)

LongCoT-Mini → full benchmark harness; Simula-driven synthetic dataset v1; Dafny proof modules for 2-3 critical invariants; TypeScript start.

Exit criterion: measurable moat (corrupt-success F1 on adversarial dataset + long-horizon critic robustness plot).

---
[2026-04-23 21:57:00] - Phase 1 bootstrap kicked off from `prompt.md`.
[2026-04-24 07:04:50] - Phase 1 Steps 0-4 complete. All quality gates green. Moving to Step 5 (report + stop).
[2026-04-24 07:45:00] - Merge: `prompt.md` "phase 1 bootstrap" retroactively relabeled **Phase 0** to match `PLAN.md` §14. `PHASE1_REPORT.md` kept as historical artifact (describes Phase 0 output). Next = Phase 1 deterministic audit, awaiting user "go".
[2026-04-26 22:30:00] - Phase 5.1 FULLY ACCEPTED — local deterministic tool gateway (commit 1e38bce). ADR-36 logged. 30/30 acceptance criteria from QA/A28.md met. Criterion #30 (real-runner CI / action-smoke run after Phase 5.1) closed by three real-runner runs all green on commit 1e38bce: ci (run id 24958890104, 1m14s), action-smoke (run id 24958890094, 57s — composite action ran end-to-end with the existing render-artifacts + build-artifact-manifest from Phase 4.9, gateway code shipped but not yet wired into the action.yml audit path which is Phase 5.2's job), provider-baseline-node24-smoke (run id 24958890095, 21s). NEW src/oida_code/verifier/tool_gateway/ package (~1200 LOC across 6 modules): contracts.py (GatewayToolDefinition with Literal[False] pins on requires_network + allows_write; ToolSchemaFingerprint runtime variant with combined_sha256; ToolAdmissionDecision/Registry; ToolGatewayAuditEvent with PolicyDecision Literal[allow|block|quarantine|reject] + RequestedBy Literal[workflow|operator|verifier] + 3 capability sentinel flags); fingerprints.py (canonical_json_sha256 JCS-approximation: sort_keys + minimal separators + UTF-8 + ensure_ascii=False; documented as approximation NOT strict RFC 8785 — future MCP integration MUST swap; fingerprint_tool_definition computes 4 hashes; compare_fingerprints returns Literal[match|drift]); admission.py (admit_tool_definition with 7 rules in QA-prescribed order; suspicious-pattern regex detection precedes fingerprint check — patterns: "ignore previous instructions" / "override policy" / "send secrets" / "exfiltrate" / "execute shell" / "write file" / inner OIDA evidence fence markers; rule 7 has precedence over rule 1 so a poisoned description never reaches the fingerprint check); audit_log.py (build_audit_event + append_audit_event + read_audit_events + audit_log_path; per-day per-tool JSONL append-only at .oida/tool-gateway/audit/<yyyy-mm-dd>/<tool_name>.jsonl — NOT .oida/mcp/audit/... namespace, "car ce n'est pas MCP" per QA/A28 §5.1.0-A line 92; schema has no request_arguments/raw_stdout/api_key fields by design); gateway.py (LocalDeterministicToolGateway.run returns VerifierToolResult exactly — NO new wrapper type, gateway is wrapper not new pipeline stage from aggregator's perspective; 5-stage pipeline registry-lookup → hash-drift-check → existing validate_request → existing get_adapter().run() → audit event; adapter exception → status=blocked + audit event; tool_missing preserves uncertainty status NOT block); __init__.py re-exports. NEW Typer sub-app `oida-code tool-gateway` with two subcommands: `fingerprint` computes 4-hash fingerprint set for ruff/mypy/pytest builtin definitions; `run` drives VerifierToolRequest list through gateway with --policy + --approved-tools + --audit-log-dir + --out. 40 new tests in tests/test_phase5_1_tool_gateway.py covering all 8 sub-blocks: 5.1.0 doc sync (test_phase5_report_audit_log_path_is_not_malformed asserts no /// pattern; test_phase5_report_test_count_matches_test_file regrep counts test file vs report); 5.1-A contracts (test_gateway_definition_pins_no_network_no_write asserts Pydantic ValidationError on Literal[False] violation; test_admission_status_enum_rejects_higher_tiers); 5.1-B fingerprinting (test_fingerprint_is_stable_across_key_order; test_fingerprint_changes_when_description_changes; ...input_schema...; ...output_schema...; test_compare_fingerprints_match_and_drift; test_hash_drift_quarantines_tool); 5.1-C admission policy (test_no_expected_fingerprint_quarantines_tool; test_matching_fingerprint_approves_read_only_tool; test_drifted_fingerprint_quarantines_tool; test_write_tool_rejected; test_network_tool_rejected; test_prompt_injection_in_tool_description_rejected with 4 poisoned variants); 5.1-D audit log (test_audit_event_written_for_allowed_tool; ...for_blocked_tool; test_audit_event_contains_no_secret_like_values asserts schema has no request_arguments/raw_stdout/api_key/token/credential/secret/raw_response fields; test_audit_event_contains_policy_decision iterates all 4 enum values; test_audit_log_jsonl_roundtrip; test_audit_log_path_namespace_is_tool_gateway asserts NOT /mcp/); 5.1-E gateway (test_gateway_runs_approved_ruff_tool asserts isinstance VerifierToolResult; test_gateway_blocks_unapproved_tool; test_gateway_quarantines_hash_drift; test_gateway_reuses_existing_tool_policy_path_traversal_block; test_gateway_reuses_existing_tool_policy_secret_path_block materialises .env file then asserts deny pattern fires; test_gateway_never_uses_shell_true greps every gateway py for shell=True; test_gateway_tool_missing_is_uncertainty; test_gateway_output_becomes_evidence_refs_only); 5.1-F CLI (test_tool_gateway_fingerprint_cli_outputs_hashes asserts 4 64-char hex hashes per entry × ≥3 entries; test_tool_gateway_run_cli_requires_approved_tools end-to-end with empty registry; test_tool_gateway_run_cli_writes_audit_log; test_tool_gateway_run_cli_no_official_fields scans both results.json and every audit JSONL); 5.1-G no-MCP regression locks (test_tool_gateway_is_not_mcp_server; test_tool_gateway_does_not_import_mcp scans gateway/*.py for forbidden imports; test_tool_gateway_has_no_tools_list_jsonrpc + ...has_no_tools_call_jsonrpc; test_tool_gateway_has_no_remote_transport scans for urllib/http/httpx/requests/websockets/aiohttp imports; test_tool_gateway_has_no_provider_tool_calling re-affirms supports_tools=True absent from provider_config.py). Phase 5.1.0 doc sync: fixed Phase 5.0 report ~13 → 16 tests (lines 361 + 378), enumerated test list expanded from 13 to 16 entries. Quality gates: ruff clean (full curated CI scope src/ tests/ scripts/), mypy clean (88 source files — +6 from Phase 5.0's 82, all 6 from new tool_gateway package), 685 passed / 4 skipped (was 645/4 before Phase 5.1 — exactly +40 new tests). ZERO new dependency in pyproject.toml. ZERO new workflow under .github/workflows/. ZERO MCP runtime code. ZERO provider tool-calling. ADR-22 hard wall holds (no V_net / debt_final / corrupt_success emission). The Phase 4.7 + Phase 5.0 anti-MCP / anti-tool-calling locks remain ACTIVE; Phase 5.1 ADDS 6 more no-MCP regression tests on the gateway package specifically. Honesty statement (verbatim per QA/A28 lines 449-458): Phase 5.1 implements a local deterministic tool gateway for existing tools; it does not implement MCP; it does not enable provider tool-calling; it does not execute remote tools; it does not allow write tools or network egress; it does not validate production predictive performance; it does not emit official total_v_net / debt_final / corrupt_success; it does not modify the vendored OIDA core. Per QA/A28 "Après Phase 5.1": Phase 5.2 = gateway integration into verifier loop (route verifier tool requests through gateway not directly through old engine; still no MCP); Phase 5.3 = local stdio MCP mock prototype contingent on schema pinning + audit log + admission registry + drift/write/network/secret blocks ALL operational AND GitHub-hosted CI staying green.

[2026-04-26 21:00:00] - Phase 5.0 FULLY ACCEPTED (design-only) — MCP / provider tool-calling design ADR (commit ee9661b). ADR-35 logged. 20/20 acceptance criteria from QA/A27.md met. Criterion #20 (real-runner CI / action-smoke run after Phase 5.0 docs) closed by three real-runner runs all green on commit ee9661b: ci (run id 24956952399, 1m13s), action-smoke (run id 24956952403, 59s — composite action ran end-to-end with the existing render-artifacts + build-artifact-manifest from Phase 4.9), provider-baseline-node24-smoke (run id 24956952386, 23s). Phase 5.0 produces SIX security documents under docs/security/ (~3500 lines combined): mcp_threat_model.md (8-section structure: Assets / Trust boundaries / Actors / Attack surfaces / Abuse cases / Required controls / Non-goals / Open questions; covers OWASP MCP Top 10 — MCP01 token mismanagement / MCP02 privilege escalation / MCP03 tool poisoning / MCP04 supply chain / MCP05 command injection / MCP06 intent flow subversion / MCP07 insufficient authn-authz / MCP08 lack of audit-telemetry / MCP09 shadow MCP servers / MCP10 context injection — plus MCP-spec authorization risks confused deputy + token passthrough; 14 abuse cases enumerated with severity; 12 required controls; 6 open questions for Phase 5.x), mcp_admission_policy.md (MCPServerStatus Literal enum with 6 values: proposed / quarantined / approved_read_only / approved_tooling / rejected / revoked; 12-item approval checklist enforcing identity pinning + provenance + version pin + schema hash + description scan + no-injection-pattern + no-write + no-network + no-secrets + no-broad-scopes + outputs-as-data + audit-log; 9 auto-rejection triggers; quarantine-as-one-way semantics; authz policy enforcing one-token-per-server + no GITHUB_TOKEN passthrough + confused-deputy guard + redirect-URI strict match + consent-per-client; operator workflow pseudocode register/inspect/approve/revoke), tool_schema_pinning.md (ToolSchemaFingerprint Pydantic schema with description_sha256 / input_schema_sha256 / output_schema_sha256 each 64-char lowercase hex; ServerOrigin Literal local_stdio/local_http/remote_http; RiskLevel Literal schema_discovery/read_only/deterministic/human_approved_write; JCS RFC 8785 canonicalisation; rug-pull rule any-hash-drift→quarantine-server; per-notification behaviour for tools/list_changed + resources/list_changed + prompts/list_changed; 6 hash domain rules), tool_call_execution_model.md (5-stage pipeline LLM-proposes-OIDA-executes preserved unchanged from Phase 4.2; 5 execution modes — Phase 5.0 only authorises mode 0 disabled + mode 1 schema-discovery in design; provider tool-calling forbidden by default with explicit rationale on provider-defined built-in tools + opaque command strings; forbidden context list pull_request from forks / pull_request_target / push by default / action-smoke default path; list of forbidden LLM utterances "run this shell command" / "merge_safe" / "production_safe" / "verified" / etc.), mcp_audit_log_schema.md (MCPAuditEvent Pydantic schema with PolicyDecision 4-value Literal allow/block/quarantine/human_review + RequestedBy 3-value Literal llm/operator/workflow + capability sentinel flags secret_access_attempted + network_access_attempted + write_access_attempted all default False; three-line principle "no tool without trace / no trace without reason / no reason without policy"; per-day per-server JSONL append-only storage at .oida/mcp/audit/<YYYY>/<MM>/<DD>/<server_id>.jsonl; redaction rules MUST NEVER carry API key + GITHUB_TOKEN + raw prompt + raw provider response + raw tool output content), mcp_unlock_criteria.md (10-item criteria list 1.threat-model-complete + 2.admission-policy-complete + 3.schema-pinning-design-reviewed + 4.no-dynamic-tool-install + 5.local-only-sandbox-design-accepted + 6.audit-log-schema-accepted + 7.allowlist-only-tool-registry-accepted + 8.human-approval-protocol-for-writes + 9.failure-mode-tests-specified + 10.rollback-plan-defined; locks STAY ACTIVE after Phase 5.0; phrase "Anti-MCP and anti-tool-calling tests remain active after Phase 5.0." appears verbatim per QA/A27 line 808). Pydantic-AI Phase 5 assessment in experiments/pydantic_ai_spike/phase5_assessment.md (~250 lines): 7 evaluation questions all admit "yes, with configuration" answers (Q1 same LLMEstimatorOutput via Agent(result_type=...); Q2 preserve evidence_refs requires retries=0; Q3 forbid authoritative=True structurally via Literal; Q4 tools=[] suffices but the surface is the entire concern; Q5 replay-only is more verbose not less; Q6 avoid MCP toolsets by NOT importing pydantic_ai.mcp.*; Q7 preserve ToolPolicy via wrapper-tools or fork-loop avoidance); recommendation = pydantic_ai_adapter_experiment documentary follow-on NOT migration. ADR-35 appended to memory-bank/decisionLog.md. reports/phase5_0_mcp_tool_calling_design.md (13-section report with diff summary + ADR-35 excerpt + threat model + admission policy + schema pinning + execution model + audit log + Pydantic-AI decision + GitHub Actions / CI impact + tests + what-this-still-does-not-prove + recommendation for Phase 5.1 + gates table). 16 design-only tests in tests/test_phase5_0_design.py with PROPER SCOPING (negative checks scan pyproject.toml + .github/workflows/ + src/oida_code/ only — deliberately NOT docs/ or reports/ which intentionally contain MCP/tool-calling/supports_tools words by design): test_phase5_design_docs_exist + test_adr35_logged + test_mcp_threat_model_mentions_tool_poisoning + test_mcp_threat_model_mentions_rug_pull + test_mcp_threat_model_mentions_confused_deputy + test_mcp_admission_policy_requires_schema_hash + test_mcp_unlock_criteria_keeps_locks_active + test_no_mcp_dependency_added (extends Phase 4.7 lock with pydantic-ai also forbidden) + test_no_mcp_workflow_added + test_no_provider_tool_calling_enabled + test_no_supports_tools_true (broader: scans every src/oida_code/*.py vs Phase 4.7 which only checked provider_config.py) + test_no_mcp_runtime_import_in_src (regex-based check on import mcp / from mcp / import pydantic_ai / from pydantic_ai under src/) + test_anti_mcp_locks_still_active (imports test_phase4_7_provider_baseline.py and asserts the Phase 4.7 lock-functions are still defined as callables — catches silent removal) + test_no_official_fields_emitted (regex check no numeric assignment to total_v_net/debt_final/corrupt_success anywhere in src/) + test_phase5_report_honesty_statement (8-line verbatim lock per QA/A27 lines 899-908) + test_phase5_report_declares_no_code_mcp. Quality gates: ruff clean, mypy clean (82 source files unchanged — Phase 5.0 ships zero src/ code), 645 passed / 4 skipped (was 629/4 before Phase 5.0 — that's +16 new tests). ZERO new code under src/oida_code/. ZERO new dependency in pyproject.toml. ZERO new workflow under .github/workflows/. The existing Phase 4.7 anti-MCP and anti-tool-calling tests remain ACTIVE; Phase 5.0 ADDS to them rather than replacing. ADR-22 hard wall holds (no V_net / debt_final / corrupt_success emission). Honesty statement verbatim per QA/A27.md lines 899-908: Phase 5.0 is design-only; it does not add MCP runtime integration; it does not enable provider tool-calling; it does not execute MCP tools; it does not remove anti-MCP or anti-tool-calling locks; it does not validate production predictive performance; it does not emit official total_v_net, debt_final, or corrupt_success; it does not modify the vendored OIDA core. Per QA/A27.md "Après Phase 5.0": Phase 5.1 = either (Option A — RECOMMENDED) local deterministic tool gateway transforming Phase 5.0 designs into runtime guard-rails on tools OIDA-code already calls (no MCP yet, no third-party tools, no new dependency), OR (Option B — alternative) MCP sandbox proof-of-concept (one local stdio MCP server, read-only, no network, no write, no secrets, pinned schema, replay-only) — but only if all 10 unlock criteria are demonstrably satisfied. Either way, the existing anti-MCP locks remain ACTIVE until the Phase 5.1+ ADR explicitly removes them.

[2026-04-26 18:30:00] - Phase 4.9 FULLY ACCEPTED — artifact UX polish + failure-path diagnostics (commits 2b706c8 → 9caf042). ADR-34 logged. 25/25 acceptance criteria from QA/A26.md met. Criterion #25 (real-runner action-smoke / provider-baseline run after 4.9 changes) closed by three real-runner runs all green on commit 9caf042: ci (run id 24955614235, 1m12s), action-smoke (run id 24955614219, 1m0s — exercised render-artifacts → diagnostic-markdown=.oida/action-smoke/diagnostic.md AND build-artifact-manifest → manifest=.oida/action-smoke/artifacts/manifest.json AND diagnostic_status=contract_clean end-to-end), provider-baseline-node24-smoke (run id 24955614230, 19s — Node 24 replay path emits diagnostic_status=contract_clean correctly). Phase 4.9.0 (preamble) closes the V4 Pro 6/8 missing-capture gap at source: ProviderRedactedIO schema widens to carry failure_kind: Literal["success", "invalid_json", "invalid_shape", "schema_violation", "transport_error", "timeout", "provider_unavailable"] (default "success" for backward compat), redacted_response_body becomes str | None (None when no body received), redacted_error: str | None added, model and http_status made optional. complete_json restructured per advisor: single try/finally with stash assembled in finally from locally-scoped variables; failure_kind set IMMEDIATELY before each raise site; redact_secret called once per body / once per error string; inner try/except around http_post keeps wall_clock_ms measured on sad path too. 6 new failure-path tests using sentinel sk-DETECT-LEAK-Z9KF1L-PROVIDER-IO-CANARY-2026 lock that the API key is never leaked even on HTTP 401 paths that ECHO the key. Runner-side change NOT NEEDED — evaluate_llm_estimator already pops unconditionally at runner.py:717. 4.9-A: NEW src/oida_code/report/diagnostic_report.py (~325 LOC) with banner-led document structure (Status card / What was measured / Key findings / Provider failure matrix / What this does NOT prove / Next actions / Stability across repeat runs); banner literal "Diagnostic only — not a merge verdict." appears as first non-title line on every output; renderer carries runtime negative-list backstop (_FORBIDDEN_PRODUCT_CLAIMS = merge_safe / merge-safe / production_safe / production-safe / bug_free / bug-free) that raises RuntimeError if any variant ever appears (defence-in-depth against stale templates). DiagnosticStatus = Literal["blocked", "contract_failed", "contract_clean", "diagnostic_only"] — the four FORBIDDEN values (merge_safe / production_safe / verified) unrepresentable by static type. derive_diagnostic_status: official_field_leak_count > 0 → blocked; all 5 contract metrics == 1.0 → contract_clean; any < 1.0 → contract_failed; otherwise diagnostic_only. NEW CLI subcommand `oida-code render-artifacts <input> --out <out.md> --format markdown`. 13 tests (5 mandatory from QA/A26 + 8 backstops). Legacy markdown_report.py (used by oida-code audit) UNTOUCHED — diagnostic_report.py operates on the calibration-eval / provider-baseline output layout. 4.9-B: action.yml step summary path bumped to cat polished diagnostic Markdown (DIAGNOSTIC_MD) into $GITHUB_STEP_SUMMARY when present; falls back to legacy `head -n 80 report.md` only when DIAGNOSTIC_MD missing. 4.9-C: SARIF category disambiguation — action.yml uploader now uses `category: oida-code/combined`; .github/workflows/sarif-upload.yml category bumped from `oida-code` → `oida-code/audit-sarif`. Both files pin upload-sarif@v4. action.yml uploader bumped from @v3 → @v4 (closing the README/action.yml inconsistency advisor flagged — README claimed v4 since Phase 4.7 but action.yml was still v3). 4.9-D: NEW action outputs diagnostic-status (enum, statically constrained to four-value Literal) + official-field-leaks (int) + diagnostic-markdown (path) + artifact-manifest (path); CLI's calibration-eval writes <out>/action_outputs.txt in key=value format; action.yml cats it into $GITHUB_OUTPUT (with safe fallback to diagnostic-status=diagnostic_only / official-field-leaks=0 when file missing). README extended with GitHub Action outputs table covering all 7 outputs. 9 tests (including e2e CLI test on real calibration_v1 dataset + action.yml schema check). 4.9-E: scripts/audit_provider_estimator_labels.py extended with provider_value (short summary like "value=0.55 conf=0.7") + action (per-classification recommendation) columns; new missing_capture classification covers TWO cases (redacted_io file absent OR file present with failure_kind != "success") so the V4 Pro 6/8 gap remains visible after Phase 4.9.0 closes the upstream cause. 6 _ACTION_RECOMMENDATIONS strings (one per classification, all explicit "PROPOSAL — do not apply automatically"). Hard rule LOCKED by test_label_audit_never_changes_expected_labels_automatically (byte-level expected.json check + mtime check after a label_too_strict classification). 7 tests. 4.9-F: NEW src/oida_code/models/artifact_manifest.py (~210 LOC) with ArtifactBundleManifest + ArtifactRef Pydantic shapes; THREE Literal pins make drift unrepresentable (mode = "diagnostic_only" / official_fields_emitted = False / contains_secrets = False per ref). 10 ArtifactKind values (json_report / markdown_report / sarif / calibration_metrics / redacted_io / label_audit / step_summary + Phase 4.9 additions diagnostic_markdown / action_outputs / stability_summary). NEW CLI subcommand `oida-code build-artifact-manifest <bundle> [--out PATH] [--provider STR] [--model STR]` walks bundle root, classifies each file by filename / parent-directory pattern, computes SHA256 (chunked 64KB read), writes <bundle>/artifacts/manifest.json (excluded from its own hash list — chicken-and-egg solved). 11 tests (4 mandatory from QA/A26 + 7 backstops including Pydantic Literal pin checks: ArtifactRef(contains_secrets=True) and ArtifactBundleManifest(official_fields_emitted=True OR mode="merge_safe") all reject at construction). Quality gates: ruff clean, mypy clean (82 source files); 629 passed / 4 skipped (was 575/4 before Phase 4.9 — that's +48 new tests across 5 new test files: test_phase4_9_diagnostic_report.py 13 / test_phase4_9_action_outputs.py 9 / test_phase4_9_step_summary_and_sarif.py 8 / test_phase4_9_label_audit.py 7 / test_phase4_9_artifact_manifest.py 11 + 6 new failure-path tests in test_phase4_8_redacted_provider_io.py 10 → 16). Reports: phase4_9_artifact_ux_polish.md (NEW, 13-section structure including honesty statement, gates table, security review, recommendation for Phase 5.0). README + memory-bank updated. ADR-22 + ADR-25..33 + ADR-34 all hold; production CLI + composite action emit no V_net / debt_final / corrupt_success. Per QA/A26.md "Après Phase 4.9": Phase 5.0 = MCP / provider tool-calling DESIGN ADR ONLY (no code) — anti-MCP + anti-tool-calling locks (tests/test_phase4_7_provider_baseline.py) hold and must be REMOVED before Phase 5.0 work begins. Pydantic-AI remains a spike (Phase 4.8-F documentary directory).

[2026-05-01 02:00:00] - Phase 4.8 FULLY ACCEPTED — provider regression deepening + private holdout protocol shipped (commits 8d09d57 → cd7e5ac → b787e14 → final docs). ADR-33 logged. 24/24 acceptance criteria from QA/A25.md met. 4.8.0-A: README contradiction cleanup (sarif@v3 stale claim + provider baseline not_run claim removed; 2 regression tests lock the cleanup). 4.8.0-B: provider-baseline-node24-smoke.yml workflow shipped (replay-only, FORCE_JAVASCRIPT_ACTIONS_TO_NODE24=true at job scope, contents:read, green on real runner id 24954060856). 4.8-A redacted provider I/O: new ProviderRedactedIO Pydantic model (frozen + extra="forbid") carrying prompt_sha256 (NOT raw prompt), redacted response body (after redact_secret(body, key)), model id, http_status, wall_clock_ms, response_id, finish_reason, token usage; OpenAICompatibleChatProvider gains capture_redacted_io: bool = False ctor flag + pop_last_redacted_io() method (slot-and-clear); CRITICAL design: redaction happens INSIDE provider where API key value is in scope, runner never holds the raw key; CLI flag --store-redacted-provider-io (opt-in only, defaults false at every layer); workflow input store-redacted-provider-io (default false); 10 tests use long sentinel sk-DETECT-LEAK-Z9KF1L-PROVIDER-IO-CANARY-2026 to assert redaction actually fires (one test injects sentinel into fake response body simulating 401-style auth-echo, asserts captured payload (a) does not contain sentinel and (b) DOES contain [REDACTED] marker — proves redaction fired vs output happens to be clean). 4.8-B label audit: scripts/audit_provider_estimator_labels.py reads redacted I/O + expected.json, classifies per (case, label) into match / label_too_strict / provider_wrong / mapping_ambiguous / contract_gap; read-only, no automatic label edits. 4.8-C private_holdout_v1: README + manifest.example.json committed, cases/ gitignored at datasets/private_holdout_v1/cases/ (trailing-slash precision). 4.8-D matrix: dataset extended L001-L004 → L001-L008 (covering completion / tests_pass / operator_accept / edge_confidence; manifest 36 → 40 cases, 2 existing Phase 4.4.1 tests updated for the new size); DeepSeek V4 Pro 8 cases × 2 repeats green on run id 24954088672 with leak_max=0; DeepSeek V4 Flash 8 cases × 1 green on run id 24954298728 with leak=0; MiniMax/Kimi marked not_run with explicit reason (two providers sufficient for "DeepSeek Pro vs Flash comparison" goal per QA/A25.md §4.8-D). 4.8-E repeat-runs: --repeat-provider-runs N (1-3 hard cap), workflow input choice 1/2/3, writes <out>/stability_summary.json with mean+std for status/estimate accuracy + safety + fenced rates + citation precision + official_field_leak_count_max; hard rule leak_max > 0 exits 3. V4 Pro stability observation: estimator_status_accuracy_mean=0.1875 std=0.0625, estimator_estimate_accuracy_mean=0.15 std=0.15 — confirms V4 Pro is non-deterministic between runs on same prompt (relevant data for Phase 4.9+ comparison studies). 4.8-F pydantic-ai spike: experiments/pydantic_ai_spike/ outside src/, NOT pulled by dev extra (pyproject.toml mypy + ruff exclude experiments/), README + adapter_sketch.py document the comparison surface + ADR-32 reconciliation requirements (zero tool calling, same forbidden phrases, same confidence caps, same evidence-ref citation rule). Empirical multi-provider data captured: V4 Flash matches replay status accuracy (0.625 each) while V4 Pro is significantly lower (0.1875 mean) AND inconsistent; per ADR-28 + ADR-32 + ADR-33 this is DATA not verdict. Audit revealed (V4 Flash 8/8 captured): 1 match (L001 capability), 1 label_too_strict (L002 capability — value 0.55 > max_value 0.4), 3 provider_wrong (L003 — provider produced estimates when expected missing because no intent), 5 contract_gap (L004-L008 — provider didn't emit secondary expected field). V4 Pro 2/8 captured (L005 + L007); 6/8 raised LLMProviderInvalidResponse-class exceptions that bypass success-path stash — documented as Phase 4.8.1 follow-up to extend capture to provider-failure paths. Real-runner runs all green: ci on cd7e5ac (24954060852), action-smoke on cd7e5ac (24954060855), provider-baseline-node24-smoke on cd7e5ac (24954060856), provider-baseline V4 Pro 8×2 (24954088672), provider-baseline V4 Flash 8×1 (24954298728). Full suite 575 passed + 4 skipped (V2 placeholder + 2 Phase-4 observability markers + 1 optional external smoke). ruff + mypy clean (broader CI scope src/ tests/ scripts/; experiments/ excluded). ADR-22 + ADR-25..32 + ADR-33 all hold; production CLI + composite action emit no V_net / debt_final / corrupt_success. Reports: phase4_8_provider_regression_deepening.md (new), provider_label_audit_l001_l008.md (V4 Pro), provider_label_audit_l001_l008_v4flash.md (V4 Flash). Per QA/A25.md "Après Phase 4.8": Phase 4.9 = artifact UX polish (recommended next); Phase 5.0 = MCP / provider tool-calling design ADR only (anti-MCP + anti-tool-calling locks must be explicitly removed before); Phase 4.8.1 = failure-path redacted IO capture (V4 Pro 6/8 missing-capture follow-up).

[2026-04-30 11:30:00] - Phase 4.7 FULLY ACCEPTED — DeepSeek V4 Pro real provider regression baseline green (commits c49a155 → ea8ddcd → c1a39b8). 24/24 acceptance criteria from QA/A24.md met. The empirical provider run that was `not_run` in the prior session log entry landed on run id 24953163352 (commit c1a39b8): operator (yannabadie) allocated DEEPSEEK_API_KEY repo secret on 2026-04-26T09:14:48Z; commit c1a39b8 unblocked the run by (a) bumping the DeepSeek profile default model from `deepseek-chat` to `deepseek-v4-pro` (DeepSeek announced 2026-07-24 sunset for the legacy aliases per their official changelog at api-docs.deepseek.com queried 2026-04-26 — base URL `https://api.deepseek.com/v1` UNCHANGED, current V4 lineup is deepseek-v4-pro / deepseek-v4-flash with 1M context + JSON output + tool calls + thinking modes), and (b) adding an optional `model` workflow input. Workflow fired with `provider-profile=deepseek model=deepseek-v4-pro max-provider-cases=4 compare-replay=true`; ran replay first then provider, both passed. Real metrics observed: cases_total=36, cases_evaluated=36, official_field_leak_count=0 for BOTH paths (gate clean — ADR-22 + ADR-32 honored end-to-end), provider_calls_observed=4, provider_calls_skipped_at_cap=0, schema_valid_rate=1.0, invalid_json_count=0, schema_violation_count=0, missing_citation_count=0, confidence_cap_violation_count=0, forbidden_phrase_count=0, provider_unavailable_count=0, timeout_count=0; retention metrics (evidence_ref_precision, evidence_ref_recall, safety_block_rate, fenced_injection_rate, unknown_ref_rejection_rate) all 1.0 on both paths; accuracy delta = provider 0.25 vs replay 0.5 on estimator_status_accuracy + provider 0.5 vs replay 1.0 on estimator_estimate_accuracy. Per ADR-28 + ADR-32 the delta is DATA captured for Phase 4.8 to investigate (per-estimate inspection of which fields the provider misses + whether the unsupported_claims shape matches the expected status mapping); explicitly NOT a verdict on the provider's quality. Artifacts inspected: replay/metrics.json + replay/report.md + deepseek/metrics.json + deepseek/report.md, ZERO raw prompt / raw provider response / API key value / unredacted error in any artifact. Workflow + secret-handling + redaction discipline all worked end-to-end on a clean GitHub-hosted runner. Reports: phase4_7_provider_regression_baseline.md updated with structured YAML markers in §5 / §6 / §7 / §8 / §9 / §14 capturing the empirical data + flipping status from "partially accepted" to "fully accepted". Per QA/A24.md "Après Phase 4.7": Phase 4.8 = real provider regression run + private holdout protocol (now possible because we have data); Phase 4.9 = artifact UX polish; Phase 5.0 = MCP / provider tool-calling design ADR only — anti-MCP and anti-tool-calling locks (test_no_mcp_workflow_or_dependency_added + test_no_provider_tool_calling_enabled in tests/test_phase4_7_provider_baseline.py) hold and must be REMOVED before Phase 5.0 work begins.

[2026-04-30 04:00:00] - Phase 4.7 provider regression baseline (structural surface) + SARIF v4 migration complete (commits c49a155 + docs follow-up). ADR-32 logged. 23/24 acceptance criteria from QA/A24.md met structurally; criterion 24 (real-runner provider-baseline run is green) is not_run with explicit reason — no API budget allocated (per QA/A24.md criterion 12, Phase 4.7 stays partially accepted until empirical provider run lands). 4.7.0 — `.github/workflows/sarif-upload.yml` bumped `github/codeql-action/upload-sarif@v3 → @v4` (v3 deprecated for December 2026; v4 ships native Node 24); validated on real runner: run id 24952767492 green + 6 SARIF analyses ingested at sarif_id `11ad3390-414e-11f1-86c6-63dd82cf10f5` (ruff 125 results, mypy 221 results, plus pytest/hypothesis/semgrep/codeql shells, category=oida-code, all tied to commit c49a155). 4.7.1 — `.github/workflows/provider-baseline.yml` shipped: workflow_dispatch ONLY (no push/pull_request/pull_request_target/schedule); three required inputs (provider-profile choice with deepseek/kimi/minimax/custom_openai_compatible, max-provider-cases default "4" for safe first-run cost, compare-replay default "true"); workflow + job permissions held to `contents: read` (no security-events / actions / checks / contents:write); concurrency cancel-in-progress; secrets enter exactly ONCE in the job's env: map (DEEPSEEK_API_KEY/KIMI_API_KEY/MINIMAX_API_KEY/CUSTOM_OPENAI_COMPATIBLE_API_KEY all sourced from secrets.*) and bash reads them as $VAR; the CLI receives only the env-var NAME via --api-key-env; replay baseline runs FIRST (gated on COMPARE_REPLAY=='true'), provider step runs SECOND with the resolve_key step mapping profile → API_KEY_ENV_NAME via case statement (never reads the value); CLI's existing 4.4 LLMProviderUnavailable handles empty-key case + 4.3.1-A `assert_no_official_field_leaks` exit-3 gate propagates through `set -euo pipefail`; post-step renders ONLY headline metrics (cases_evaluated, official_field_leak_count, estimator_status_accuracy, estimator_estimate_accuracy, estimator_cases_evaluated, estimator_cases_skipped, safety_block_rate, fenced_injection_rate) into per-profile report.md + $GITHUB_STEP_SUMMARY (no raw prompt, no raw provider response, no API key); artifacts uploaded as `provider-baseline-${PROVIDER_PROFILE}` via actions/upload-artifact@v4. tests/test_phase4_7_provider_baseline.py — 17 invariants: 3 SARIF v4 (uses_codeql_action_v4 + no @v3 leftover, v4 still scopes security-events:write to job, v4 no external provider), 12 provider-baseline structural (workflow_exists, is_workflow_dispatch_only, permissions_read_only, has_no_pull_request_target, does_not_run_on_push_or_pr, requires_explicit_provider_profile, default_max_cases_is_small ∈ [1,8], uses_secrets_context_only, does_not_echo_secret_values via regex on echo/printf/tee/pipe + $X_API_KEY, runs_replay_before_external via step-order walk, artifacts_do_not_include_raw_prompt_by_default rejecting --debug-raw-prompt/--store-raw/etc, official_leak_count_failure_path rejecting --allow-leaks/--no-leak-gate AND any || on calibration-eval lines), and 2 anti-MCP / anti-tool-calling (no_mcp_workflow_or_dependency_added scans every workflow + pyproject.toml for "model-context-protocol"/"mcp-server"/`"mcp"` package, no_provider_tool_calling_enabled greps src/oida_code/estimators/provider_config.py for `supports_tools=True`). Phase 4.7's metric set per QA/A24.md §4.7.4 (schema_valid_rate, invalid_json_rate, schema_violation_rate, missing_citation_rate, confidence_cap_violation_rate, forbidden_claim_rate, official_field_leak_count, evidence_ref_precision, evidence_ref_recall, safety_block_rate, fenced_injection_pass_rate, provider_unavailable_count, timeout_count) is partially covered by today's CalibrationMetrics (evidence_ref_precision, evidence_ref_recall, safety_block_rate, fenced_injection_rate, official_field_leak_count, plus 4.4.1 estimator family); the remaining provider-failure-mode counters (invalid JSON, missing citation, schema violation, provider_unavailable, timeout) are deliberately NOT added to the Pydantic model in this commit (would create empty fields with no observed meaning) — Phase 4.7.1 will add them when a real provider run produces non-zero values worth schematising. ci on c49a155 (run id 24952744508): all 6 jobs green incl. Node 24 compat. action-smoke on c49a155 (run id 24952744506): green in 51s. sarif-upload v4 on c49a155 (run id 24952767492): green + ingested. Full suite 558 passed + 4 skipped (V2 placeholder + 2 Phase-4 observability markers + 1 optional external smoke). ruff + mypy clean (84 src files). ADR-22 + ADR-25..31 + ADR-32 all hold; production CLI and the composite action emit no V_net / debt_final / corrupt_success. Reports: phase4_7_provider_regression_baseline.md (new) + README + progress.md updated. Next: operator allocates API budget → fires `gh workflow run provider-baseline.yml -f provider-profile=deepseek -f max-provider-cases=4` → Phase 4.7 §7 populates with run_id + observed metrics → Phase 4.7 flips from "partially accepted" to "accepted end-to-end". Per QA/A24.md "Après Phase 4.7": Phase 4.8 = real provider regression run + private holdout protocol; Phase 4.9 = artifact UX polish; Phase 5.0 = MCP / provider tool-calling design ADR only.

[2026-04-29 03:00:00] - Phase 4.6 real-runner / operator smoke complete (commits d32fc1f + a9de514). ADR-31 logged. 20/20 acceptance criteria from QA/A23.md met. Three new workflows shipped + green on real GitHub-hosted runners: (1) `.github/workflows/ci.yml` gains `node24-compat` job — `FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: "true"` at job scope, runs validator + Phase 4.5 + Phase 4.6 invariant tests under Node 24, read-only perms, no external provider — green on run id 24948296296 alongside the existing 5 jobs (ruff/mypy/pytest/calibration-eval/security-smoke); (2) `.github/workflows/action-smoke.yml` — composite-action consumer smoke fires on push:main + workflow_dispatch, uses `uses: ./` against the local checkout with replay-only inputs (`upload-sarif: false`, `fail-on: none`, `llm-provider: replay`), `fetch-depth: 2` so `base-ref: HEAD~1` resolves, read-only workflow perms, no secrets — green on run id 24948296297 in 51s; (3) `.github/workflows/sarif-upload.yml` — manual workflow_dispatch only, the upload job grants `security-events: write` at JOB scope (never workflow level), uses `github/codeql-action/upload-sarif@v3` for canonical ingestion — green on run id 24948316005, ingestion CONFIRMED via `gh api repos/yannabadie/oida-code/code-scanning/analyses` (6 analyses ingested at 2026-04-26T04:34:44Z, sarif_id=3f40cd46-4129-11f1-90ee-8add715f88c8, category=oida-code, tools: ruff 125 results / mypy 215 results / pytest+hypothesis+semgrep+codeql shells). Latent Phase 4.5 bug surfaced and fixed: action.yml `inputs.repo-path.description` interpolated `${{ github.workspace }}` which GitHub's manifest loader rejects (Phase 4.5 tests parsed YAML structurally but never executed the action runtime, so the bug shipped silently). Fix: rewrite description as plain text + new regression test `test_action_inputs_descriptions_have_no_expression_interpolation` walks every `inputs.<name>.description` and rejects any `${{ ... }}` substring. tests/test_phase4_6_real_runner_smoke.py — 15 invariants total: 4 node24-compat job (presence, FORCE_JAVASCRIPT flag, no-external-provider, read-only perms), 5 action-smoke (file exists, uses local action, replay-only, no pull_request_target, read-only perms), 5 sarif-upload (file exists, workflow_dispatch only, security-events scoped to job, codeql-action used, no provider), 1 action-input-description regression. Two YAML 1.1 tactical helpers in the test file: `_on_block(payload)` handles the `on:` → boolean True trap (PyYAML defaults to YAML 1.1; lookup falls back to `payload.get(True)`), and a regex `_PULL_REQUEST_TARGET_KEY` matches the YAML key only — not the literal in comments. Fork-PR fence smoke + provider regression baseline marked `not_run` with explicit reasons in reports/phase4_6_real_runner_operator_smoke.md §5 and §7 — no fork of yannabadie/oida-code exists at this commit; no API budget allocated (per QA/A23.md "ne fake pas le résultat"). Two non-blocking annotations remain in CI runs: Node 20 deprecation on actions/checkout@v4 / setup-python@v5 / upload-artifact@v4 (mitigated by node24-compat job opt-in env var; full action-vendor bumps when their releases land), and `github/codeql-action/upload-sarif@v3` deprecation announced for December 2026 (Phase 4.7 ticket: bump to v4 when released). Full suite 541 passed + 4 skipped (V2 placeholder + 2 Phase-4 observability markers + 1 optional external smoke). ruff + mypy clean (84 src files). ADR-22 + ADR-25 + ADR-26 + ADR-27 + ADR-28 + ADR-29 + ADR-30 + ADR-31 all hold; production CLI and the composite action emit no V_net / debt_final / corrupt_success. Reports: phase4_6_real_runner_operator_smoke.md (new) + README + progress.md updated. Next per QA/A23.md "Après Phase 4.6": Phase 4.7 = provider regression baseline on calibration_v1 (workflow_dispatch, secret budget required) OR Phase 4.8 = MCP/tool-calling design ADR only OR Phase 4.9 = artifact UX polish — pick one per QA/A24.md once it lands.

[2026-04-28 18:00:00] - Phase 4.5.2 real-runner fix (commit pending). QA/A22.md identified that the first GHA CI run on commit d910006 (run id 24938535270) failed in two jobs: workflow security smoke exit 2 (PyYAML missing on a clean runner because `[project.optional-dependencies].dev` did not declare it) and pytest exit 1 (`tests/test_cli_smoke.py::test_inspect_help_shows` — Rich detects `CI=true` on the GHA Ubuntu runner and forces an 80-column coloured panel that wraps `--base` inside the help table cell, breaking the substring assertion; locally on Windows the same CliRunner produces plain ASCII so the test had been passing). Fixes: (i) pyproject.toml adds `PyYAML>=6.0` to the dev extra with a comment explaining the validator dependency; new regression test `test_dev_extra_includes_pyyaml_for_workflow_validator` parses the dev block and asserts `PyYAML` is present so a future PR cannot drop it without local CI failing first; (ii) tests/test_cli_smoke.py pins `runner = CliRunner(env={"NO_COLOR": "1", "COLUMNS": "200"})` at module scope — applies to all six tests in the file, makes Rich rendering deterministic across Windows local + Linux GHA + macOS, no-color.org convention; (iii) action.yml `enable-shadow` input description updated to say RESERVED / NOT YET WIRED (Option B per QA/A22.md §4.5.2-E) — input still accepted for forward-compat but the composite body does not consume it yet, implementation deferred to Phase 4.6+. Phase 4.5.3 ticket opened (not blocking) for Node 20 deprecation warnings on actions/checkout@v4 / setup-python@v5 / upload-artifact@v4 / codeql-action/upload-sarif@v3; GHA defaults to Node 24 from 2026-06-02. Full suite 526 passed + 4 skipped (V2 placeholder + 2 Phase-4 observability markers + 1 optional external smoke). ruff + mypy clean. Acceptance criterion (QA/A22.md §4.5.2-C): GHA run #2 after this commit must be all green; until then Phase 4.5 remains "shipped structurally, not accepted end-to-end". Reports: phase4_5_ci_github_action.md §12.1 (new) + status banner. Next = watch CI run #2; if green, Phase 4.5 is fully accepted and we await QA/A23.md for Phase 4.6 candidate selection.

[2026-04-28 09:30:00] - Phase 4.4.1 calibration-eval external path alignment + Phase 4.5 CI workflow / composite GitHub Action complete (commit pending). ADR-30 logged. 23/23 acceptance criteria from QA/A21.md met. 4.4.1: src/oida_code/calibration/models.py extended — CalibrationFamily Literal includes "llm_estimator"; new EstimateExpected = Literal["accepted","rejected","unsupported","missing"]; new EstimatorStatusExpected = Literal["blocked","diagnostic_only","shadow_ready","official_ready_candidate"]; new ExpectedEstimateLabel (frozen, extra="forbid", validate_assignment) with field/event_id/expected_status/min_value/max_value/required_evidence_refs; CalibrationCase gains packet_path, llm_response_path, expected_estimator_status, expected_estimates with @model_validator(mode="after") enforcing family-specific invariants (llm_estimator requires packet_path + expected_estimator_status; non-llm families reject those fields). src/oida_code/calibration/runner.py: new evaluate_llm_estimator(case, case_dir, provider) — loads packet, builds FileReplayLLMProvider when provider is None, calls run_llm_estimator(packet, provider), scores estimator_status_match + per-estimate matches via _estimate_matches_label (semantics: "missing" matches None or source="missing"; "rejected" matches absence; "unsupported" matches confidence==0; numeric bounds + required_evidence_refs both checked); CaseResult per-case fields: estimator_status_match, estimator_status_actual, estimator_estimate_match_count, estimator_estimate_total, estimator_skipped, estimator_skipped_reason; aggregate computes estimator_status_accuracy / estimator_estimate_accuracy / estimator_cases_evaluated / estimator_cases_skipped (Optional[float] / int — null when zero cases). src/oida_code/calibration/metrics.py: 4 new estimator fields. src/oida_code/cli.py: calibration-eval gains --llm-provider, --provider-profile, --api-key-env, --model, --base-url, --max-provider-cases, --timeout; cases beyond cap recorded as estimator_skipped=True with reason "max_provider_cases reached" and dropped from headline numerator/denominator. scripts/build_calibration_dataset.py: new build_llm_estimator(out) producing 4 cases L001-L004 (capability_supported_clean→shadow_ready, capability_missing_mechanism→diagnostic_only, benefit_missing_intent→blocked, observability_negative_path→diagnostic_only) each shipping packet.json+llm_response.json+expected.json; manifest now reports 36 cases across 6 families. tests/test_phase4_4_real_provider.py: +9 mandatory 4.4.1 tests. tests/test_phase4_3_calibration.py: renamed test_pilot_dataset_has_all_five_families → test_pilot_dataset_has_all_six_families with llm_estimator added. Phase 4.5: .github/workflows/ci.yml — five jobs (lint/typecheck/test/calibration/security-smoke) all under permissions: contents: read; triggers push on main + pull_request on main + workflow_dispatch (NO pull_request_target); concurrency cancel-in-progress; OIDA_RUN_EXTERNAL_PROVIDER_TESTS=0 pinned in test job env; calibration job uploads .oida/calibration_v1/ artifact via actions/upload-artifact@v4; security-smoke runs validate_github_workflows.py. action.yml at repo root — composite action with 12 inputs (repo-path/base-ref/intent-file/output-dir/upload-sarif/fail-on/surface/enable-shadow/llm-provider [default replay] /provider-profile/api-key-env/max-provider-cases) + 4 outputs (report-json/report-markdown/report-sarif/calibration-metrics); first step blocks llm-provider=='openai-compatible' on fork PRs via github.event.pull_request.head.repo.full_name != github.repository (anti-secret-exfil guard); runs inspect → audit (markdown/json/sarif) → calibration-eval; writes $GITHUB_STEP_SUMMARY with markdown report excerpt + calibration headline; uploads via actions/upload-artifact@v4; SARIF via github/codeql-action/upload-sarif@v3 only when inputs.upload-sarif=='true'; NO ${{ secrets.* }} reference in action body (secrets travel through env: in calling workflow's job, action receives env-var name only via api-key-env); Phase 4.5.1 hardening: every PR-influenced expression (inputs.*, github.action_path) is lifted into the run-audit step's env: map (REPO_PATH/BASE_REF/INTENT_FILE/OUTPUT_DIR/FAIL_ON/LLM_PROVIDER/PROVIDER_PROFILE/API_KEY_ENV/MAX_PROVIDER_CASES/ACTION_PATH) and referenced as $VAR in bash, never inline ${{ ... }} inside the heredoc (shell-injection mitigation per GitHub's documented intermediate-env-var pattern). scripts/validate_github_workflows.py — PyYAML-based static checker: API-key prefix patterns absent, no pull_request_target, top-level permissions: contents: read present, per-job permissions only read/none/security-events: write, no ${{ secrets.* }} in run blocks, no PR-controlled `${{ inputs.* / github.head_ref / github.event.pull_request.* / github.actor / github.triggering_actor / github.event.head_commit.message }}` interpolated inside any run block (Phase 4.5.1 shell-injection hardening, applies to workflow AND composite-action runs), action uses runs.using: composite, llm-provider default = replay, fork-PR guard present (head.repo.full_name), required inputs declared; exits 0 with "OK: workflow + action invariants hold" or 1 on failure or 2 if PyYAML missing. tests/test_phase4_5_ci_github_action.py — 17 invariant tests parsing YAML directly: file existence (3), permissions/triggers (3), action invariants including structural fork-fence (6), Phase 4.5.1 shell-injection hardening (1: no PR-controlled `${{ ... }}` inside run), output safety (1), validator integration (3 incl. shell-injection fixture). Full suite 525 passed, 4 skipped (V2 placeholder + 2 Phase-4 observability markers + 1 optional external smoke). ruff clean across src/+tests/+scripts; mypy clean (84 source files). ADR-22 + ADR-25 + ADR-26 + ADR-27 + ADR-28 + ADR-29 + ADR-30 all hold; production CLI and the composite action emit no V_net / debt_final / corrupt_success — the action's outputs are name-checked against the forbidden phrase set in tests. Reports: phase4_5_ci_github_action.md (new) + phase4_4_real_provider_binding.md §16 (4.4.1 alignment section appended). Next = Phase 4.6 candidate set (real-runner smoke / provider regression baseline / MCP+tool-calling) — pick one per QA/A22.md once it lands.
[2026-04-25 14:30:00] - Phase 3.5 Block E2 complete (commit pending). ADR-23 logged. KEEP V1 with two minor revisions already shipped (missing-grounding fix in E1.1, edge_confidences override here). 17 new tests in `tests/test_e2_shadow_formula_decision.py` (16 pass + 1 V2 placeholder skip), full suite 277/278. Sensitivity sweep (`scripts/evaluate_shadow_formula.py`) shows delta=0.0 across 26 inputs; graph ablation 7/7 invariants hold; real-repo shadow smoke (`scripts/real_repo_shadow_smoke.py`) PASS on oida-code self + attrs (`has_forbidden_summary_keys = False`, `authoritative = False`, `readiness_status = blocked`). Report: `reports/e2_shadow_formula_decision.md`. Next = E3 (estimator contracts) per QA/A13.md §"What changes before E3".
[2026-04-27 11:00:00] - Phase 4.3.1 calibration hardening + Phase 4.4 real provider binding complete (commit pending). ADR-29 logged. 22/22 acceptance criteria from QA/A20.md met. 4.3.1-A: official_field_leak_count is now `int = Field(ge=0)` (was `Literal[0]`); aggregate emits the real sum; new `assert_no_official_field_leaks` helper + `OfficialFieldLeakError`; `run_calibration_eval.py` exits 3 on any positive leak count; calibration-eval CLI subcommand added with the same gate. 4.3.1-B: F2P/P2P metrics now `Optional[float]` + new `code_outcome_status: Literal["not_computed","from_stability_report"]`; aggregate accepts a `stability_report` arg and folds unanimous-pass counts; flaky cases drop from BOTH numerator and denominator; missing report yields null + "not_computed". 4.3.1-C: `_check_safety_fences` uses an exact regex bounded to the data region between `EVIDENCE:` and `DETERMINISTIC_ESTIMATES:`; per-id matching open + close required; kind mismatch rejected; duplicate / forged closes rejected; hostile needles must sit inside spans. Phase 4.4: src/oida_code/estimators/provider_config.py with frozen ProviderProfile (no API-key field; only api_key_env name; Literal[ProviderName, ApiStyle]; predefined deepseek/kimi/minimax profiles via get_predefined_profile). src/oida_code/estimators/providers/openai_compatible.py with OpenAICompatibleChatProvider (speaks OpenAI Chat Completions over urllib.request; injectable HttpPostFn for tests; redact_secret helper; ProviderRawResponse stores prompt SHA256 not full prompt; status 0 → LLMProviderUnavailable / LLMProviderTimeout; status >=400 → LLMProviderError with body redacted; missing choices[0].message.content → LLMProviderInvalidResponse). CLI: estimate-llm gains --llm-provider openai-compatible + --provider-profile + --api-key-env + --model + --base-url; calibration-eval subcommand wraps the runner with stability fold-in and exits 3 on leak. 13 new tests for 4.3.1 + 26 new tests for 4.4 (24 mandatory + 1 unsupported case + 1 optional external smoke skipped unless OIDA_RUN_EXTERNAL_PROVIDER_TESTS=1 + relevant key). Full suite 499/503 (4 documented skips: V2 placeholder + 2 Phase-4 observability markers + 1 optional external smoke). pyproject.toml registers `external_provider` pytest marker. Pilot calibration eval still reports zero leaks. ADR-22 + ADR-25 + ADR-26 + ADR-27 + ADR-28 + ADR-29 all hold; production CLI emits no V_net / debt_final / corrupt_success and the openai-compatible provider goes through the same `LLMEstimatorOutput` validator as the replay path so contract compliance is identical. Reports: phase4_4_real_provider_binding.md. Next = Phase 4.5 (CI / GitHub Action integration) per QA/A20.md §"Après Phase 4.4".
[2026-04-26 14:30:00] - Phase 4.2.1 hardening + Phase 4.3 calibration dataset design complete (commit pending). ADR-28 logged. 24/24 acceptance criteria from QA/A19.md met. 4.2.1: phase4_2 report § 4.1.1 + prompt_injection_in_tool_output now spell out `<<<OIDA_EVIDENCE id="[E.x.y]" kind="...">>>` ... `<<<END_OIDA_EVIDENCE id="[E.x.y]">>>` explicitly (no `<<...>>` shorthand); engine pre-clamps per-tool max_runtime_s to remaining global budget AND blocks without invocation when budget exhausted; honors max(wall_clock, executor_reported runtime_ms); 3 new tests. Phase 4.3: src/oida_code/calibration/ with models.py (CalibrationCase + ExpectedClaimLabel + ExpectedToolResultLabel + ExpectedCodeOutcome with @model_validator requiring ≥1 F2P + ExpectedRepairBehavior + CalibrationProvenance + CalibrationManifest with Literal[False] official_vnet_allowed pin), metrics.py (CalibrationMetrics with Literal[0] official_field_leak_count pin + macro_f1_from_confusion + precision/recall/safe_rate/pairwise_order_accuracy helpers), runner.py (per-family evaluators dispatching to claim_contract/tool_grounded/shadow_pressure/code_outcome/safety_adversarial + aggregate that excludes contamination_risk=public_high cases from headline metrics). datasets/calibration_v1/ with manifest + 32 hermetic cases (8 claim_contract + 8 tool_grounded + 6 shadow_pressure + 6 code_outcome with seeded synthetic divide-by-zero F2P/P2P + 4 safety_adversarial including prompt_injection_in_code_comment, prompt_injection_in_tool_output, forged_evidence_id, fence_close_attempt). Three operator scripts: build_calibration_dataset.py (deterministic builder), run_calibration_eval.py (emits .oida/calibration_v1/metrics.json + report.md), check_calibration_stability.py (3× pytest stability runs for code_outcome with shell=False). 22 new tests in tests/test_phase4_3_calibration.py covering schema invariants (frozen, family-specific validators, official_vnet_allowed pin, official_field_leak_count pin), metric helpers (macro-F1 imbalanced/balanced, pairwise order, safe_rate), per-family runner correctness on tiny synthetic cases, and pilot dataset smoke (all 32 cases load + run with 0 leaks). Plus 3 new tests for 4.2.1 in tests/test_phase4_2_tool_grounded_verifier.py. Full suite 462/465 (3 documented skips). pyproject.toml: mypy excludes datasets/ (the 6 code_outcome cases ship intentional duplicate src/ layouts). Pilot eval reports all behavioural metrics at 1.0 with leaks=0; ADR-28 §4.3-G explicitly forbids treating the pilot as production thresholds — the dataset's job is self-test of the measurement pipeline. ADR-22 + ADR-25 + ADR-26 + ADR-27 + ADR-28 all hold; production CLI emits no V_net / debt_final / corrupt_success. Reports: phase4_3_calibration_dataset_design.md. Next = Phase 4.4 (real provider binding behind explicit opt-in) per QA/A19.md §"Après Phase 4.3".
[2026-04-26 10:00:00] - Phase 4.1.1 hardening + Phase 4.2 bounded tool-grounded verifier loop complete (commit pending). ADR-27 logged. 24/24 acceptance criteria from QA/A18.md met. 4.1.1: phase4_1 report § fixture table + bullet now spell out OIDA_EVIDENCE fences explicitly; aggregator now rejects claims/backwards whose event_id != forward.event_id and reads tool-failure on claim.event_id (not forward.event_id). 3 new tests in tests/test_phase4_1_verifier_contract.py. Phase 4.2: src/oida_code/verifier/tools/ with contracts.py (ToolPolicy/VerifierToolRequest/VerifierToolResult, frozen, deny-default network/write), sandbox.py (path traversal + deny patterns + truncate-and-hash + absolute-path detection BEFORE normalisation, _normalise no longer strips leading dot), adapters.py (ruff/mypy/pytest deterministic adapters; build_argv pure; subprocess.run with shell=False, capture_output, timeout), registry.py (allowlist), __init__.py (ToolExecutionEngine; executor resolved at call time via attribute lookup so monkey-patching works). CLI: oida-code run-tools <requests.json> --policy <policy.json> --out <results.json>. 34 new tests in tests/test_phase4_2_tool_grounded_verifier.py covering schema invariants, sandbox unit tests, adapter argv+parse, engine budget enforcement (max_tool_calls extras appended in order, max_total_runtime_s budget), 8 inline parametrised hermetic fixtures (ruff/mypy contradicts, pytest negative-path/scoped-pass, tool_missing/timeout, path_traversal_blocked, prompt_injection_in_tool_output), CLI smoke + path-traversal smoke. Full suite 437/440 (3 documented skips). Two-pass loop is operator-driven (run-tools + verify-claims chain) per QA/A18.md preference; verify-grounded umbrella deferred to 4.2.x. OptionalExternalVerifierProvider remains a Phase 4.2.x stub. ADR-22 + ADR-25 + ADR-26 + ADR-27 hold; production CLI emits no V_net / debt_final / corrupt_success. Reports: phase4_2_tool_grounded_verifier_loop.md. Next = Phase 4.3 (calibration dataset design) per QA/A18.md §"Après Phase 4.2".
[2026-04-25 21:15:00] - Phase 4.0.1 fence hardening + Phase 4.1 forward/backward verifier contract complete (commit pending). ADR-26 logged. 21/21 acceptance criteria from QA/A16.md met. 4.0.1: replaced legacy `<<<EVIDENCE_BLOB ...>>>` with named per-item fences `<<<OIDA_EVIDENCE id="..." kind="...">>>` ... `<<<END_OIDA_EVIDENCE id="...">>>`; inner attempts to forge a closing fence are neutralised with a zero-width space; Phase 4.0 report updated; 5 new tests pin the fence name and behaviour. Phase 4.1: src/oida_code/verifier/ sub-package with contracts.py (VerifierClaim with 7 allowed claim_types + Literal[False] is_authoritative, ForwardVerificationResult, BackwardRequirement, BackwardVerificationResult, VerifierAggregationReport with Literal[False] authoritative pin, VerifierToolCallSpec for description-only tool calls), aggregator.py (7-rule aggregation: forward + backward + evidence + tool non-contradiction + claim-type allowlist + cap + forbidden phrases), forward_backward.py (run_verifier driver, never raises), replay.py (Fake/FileReplay/OptionalExternalVerifierProvider — no API call by default, env var never echoed). 8 hermetic fixtures (forward_supported_backward_supported, forward_supported_backward_missing_negative_path, forward_overclaims_capability, backward_requires_missing_intent, tool_failure_contradicts_claim, unknown_evidence_ref, prompt_injection_claim_payload, repair_needed_supported). CLI: `oida-code verify-claims <packet> --forward-replay <r> --backward-replay <r>`. 31 new tests in tests/test_phase4_1_verifier_contract.py. Full suite 400/403 (3 documented skips). ToolCallSpec exists but Phase 4.1 explicitly does not execute it; `test_tool_call_specs_are_not_executed_in_phase4_1` is the canary. Reports: phase4_1_forward_backward_contract.md. Next = Phase 4.2 (tool-grounded verifier loop) per QA/A16.md §"Après Phase 4.1".
[2026-04-25 18:45:00] - Phase 4.0 LLM estimator dry-run complete (commit pending). ADR-25 logged. 21/21 acceptance criteria from QA/A15.md met. Deliverables: (4.0-A) `src/oida_code/estimators/llm_provider.py` with LLMProvider Protocol + FakeLLMProvider (deterministic, tests-only) + FileReplayLLMProvider (reads JSON from disk) + OptionalExternalLLMProvider (opt-in stub; no API call by default; never echoes secret values). (4.0-B) `src/oida_code/estimators/llm_prompt.py` with EvidenceItem + LLMEvidencePacket (frozen, citable [E.kind.idx] IDs); render_prompt wraps user-supplied text in `<<<EVIDENCE_BLOB ...>>>` data-fences and explicitly tells the LLM that fenced text is data, not instruction. (4.0-C) `src/oida_code/estimators/llm_estimator.py` with run_llm_estimator() — never raises; invalid JSON / schema violation / forbidden phrases / cap breaches / missing citations / contradictions with deterministic tools all become blockers/warnings. (4.0-D) 8 hermetic fixtures under `tests/fixtures/llm_estimator_dryrun/`: capability_supported_by_guard, capability_missing_mechanism, benefit_missing_intent, benefit_intent_aligned, observability_tests_only, observability_negative_path_present, llm_overclaims_without_evidence, prompt_injection_in_code_comment. (4.0-E) `oida-code estimate-llm` CLI subcommand (separate from score-trace per QA/A15.md preference). (4.0-F) Tests verify official summary fields stay null and `official_ready_candidate` never appears across all 8 fixtures. 32 new tests in `tests/test_phase4_0_llm_estimator_dryrun.py` (8 hermetic + 24 unit incl. provider security tests + cross-fixture observability monotonicity). Full suite 364/367 (3 documented skips). Repo + history scanned for committed keys before shipping; clean. ADR-22 + ADR-25 hold: production CLI emits no V_net. Report: `reports/phase4_0_llm_estimator_dryrun.md`. Next = Phase 4.1 (forward/backward verifier contract) per QA/A15.md §"Après Phase 4.0".
[2026-04-25 16:30:00] - Phase 3.5 Block E3 complete (commit pending). ADR-24 logged. 19/19 acceptance criteria from QA/A14.md met. Deliverables: (E3.0) `src/oida_code/score/event_evidence.py` with `EventEvidenceView` + per-event helpers; `src/oida_code/score/mapper.py` gains `build_scoring_inputs`, `edge_confidences_from_dependency_graph`, `ScoringInputs` (non-breaking — `obligations_to_scenario` is now a thin wrapper). (E3.1) `src/oida_code/estimators/contracts.py` with frozen `SignalEstimate` + `EstimatorReport` (model validators reject default+confidence>0, missing+confidence>0, llm+authoritative, etc.). (E3.2) `src/oida_code/estimators/deterministic.py` with capability/benefit/observability + completion/tests_pass/operator_accept baselines. (E3.3) `src/oida_code/estimators/llm_contract.py` with `LLMEstimatorInput` / `LLMEstimatorOutput` (caps confidence at 0.6 LLM-only, 0.8 hybrid; requires citations). **No LLM is called.** (E3.4) `src/oida_code/estimators/readiness.py` with `assess_estimator_readiness` ladder (blocked/diagnostic_only/shadow_ready/official_ready_candidate); CLI score-trace now emits `payload["estimator_readiness"]` alongside the official `payload["readiness"]`. 57 new tests across `tests/test_e3_evidence_plumbing.py` (15) and `tests/test_e3_estimator_contracts.py` (42). Full suite 332/335 (3 skips = V2 placeholder + 2 Phase-4 observability markers). Differentiation fixture proves shadow pressure varies with evidence (B - A >= 0.10) — the LLM estimators in Phase 4 will arrive on a real surface. ADR-22 still holds: `total_v_net` / `debt_final` / `corrupt_success` remain null/blocked. Report: `reports/e3_estimator_contracts.md`. Next = Phase 4.0 (LLM estimator dry-run on hermetic fixtures).

[2026-04-26 23:30:00] - Phase 5.2 gateway-grounded verifier loop complete (commit pending). ADR-37 logged. 27/27 acceptance criteria from QA/A29.md met (criterion #27 GitHub-hosted run captured in follow-up commit). NEW src/oida_code/verifier/gateway_loop.py (~340 LOC) hosting tool_request_from_spec mapper, deterministic_estimates_from_tool_result classifier (failed=>negative tool estimate; error/timeout/tool_missing/blocked=>uncertainty), GatewayGroundedVerifierRun model (frozen, extra=forbid), run_gateway_grounded_verifier two-pass runner (max_passes=2 implicit, max_tool_calls=5 default), and _enforce_pass2_tool_citation helper that demotes pass-2 accepted claims that do not cite enriched [E.tool_output.*] refs (when tools ran). 5.1.1 hardening on src/oida_code/verifier/tool_gateway/gateway.py: new Stage-0 mismatch check blocks request when request.tool != gateway_definition.tool_name BEFORE the registry lookup; emits one audit event with policy_decision=block; reason names both fields verbatim. _blocked_result now populates blockers=(reason,) alongside warnings=(reason,) on every blocked path (registry miss, hash drift, sandbox violation, mismatch, adapter exception). src/oida_code/verifier/contracts.py: ForwardVerificationResult gains requested_tools: tuple[VerifierToolCallSpec, ...] = () (default empty for Phase 4.1 replay backward-compat). src/oida_code/cli.py: NEW oida-code verify-grounded subcommand with required --forward-replay-{1,2}, --backward-replay-{1,2}, --tool-policy, --approved-tools, --gateway-definitions; default --audit-log-dir=.oida/tool-gateway/audit, --out=.oida/verifier/grounded_report.json, --max-tool-calls=5; not integrated into oida-code audit and not the default action.yml flow. action.yml: NEW enable-tool-gateway reserved input with default "false". NEW .github/workflows/gateway-grounded-smoke.yml on workflow_dispatch + push to main only, replay-only, permissions: contents: read, no external provider, no secrets, no MCP, no network egress. NEW tests/fixtures/gateway_grounded_verifier/ with 8 hermetic scenarios (no_tool_needed_claim_supported, tool_needed_then_supported, tool_needed_but_unapproved, tool_hash_drift, tool_failed_contradicts_claim, tool_error_uncertainty, path_traversal_blocked, prompt_injection_in_tool_output) plus README.md describing each scenario. 45 new tests in tests/test_phase5_2_gateway_grounded_verifier.py covering: 5.1.1 hardening (4 tests including test_gateway_blocks_request_tool_definition_mismatch + test_gateway_mismatch_writes_audit_event + test_gateway_blocked_result_sets_blockers + test_gateway_adapter_exception_sets_blockers); 5.2-A (4 tests including test_legacy_forward_replay_without_requested_tools_still_validates + test_requested_tools_do_not_execute_during_plain_verify_claims); 5.2-B (4 tests including test_tool_call_spec_never_contains_argv asserting forbidden field set {argv, command, shell, shell_command} disjoint from both schemas); 5.2-D (5 tests including test_failed_tool_result_creates_negative_deterministic_estimate + test_tool_error_is_uncertainty_not_negative_proof + test_tool_missing_is_uncertainty_not_negative_proof + test_blocked_tool_blocks_claim_but_not_code_failure + test_tool_contradiction_rejects_llm_claim_after_gateway); 5.2-C (8 tests including test_gateway_loop_pass1_tool_request_becomes_gateway_call + test_gateway_loop_appends_tool_evidence_to_packet + test_gateway_loop_pass2_claim_must_cite_tool_evidence + test_gateway_loop_aggregator_accepts_only_after_backward_support + test_gateway_loop_blocks_when_tool_not_approved + test_gateway_loop_respects_max_tool_calls + test_gateway_loop_never_calls_gateway_when_no_requested_tools + test_gateway_loop_official_fields_absent); 5.2-E (6 tests including test_verify_grounded_cli_writes_report + test_verify_grounded_cli_writes_audit_log + test_verify_grounded_cli_official_fields_absent + test_action_enable_tool_gateway_default_false); 5.2-F (5 fixture-driven tests including test_fixture_prompt_injection_tool_output_is_data); 5.2-G (4 workflow tests asserting replay-only triggers + no external provider + read-only permissions + no MCP); 5.2-H (5 no-MCP regression locks including test_no_mcp_dependency_added + test_no_mcp_workflow_added + test_no_jsonrpc_tools_list_or_tools_call_runtime + test_no_provider_tool_calling_enabled + test_gateway_loop_is_not_mcp). Quality gates: ruff clean (full curated CI scope src/ tests/ scripts/), mypy clean (89 source files — +1 from Phase 5.1 88, all from new gateway_loop module), 730 passed / 4 skipped (was 685/4 before Phase 5.2 — exactly +45 new tests). ZERO new dependency in pyproject.toml. ZERO MCP runtime code. ZERO provider tool-calling. ADR-22 hard wall holds (no V_net / debt_final / corrupt_success emission). The Phase 4.7 + Phase 5.0 + Phase 5.1 anti-MCP / anti-tool-calling locks remain ACTIVE; Phase 5.2 ADDS test_gateway_loop_is_not_mcp + 4 sibling tests on the new module specifically. Honesty statement (verbatim per QA/A29 lines 412-419): Phase 5.2 integrates the local deterministic tool gateway into the verifier loop; it does not implement MCP; it does not enable provider tool-calling; it does not execute remote tools; it does not allow write tools or network egress; it does not validate production predictive performance; it does not emit official total_v_net / debt_final / corrupt_success; it does not modify the vendored OIDA core. Per QA/A29 "Apres Phase 5.2": option recommandee = Phase 5.3 private holdout + verifier loop calibration (mesure si le gateway-grounded verifier ameliore citation fidelity / claim acceptance correctness / tool contradiction rejection / unsupported claim detection — toujours sans MCP). Option alternative = Phase 5.3 local stdio MCP mock prototype, contingent on gateway loop + audit log + hash drift quarantine + path traversal/secret/network/write blocks all operational AND GitHub-hosted CI staying green. QA/A29 prefers holdout/calibration before MCP.

[2026-04-26 23:45:00] - Phase 5.2 FULLY ACCEPTED — gateway-grounded verifier loop (commit 6b741ee). ADR-37 logged. 27/27 acceptance criteria from QA/A29.md met. Criterion #27 (real-runner CI / action-smoke run after Phase 5.2) closed by four real-runner runs all green on commit 6b741ee: ci (run id 24959872919, 1m15s), action-smoke (run id 24959872926, 1m01s — composite action ran end-to-end with verify-grounded NOT wired into action.yml audit path because enable-tool-gateway defaults to "false" per QA/A29 §5.2-E line 274), provider-baseline-node24-smoke (run id 24959872917, 19s), and the new gateway-grounded-smoke (run id 24959872936, 22s — replay-only smoke job running pytest tests/test_phase5_2_gateway_grounded_verifier.py with permissions: contents: read, no external provider, no secrets). Quality gates: ruff clean, mypy clean (89 source files), 730 passed / 4 skipped (was 685/4 before Phase 5.2 — exactly +45 new tests). ZERO new dependency in pyproject.toml. ZERO MCP runtime code. ZERO provider tool-calling. ADR-22 hard wall holds. Honesty statement (verbatim per QA/A29 lines 412-419): Phase 5.2 integrates the local deterministic tool gateway into the verifier loop; it does not implement MCP; it does not enable provider tool-calling; it does not execute remote tools; it does not allow write tools or network egress; it does not validate production predictive performance; it does not emit official total_v_net / debt_final / corrupt_success; it does not modify the vendored OIDA core. Per QA/A29 "Apres Phase 5.2": option recommandee = Phase 5.3 private holdout + verifier loop calibration (toujours sans MCP). Option alternative = Phase 5.3 local stdio MCP mock prototype, contingent on gateway loop + audit log + drift quarantine + path traversal/secret/network/write blocks all operational AND GitHub-hosted CI staying green.

[2026-04-26 23:55:00] - Phase 5.3 gateway verifier calibration complete (commit pending). ADR-38 logged. 29/29 acceptance criteria from QA/A30.md met (criterion #29 GitHub-hosted run captured in follow-up commit). 5.2.1-A: Phase 5.2 report fence wording fixed (line 189) — was bare prefix <<<OIDA_EVIDENCE>>>, now explicit named per-item form <<<OIDA_EVIDENCE id=... kind=...>>> ... <<<END_OIDA_EVIDENCE id=...>>>. Canary test_phase5_2_report_uses_current_fence_constant imports FENCE_NAME from oida_code.estimators.llm_prompt and verifies the report references the live constant. 5.2.1-B: new _enforce_requested_tool_evidence helper in src/oida_code/verifier/gateway_loop.py runs BEFORE _enforce_pass2_tool_citation. When forward.requested_tools non-empty AND tool_phase.new_evidence==(), the helper adds one of three sub-case blockers (missing definition / all calls blocked / gateway ran with no findings), demotes EVERY pass-2 accepted claim to unsupported, and forces report status off verification_candidate. _ToolPhaseOutput gains requested_count: int and missing_definition_tools: tuple[str, ...]; missing definitions now flow into blockers (not just warnings) matching the 5.1.1 hardening pattern. 5.2.1-C: VerifierToolCallSpec gains optional requested_by_claim_id: str | None = None; tool_request_from_spec resolves explicit kwarg first, falls back to spec attribution. NEW src/oida_code/calibration/gateway_holdout.py with ExpectedVerifierOutcome (accepted/unsupported/rejected claim_ids + blockers/warnings_expected, all tuple-based) and GatewayHoldoutExpected (case_id, expected_baseline, expected_gateway, expected_delta Literal[improves|same|worse_expected|not_applicable], required_tool_evidence_refs, forbidden_acceptance_reasons; frozen + extra=forbid). NEW src/oida_code/calibration/gateway_calibration.py (~330 LOC) hosting CalibrationCaseEntry + GatewayCalibrationManifest schemas, run_calibration emitting five artifacts (baseline_metrics.json, gateway_metrics.json, delta_metrics.json with delta_diagnostic_only=true flag + verbatim RESERVED warning, failure_analysis.md with seven-column table + classification legend, artifact_manifest.json with SHA256 hashes), seven canonical FAILURE_CLASSIFICATIONS (label_too_strict, gateway_bug, tool_adapter_bug, aggregator_bug, citation_gap, insufficient_fixture, expected_behavior_changed). NEW scripts/run_gateway_calibration.py Typer-free CLI wrapper. NEW datasets/private_holdout_v2/ with README.md (cross-link to v1, contamination policy synthetic/private/public_low/public_high with public_high excluded from headline metrics, F2P/P2P inheriting from Phase 4.3 CalibrationCase, anti-mutation invariant, 24-case pilot slate documented) + manifest.example.json (4 example slate entries) + cases/ (gitignored). .gitignore extended for datasets/private_holdout_v2/cases/. NEW .github/workflows/gateway-calibration.yml on workflow_dispatch + push to main only, replay-only, permissions: contents: read, no external provider, no secrets, no MCP, no network egress, no SARIF upload (calibration is measurement not code-scanning). 33 new tests in tests/test_phase5_3_gateway_calibration.py covering 5.2.1-A canary (1), 5.2.1-B blockers (4), 5.2.1-C field (4), 5.3-C schemas (4), 5.3-A/F dataset (4), 5.3-D runner + 5.3-E failure analysis (6 including test_calibration_runner_does_not_mutate_dataset), 5.3-G no-MCP locks (4), 5.3-H workflow (6 including test_gateway_calibration_workflow_no_sarif_upload). Quality gates: ruff clean (full curated CI scope src/ tests/ scripts/), mypy clean (92 source files — +3 from Phase 5.2 89), 763 passed / 4 skipped (was 730/4 before Phase 5.3 — exactly +33 new tests). ZERO new dependency in pyproject.toml. ZERO MCP runtime code. ZERO provider tool-calling. ADR-22 hard wall holds. The Phase 4.7 + Phase 5.0 + Phase 5.1 + Phase 5.2 anti-MCP locks remain ACTIVE; Phase 5.3 ADDS 4 more locks on the calibration script + workflow. Honesty statement (verbatim per QA/A30 lines 410-416): Phase 5.3 calibrates the gateway-grounded verifier loop on controlled holdout cases; it does not implement MCP; it does not enable provider tool-calling; it does not validate production predictive performance; it does not tune production thresholds; it does not emit official total_v_net / debt_final / corrupt_success; it does not modify the vendored OIDA core. Per QA/A30 "Apres Phase 5.3": if calibration shows clear improvement, Phase 5.4 = integrate gateway-grounded verifier as opt-in action path (still with enable-tool-gateway=false default); if improvement is weak/negative, Phase 5.4 = fix verifier prompts/labels/tool-request policy; only much later, Phase 5.5+ = local stdio MCP mock prototype.

[2026-04-27 00:15:00] - Phase 5.3 FULLY ACCEPTED — gateway verifier calibration (commit 103b2ff). ADR-38 logged. 29/29 acceptance criteria from QA/A30.md met. Criterion #29 (real-runner CI / gateway-calibration run after Phase 5.3) closed by FIVE real-runner runs all green on commit 103b2ff: ci (run id 24961882218, 1m13s), action-smoke (run id 24961882214, 1m08s — composite action ran end-to-end with verify-grounded NOT wired into action.yml audit path because enable-tool-gateway defaults to "false"), provider-baseline-node24-smoke (run id 24961882219, 20s), gateway-grounded-smoke (run id 24961882225, 19s — replay-only Phase 5.2 smoke), and the new gateway-calibration (run id 24961882238, 20s — replay-only Phase 5.3 smoke executing scripts/run_gateway_calibration.py against the v2 manifest example, asserting the five expected artifacts exist, uploading them under actions/upload-artifact@v4, no SARIF upload). Quality gates: ruff clean, mypy clean (92 source files), 763 passed / 4 skipped (was 730/4 before Phase 5.3 — exactly +33 new tests). ZERO new dependency in pyproject.toml. ZERO MCP runtime code. ZERO provider tool-calling. ADR-22 hard wall holds. Honesty statement (verbatim per QA/A30 lines 410-416): Phase 5.3 calibrates the gateway-grounded verifier loop on controlled holdout cases; it does not implement MCP; it does not enable provider tool-calling; it does not validate production predictive performance; it does not tune production thresholds; it does not emit official total_v_net / debt_final / corrupt_success; it does not modify the vendored OIDA core. Per QA/A30 "Apres Phase 5.3": if calibration shows clear improvement, Phase 5.4 = integrate gateway-grounded verifier as opt-in action path (enable-tool-gateway=false default); if improvement is weak/negative, Phase 5.4 = fix verifier prompts/labels/tool-request policy; only much later Phase 5.5+ = local stdio MCP mock prototype.

[2026-04-27 00:30:00] - Phase 5.4 real gateway calibration complete (commit pending). ADR-39 logged. 27/27 acceptance criteria from QA/A31.md met (criterion #27 GitHub-hosted run captured in follow-up commit). NEW datasets/gateway_holdout_public_v1/ ships 8 fully-committed synthetic cases (no gitignore entry — fully public): tool_needed_then_supported (improves), claim_supported_no_tool_needed (same — sentinel), tool_failed_contradicts_claim (improves), tool_requested_but_blocked (worse_expected — empty admission), hash_drift_quarantine (worse_expected — drifted definition), prompt_injection_in_tool_output (improves — pass-2 declines on contaminated stdout), negative_path_missing (improves — observability claim demoted), f2p_p2p_regression (improves — P2P-style failure rejects fix). Each case carries 11 required files (packet.json + baseline_{forward,backward}.json + gateway_pass{1,2}_{forward,backward}.json + tool_policy.json + gateway_definitions.json + approved_tools.json + expected.json) + optional executor.json + per-case README. src/oida_code/calibration/gateway_calibration.py rewritten from ~330 LOC stub to ~720 LOC real-execution: _run_one_case loads each case, drives run_verifier baseline + run_gateway_grounded_verifier gateway, compares against GatewayHoldoutExpected.expected_baseline/expected_gateway, accumulates per-mode _PerModeMetrics (now 14 fields including claim_accept_accuracy + claim_macro_f1 + fresh_tool_ref_citation_rate + tool_contradiction_rejection_rate + evidence_ref_precision/recall + official_field_leak_count), classifies per-case outcome (8 classifications including new tool_request_policy_gap). Per-case audit logs land under <out>/audit/<case_id>/<yyyy-mm-dd>/<tool>.jsonl (NOT .oida/tool-gateway/audit/ — calibration runs deliberately stay out of operator namespace). New decision_summary.json artifact with 5-value recommendation Literal (integrate_opt_in | revise_prompts | revise_labels | revise_tool_policy | insufficient_data) + recommendation_diagnostic_only=true flag + verbatim reserved warning. Pilot run on 8-case slate: claim_accept_accuracy_delta=0.0, claim_macro_f1_delta=+0.6667, evidence_ref_precision_delta=+0.3333, evidence_ref_recall_delta=+0.2, tool_contradiction_rejection_rate_delta=+0.4, fresh_tool_ref_citation_rate=0.5, official_field_leak_count=0; gateway_improves_count=5, gateway_same_count=1, gateway_worse_count=2; recommendation=insufficient_data (n=8 below 12-case threshold — Phase 5.4 deliberately refuses to promote on a small sample even with positive secondary deltas, preserving the Phase-3 discipline). Failure analysis Markdown extended with two new columns (actual_delta + label_change_proposed) and the tool_request_policy_gap classification. NO automatic label mutation — runner is read-only over BOTH datasets (test_runner_does_not_mutate_public_holdout snapshots mtimes). .github/workflows/gateway-calibration.yml updated to point at the public dataset, asserts SIX expected artifacts (added decision_summary.json), runs inline official_field_leak_count==0 gate before uploading via actions/upload-artifact@v4. New scripts/_build_phase5_4_cases.py fixture builder (templates the 7 cases beyond the hand-crafted reference case 1; deterministic regeneration tool). 23 new tests in tests/test_phase5_4_real_calibration.py covering 7 sub-blocks. 2 Phase-5.3 test updates align with the schema extension (extended failure_analysis columns + 8th classification). Quality gates: ruff clean (full curated CI scope), mypy clean (92 source files; same count as Phase 5.3 because the rewrite stays inside the same module dir), 786 passed / 4 skipped (was 763/4 before Phase 5.4 — exactly +23 new tests). ZERO new dependency in pyproject.toml. ZERO MCP runtime code. ZERO provider tool-calling. ADR-22 hard wall holds. The Phase 4.7 + Phase 5.0 + Phase 5.1 + Phase 5.2 + Phase 5.3 anti-MCP locks remain ACTIVE; Phase 5.4 ADDS test_no_mcp_runtime_in_calibration_module + test_no_provider_tool_calling_enabled_in_phase5_4 + test_action_yml_does_not_default_enable_tool_gateway_true. Honesty statement (verbatim per QA/A31 lines 379-386): Phase 5.4 measures the gateway-grounded verifier on runnable holdout fixtures; it does not implement MCP; it does not enable provider tool-calling; it does not make verify-grounded default; it does not validate production predictive performance; it does not tune production thresholds; it does not emit official total_v_net / debt_final / corrupt_success; it does not modify the vendored OIDA core. Per QA/A31 "Apres Phase 5.4": pilot recommendation insufficient_data (n=8) even with positive secondary deltas — Phase 5.5 = either expand runnable slate to 12+ cases OR revise verifier prompts/labels/tool-request policy; MCP stays explicitly deferred to Phase 5.6+ at the earliest, contingent on Phase 5.5 producing a clear integrate_opt_in recommendation.

[2026-04-27 00:50:00] - Phase 5.4 FULLY ACCEPTED — real gateway calibration on runnable holdout (commit 28728cb). ADR-39 logged. 27/27 acceptance criteria from QA/A31.md met. Criterion #27 (real-runner gateway-calibration run on the runnable subset) closed by FIVE real-runner runs all green on commit 28728cb: ci (run id 24963499030, 1m18s), action-smoke (run id 24963499027, 1m04s — composite action ran end-to-end with verify-grounded NOT wired into action.yml because enable-tool-gateway defaults to "false"), provider-baseline-node24-smoke (run id 24963499016, 24s), gateway-grounded-smoke (run id 24963499025, 24s — Phase 5.2 replay-only smoke), and gateway-calibration (run id 24963499014, 28s — Phase 5.4 NOW pointing at datasets/gateway_holdout_public_v1/manifest.json with 8 fully-committed synthetic cases, asserting 6 expected artifacts including decision_summary.json, running inline official_field_leak_count==0 gate, uploading via actions/upload-artifact@v4). Pilot calibration result on the public 8-case slate: claim_accept_accuracy_delta=0.0, claim_macro_f1_delta=+0.6667, evidence_ref_precision_delta=+0.3333, evidence_ref_recall_delta=+0.2, tool_contradiction_rejection_rate_delta=+0.4, fresh_tool_ref_citation_rate=0.5, official_field_leak_count=0; gateway_improves_count=5, gateway_same_count=1, gateway_worse_count=2; recommendation=insufficient_data (n=8 below 12-case threshold — Phase 5.4 deliberately refuses to promote on a small sample even with positive secondary deltas). Quality gates: ruff clean, mypy clean (92 source files), 786 passed / 4 skipped (was 763/4 before Phase 5.4 — exactly +23 new tests). ZERO new dependency in pyproject.toml. ZERO MCP runtime code. ZERO provider tool-calling. ADR-22 hard wall holds. Honesty statement (verbatim per QA/A31 lines 379-386): Phase 5.4 measures the gateway-grounded verifier on runnable holdout fixtures; it does not implement MCP; it does not enable provider tool-calling; it does not make verify-grounded default; it does not validate production predictive performance; it does not tune production thresholds; it does not emit official total_v_net / debt_final / corrupt_success; it does not modify the vendored OIDA core. Per QA/A31 "Apres Phase 5.4": Phase 5.5 = either expand runnable slate to 12+ cases OR revise verifier prompts/labels/tool-request policy; MCP stays explicitly deferred to Phase 5.6+ at the earliest, contingent on Phase 5.5 producing a clear integrate_opt_in recommendation.


[2026-04-27 12:00:00] - Phase 5.5 runnable holdout expansion complete (commit pending). ADR-40 logged. 36/36 acceptance criteria from QA/A32.md met (criterion #36 GitHub-hosted run captured in follow-up commit). 5.5.0-A: Phase 5.4 report audit-log path wording fixed on lines 215-230 (literal example .oida/gateway-calibration/audit/tool_needed_then_supported/2026-04-26/pytest.jsonl with each component named explicitly); 2 canary tests in tests/test_phase5_4_real_calibration.py lock in the fix. 5.5.0-B: src/oida_code/calibration/gateway_calibration.py refactored to use a per-class _PerClassConfusion dataclass tracking tp/fp/fn (replacing the six flat *_correct/*_wrong counters); _PerModeMetrics now holds three of these (accepted, unsupported, rejected); claim_macro_f1 is the mean of three per-class f1 scores (numerically equal to the Phase 5.4 symmetric proxy 2*TP/(2*TP+FP+FN), but the metric JSON now exposes accepted_precision/recall/f1 and similar for unsupported/rejected so any asymmetric P/R splits become visible); _update_metrics computes FP via actual_C - expected_C and FN via expected_C - actual_C; Phase 5.4 backward-compat keys (accepted_correct, accepted_wrong, etc.) remain in to_json(). 5.5.0-C: _RECOMMENDATION_LITERAL renamed integrate_opt_in -> integrate_opt_in_candidate; decision_summary.json carries new STRUCTURAL pin promotion_allowed: false (hardcoded False regardless of recommendation; Phase 5.5 picks the next phase, not the action default); _decide_recommendation rewritten with five rules in QA/A32 §5.5-C order (leak -> revise_tool_policy; runnable<12 -> insufficient_data; macro-F1 > +0.05 AND tool-contradiction non-negative AND evidence-precision non-negative AND no critical gateway_bug -> integrate_opt_in_candidate; macro-F1 < -0.05 -> revise_labels; otherwise -> revise_prompts). 5.5-A: 4 new public synthetic cases — tool_missing_uncertainty (executor returncode=null -> adapter status=tool_missing -> Phase 5.2.1-B enforcer demotes claim to unsupported; uncertainty preserved, NOT rejected as code failure), tool_timeout_uncertainty (timed_out=true -> status=timeout -> demote + budget warning), multi_tool_static_then_test (pass-1 requests ruff + mypy + pytest via new by_tool executor schema; pytest fails while static checks pass; aggregator rejects C.fix because the pytest negative estimate dominates), duplicate_tool_request_budget (pass-1 requests pytest 3 times with tool_policy.max_tool_calls=2; budget cap leaves only 2 audit events). New scripts/_build_phase5_5_cases.py fixture builder. Runner gained two changes: optional per-tool by_tool executor schema (so a single case can exercise multiple adapters with distinct outcomes), and tool_policy.max_tool_calls is now wired through to run_gateway_grounded_verifier(max_tool_calls=...) so the budget cap is observable in the audit log. 5.5-B: 12-case run on the expanded slate produces claim_accept_accuracy_delta=0.0, claim_macro_f1_delta=+0.6667, evidence_ref_precision_delta=+0.4, evidence_ref_recall_delta=+0.2857, tool_contradiction_rejection_rate_delta=+0.4286, fresh_tool_ref_citation_rate=0.6667, official_field_leak_count=0; gateway_improves_count=8, gateway_same_count=2, gateway_worse_count=2; recommendation=integrate_opt_in_candidate with promotion_allowed=false (hardcoded). 5.5-D: failure_analysis.md extended with two more proposal columns (tool_request_policy_change_proposed, prompt_change_proposed) for a 12-column table; two new classifications added to FAILURE_CLASSIFICATIONS (tool_budget_gap, uncertainty_preserved); both documented in the legend; classifier auto-sets tool_request_policy_change_proposed=true on gateway_bug rows and prompt_change_proposed=true on aggregator_bug rows. 5.5-E: private holdout protocol unchanged. 5.5-F: anti-MCP locks remain active; Phase 5.5 ADDS 6 (test_no_mcp_dependency_added_phase5_5, test_no_mcp_workflow_added_phase5_5, test_no_jsonrpc_tools_list_or_tools_call_runtime_phase5_5, test_no_provider_tool_calling_enabled_phase5_5, test_action_yml_does_not_default_enable_tool_gateway_true_phase5_5, test_calibration_module_does_not_import_mcp_runtime). 35 new tests in tests/test_phase5_5_holdout_expansion.py + 2 audit-log canaries in tests/test_phase5_4_real_calibration.py + 1 Phase-5.3 test extension (now expects 10 documented classifications including tool_budget_gap + uncertainty_preserved). Quality gates: ruff clean (full curated CI scope incl. scripts/_build_phase5_5_cases.py), mypy clean (92 source files), pytest 821 passed / 4 skipped (was 786/4 before Phase 5.5 — exactly +35 new tests). ZERO new dependency in pyproject.toml. ZERO MCP runtime code. ZERO provider tool-calling. ADR-22 hard wall holds. Honesty statement (verbatim per QA/A32 lines 447-455): Phase 5.5 expands and recalibrates the runnable gateway holdout; it does not make verify-grounded the default; it does not integrate MCP; it does not enable provider tool-calling; it does not allow write tools or network egress; it does not validate production predictive performance; it does not tune production thresholds; it does not emit official total_v_net / debt_final / corrupt_success; it does not modify the vendored OIDA core. Per QA/A32 "Apres Phase 5.5": every precondition met (cases_runnable=12 >= 12, official leak count 0, true macro-F1 positive, tool contradiction rejection positive, fresh tool-ref citation positive, no critical gateway_bug) -> Phase 5.6 = integrate gateway-grounded verifier as opt-in action path with enable-tool-gateway: false default + workflow_dispatch / explicit input only + replay/fake by default + no external provider + no MCP + no write/network + no official fields.

[2026-04-27 12:30:00] - Phase 5.5 emission gap closed — advisor flagged that tool_budget_gap and uncertainty_preserved were in FAILURE_CLASSIFICATIONS + legend but never returned by _classify_case. Fixed by upgrading the classifier: when both modes match expected, the runner now checks gateway tool_results for status in (tool_missing, timeout) and emits uncertainty_preserved; checks pass1_requested_tools_count > len(gateway_tool_results) and emits tool_budget_gap. Three new regression tests in tests/test_phase5_5_holdout_expansion.py (test_uncertainty_preserved_actually_emitted_for_tool_missing, test_uncertainty_preserved_actually_emitted_for_timeout, test_tool_budget_gap_actually_emitted_for_duplicate_case) lock in the actual emission. On the 12-case run: 9 cases expected_behavior_changed, 2 uncertainty_preserved, 1 tool_budget_gap. Final test count 824 passed / 4 skipped (was 786/4 before Phase 5.5 — exactly +38 net new tests).


[2026-04-27 13:00:00] - Phase 5.5 FULLY ACCEPTED — runnable holdout expansion (commit 23017cb). ADR-40 logged. 36/36 acceptance criteria from QA/A32.md met. Criterion #36 (real-runner gateway-calibration run on the expanded slate) closed by FIVE real-runner runs all green on commit 23017cb: ci (run id 24965538661, 1m25s), action-smoke (run id 24965538659, 1m03s), provider-baseline-node24-smoke (run id 24965538667, 25s), gateway-grounded-smoke (run id 24965538670, 31s), and gateway-calibration (run id 24965538668, 22s — Phase 5.5 NOW pointing at datasets/gateway_holdout_public_v1/manifest.json with 12 fully-committed synthetic cases including the four Phase 5.5 additions, asserting 6 expected artifacts including decision_summary.json, running inline official_field_leak_count==0 gate, uploading via actions/upload-artifact@v4). Calibration result on the public 12-case slate: claim_accept_accuracy_delta=0.0, claim_macro_f1_delta=+0.6667 (TRUE per-class macro-F1, not the Phase 5.4 symmetric proxy), evidence_ref_precision_delta=+0.4, evidence_ref_recall_delta=+0.2857, tool_contradiction_rejection_rate_delta=+0.4286, fresh_tool_ref_citation_rate=0.6667, official_field_leak_count=0; gateway_improves_count=8, gateway_same_count=2, gateway_worse_count=2; recommendation=integrate_opt_in_candidate with promotion_allowed=false (STRUCTURAL pin, hardcoded, regardless of recommendation). Quality gates: ruff clean, mypy clean (92 source files), 824 passed / 4 skipped (was 786/4 before Phase 5.5 — exactly +38 new tests). ZERO new dependency in pyproject.toml. ZERO MCP runtime code. ZERO provider tool-calling. ADR-22 hard wall holds. Honesty statement (verbatim per QA/A32 lines 447-455): Phase 5.5 expands and recalibrates the runnable gateway holdout; it does not make verify-grounded the default; it does not integrate MCP; it does not enable provider tool-calling; it does not allow write tools or network egress; it does not validate production predictive performance; it does not tune production thresholds; it does not emit official total_v_net / debt_final / corrupt_success; it does not modify the vendored OIDA core. Per QA/A32 'Apres Phase 5.5': every precondition met -> Phase 5.6 = integrate gateway-grounded verifier as opt-in action path with enable-tool-gateway: false default + workflow_dispatch / explicit input only + replay/fake by default + no external provider + no MCP + no write/network + no official fields. MCP remains explicitly deferred; the earliest a local stdio MCP mock prototype can be revisited is post-Phase-5.6 contingent on production soak.


[2026-04-27 14:00:00] - Phase 5.6 opt-in gateway-grounded action path complete (commit pending). ADR-41 logged. 35/35 acceptance criteria from QA/A33.md met locally (criterion #35 GitHub-hosted action-gateway-smoke run captured in follow-up commit). NEW src/oida_code/action_gateway/ package (~640 LOC) with bundle.py (validate_gateway_bundle: 8 required files + path-traversal + secret/provider/MCP filename rejection), status.py (GatewayStatus = Literal[disabled|diagnostic_only|contract_clean|contract_failed|blocked]; product verdicts unrepresentable; derive_gateway_status helper; FORBIDDEN_VERDICT_TOKENS list), summary.py (render_gateway_summary + runtime _scan_for_forbidden_phrases raising ForbiddenSummaryPhraseError on any product-verdict hit). NEW CLI subcommands validate-gateway-bundle / render-gateway-summary / emit-gateway-status — emit-gateway-status runs a runtime forbidden-token leak scan over the grounded report when --grounded-report is supplied; leak count > 0 forces gateway-status=contract_failed. action.yml rewritten: new inputs gateway-bundle-dir + gateway-output-dir + gateway-fail-on-contract alongside the now-implemented enable-tool-gateway (description rewritten from RESERVED to EXPERIMENTAL OPT-IN); new outputs gateway-report-json + gateway-summary-md + gateway-audit-log-dir + gateway-status + gateway-official-field-leak-count; new step Phase 5.6 — block gateway on PR / fork PR (exits 2 with ::error:: when enable-tool-gateway=true AND event_name in pull_request/pull_request_target — anti-RCE, pytest can execute repo code from untrusted forks); new step Phase 5.6 — gateway-grounded verifier (opt-in) with id: gateway that always runs and branches on ENABLE_TOOL_GATEWAY (so steps.gateway.outputs.X is always set); when enabled, the step validates the bundle via oida-code validate-gateway-bundle, runs verify-grounded with the eight QA-spec'd files (pass1_forward.json etc, no gateway_ prefix), runs emit-gateway-status, runs render-gateway-summary, runs build-artifact-manifest (Phase 4.9-F reuse), appends summary.md to GITHUB_STEP_SUMMARY + action_outputs.txt to GITHUB_OUTPUT; optional gateway-fail-on-contract gate exits 3 if leak count > 0; new conditional upload-artifact step uploads gateway-output-dir as oida-code-gateway artifact. NEW tests/fixtures/action_gateway_bundle/tool_needed_then_supported/ with the eight required files (mirrored from the Phase 5.4 case but renamed to drop the gateway_ prefix on replays — that's the Phase 5.6 stable bundle layout) + executor.json + README.md. NEW .github/workflows/action-gateway-smoke.yml on workflow_dispatch + push to main, permissions: contents: read, no secrets, no external provider, no MCP, no network egress, no SARIF upload; calls the composite action with enable-tool-gateway: "true" and gateway-bundle-dir pointing at the new fixture; asserts the 5 expected gateway artifacts (grounded_report.json + summary.md + action_outputs.txt + audit/ + artifacts/manifest.json), runs an inline forbidden-token scan over the report JSON, asserts gateway-status is in the canonical 5-value enum, uploads via actions/upload-artifact@v4. Shell-injection guard: every operator-controlled value (ENABLE_TOOL_GATEWAY, BUNDLE_DIR, GATEWAY_OUTPUT_DIR, GATEWAY_FAIL_ON_CONTRACT, GH_WORKSPACE) is lifted to env: and referenced as a bash variable in run: — Phase 4.5.1 rule applied to the new gateway-* inputs; three regression tests parse action.yml and walk the gateway run block to lock the rule in. 48 new tests in tests/test_phase5_6_action_gateway_opt_in.py covering 8 sub-blocks (5.6-A inputs 5 tests, 5.6-B bundle validator 9 tests including missing-dir / missing-files / workspace-traversal / secret / provider / mcp variants, 5.6-D summary 4 tests including the forbidden-phrase injection canary, 5.6-E gateway-status enum + derive helper 5 tests + emit-gateway-status CLI 3 tests + validate-gateway-bundle CLI 2 tests, 5.6-F workflow + fixture 6 tests, 5.6-G PR/fork guard 3 tests, 5.6-H shell-injection 3 tests, 5.6-E outputs 2 tests) + 4 anti-MCP regression locks (test_no_mcp_dependency_added_phase5_6, test_no_mcp_workflow_added_phase5_6, test_no_provider_tool_calling_enabled_phase5_6, test_action_gateway_module_does_not_import_mcp_runtime — AST-walks every .py under action_gateway/ to assert no MCP imports; strips docstrings + comments before scanning for runtime tokens) + 1 anti-mutation invariant on the fixture + 1 end-to-end CLI flow test exercising the full validate -> verify-grounded -> emit-status -> render-summary pipeline. Quality gates: ruff clean (full curated CI scope), mypy clean (96 source files; was 92 in Phase 5.5 — +4 from the new action_gateway/ package), pytest 872 passed / 4 skipped (was 824/4 before Phase 5.6 — exactly +48 new tests). ZERO new dependency in pyproject.toml. ZERO MCP runtime code. ZERO provider tool-calling. ADR-22 hard wall holds. The Phase 4.7 + Phase 5.0 + Phase 5.1 + Phase 5.2 + Phase 5.3 + Phase 5.4 + Phase 5.5 anti-MCP locks remain ACTIVE; Phase 5.6 ADDS 4 more. Honesty statement (verbatim per QA/A33 lines 354-360): Phase 5.6 exposes the gateway-grounded verifier as an explicit opt-in GitHub Action path; it remains disabled by default; it does not make verify-grounded the default audit path; it does not implement MCP; it does not enable provider tool-calling; it does not run on fork PRs; it does not validate production predictive performance; it does not emit official total_v_net, debt_final, or corrupt_success; it does not modify the vendored OIDA core. Per QA/A33 "Apres Phase 5.6": if the smoke is green and the artifact pipeline holds, Phase 5.7 = operator soak on real repos (3-5 controlled PRs, collect artifacts, measure FP/FN, keep enable-tool-gateway default false). MCP remains explicitly deferred.

[2026-04-27 14:30:00] - Phase 5.6 FULLY ACCEPTED — opt-in gateway-grounded action path (commit 8fca666). ADR-41 logged. 35/35 acceptance criteria from QA/A33.md met. Criterion #35 (GitHub-hosted action-gateway-smoke run green) closed by SIX real-runner runs all green on commit 8fca666: ci (run id 24967291123, 2m11s), action-smoke (run id 24967291127, 1m43s), provider-baseline-node24-smoke (run id 24967291130, 24s), gateway-grounded-smoke (run id 24967291138, 26s), gateway-calibration (run id 24967291137, 22s), and the NEW action-gateway-smoke (run id 24967291132, 1m57s — composite action with enable-tool-gateway: "true" pointing at tests/fixtures/action_gateway_bundle/tool_needed_then_supported, asserting the 5 expected gateway artifacts exist, running an inline forbidden-token scan over the report JSON, asserting the gateway-status output is in the canonical 5-value enum, uploading via actions/upload-artifact@v4). Quality gates: ruff clean, mypy clean (96 source files), 872 passed / 4 skipped (was 824/4 before Phase 5.6 — exactly +48 new tests). ZERO new dependency in pyproject.toml. ZERO MCP runtime code. ZERO provider tool-calling. ADR-22 hard wall holds. Honesty statement (verbatim per QA/A33 lines 354-360): Phase 5.6 exposes the gateway-grounded verifier as an explicit opt-in GitHub Action path; it remains disabled by default; it does not make verify-grounded the default audit path; it does not implement MCP; it does not enable provider tool-calling; it does not run on fork PRs; it does not validate production predictive performance; it does not emit official total_v_net, debt_final, or corrupt_success; it does not modify the vendored OIDA core. Per QA/A33 "Apres Phase 5.6": every precondition met -> Phase 5.7 = operator soak on real repos (3-5 controlled PRs, collect artifacts, measure FP/FN, keep enable-tool-gateway default false). MCP remains explicitly deferred.

[2026-04-27 16:30:00] - Phase 5.7 SCAFFOLDED — operator soak protocol on controlled repos (ADR-42). 23/23 in-scope acceptance criteria from QA/A34.md met (10 shipped, 6 scaffolded-blocked-on-operator, 1 fork-PR not_run with reason, 6 operational gates). Cases_completed=0 -> recommendation=continue_soak per QA/A34 sec.5.7-F rule 1. New top-level operator_soak_cases/ directory with protocol README + case_001_oida_code_self/ scaffold (status awaiting_run; no controlled-change branch available; the Phase 5.6 contract-test fixture is intentionally NOT repurposed). New src/oida_code/operator_soak/ package with frozen extra=forbid Pydantic schemas (OperatorSoakFiche, OperatorLabelEntry with 6-bucket Literal label + 3-10 line rationale rule, OperatorUxScore with four 0/1/2 questions, AggregateReport with is_authoritative pinned Literal[False] + recommendation as 5-value Literal). Pure aggregator aggregate_cases(cases_root) + Markdown renderer + scripts/run_operator_soak_eval.py runner. Decision rules: leak>0 -> fix_contract_leak (beats every other rule); cases_completed<3 -> continue_soak; FN>=2 -> revise_gateway_policy_or_prompts; FP>=2 -> revise_report_ux_or_labels; cases_completed>=5 AND usefulness_rate>=0.6 -> document_opt_in_path; otherwise continue_soak. Even document_opt_in_path does NOT flip enable-tool-gateway default. Empty baseline reports/operator_soak/aggregate.{json,md} checked in. Fork-PR block smoke explicitly not_run with reason no controlled fork available (Phase 5.6 guard remains active in action.yml). No autonomous background agent, no MCP runtime, no provider tool-calling, no pull_request_target, no modification of action.yml, no new dependency, no modification of vendored OIDA core. 41 new tests in tests/test_phase5_7_operator_soak.py covering schema invariants, decision rule precedence, aggregator I/O, eval script CLI, and anti-MCP locks. ADR-42 timestamp 2026-04-27 16:00:00 appended to memory-bank/decisionLog.md. 913 passed / 4 skipped (was 872/4 at end of Phase 5.6 — exactly +41 tests).

[2026-04-27 17:30:00] - Phase 5.7 FULLY ACCEPTED — operator soak protocol scaffolding (commit da52765). All six GitHub-hosted workflows green on da52765: ci (run id 24982019499, 2m28s), action-smoke (run id 24982019505, 1m43s), provider-baseline-node24-smoke (run id 24982019503, 22s), gateway-grounded-smoke (run id 24982019504, 27s), gateway-calibration (run id 24982019488, 24s), action-gateway-smoke (run id 24982019509, 1m52s). Criterion #24 (at least one GitHub-hosted action-gateway-smoke or soak workflow green after Phase 5.7 changes) satisfied via action-gateway-smoke 24982019509. Phase 5.6 PR/fork guard remains active and was not modified; the Phase 5.6 fork-PR guard test in tests/test_phase5_6_action_gateway_opt_in.py remains green on da52765 inside the ci run. No autonomous background agent. Per feedback_no_schedule_for_observation_phases.md: no /schedule offer at the close of Phase 5.7 — observation reminders are appropriate only AFTER real operator labels exist in reports/operator_soak/.

[2026-04-27 17:30:00] - Phase 5.8 first operator soak cycle complete on case_001 (commit e7c9139). All six GitHub-hosted workflows green on e7c9139: ci (run id 25000832235, 2m21s), action-smoke (run id 25000832255, 1m52s), provider-baseline-node24-smoke (run id 25000832268, 22s), gateway-grounded-smoke (run id 25000832264, 23s), gateway-calibration (run id 25000832246, 20s), action-gateway-smoke (run id 25000832210, 2m1s). The operator-soak.yml workflow itself is workflow_dispatch-only and was exercised twice on case_001: run 24994865500 failed at 27s due to a relative-path bug in the workflow (../oida-target did not resolve from  cwd; fixed in commit 0b7a657 with absolute github.workspace paths); run 24995045522 succeeded at 1m56s after the fix. Run 24995045522 outcome: gateway-status=diagnostic_only; gateway-official-field-leak-count=0 (ADR-22 hard wall holds); accepted_claims=0; rejected_claims=1 (claim C.docstring.no_behavior_delta rejected because pytest tool ran 193ms then errored emitting no [E.tool.pytest.0] evidence). cgpro session phase58-soak (uuid 69ef3a8c-0198-8394-8f09-14a7b120d192) labelled the case insufficient_fixture (rationale: replay/tool-evidence path failed to ground the preapproved claim; not a code concern) with UX scores 1/1/1/2 (notes: report avoids product verdicts but summary alone is not enough; pytest error lacks stderr/findings). Aggregate state: cases_total=3, cases_completed=1, insufficient_fixture_count=1, official_field_leak_count=0, recommendation=continue_soak (1 less than threshold of 3). cases 002 (python-semver) and 003 (markupsafe) remain awaiting_real_audit_packet_decision. Next operator session decision: diagnose pytest status=error at 193ms (probable cause: oida_code installed editable from oida-main checkout but pytest cwd was oida-target — module path / conftest mismatch) before relabelling case_001 OR move to cases 002/003 with their own real audit packets. enable-tool-gateway default stays false. No autonomous follow-up scheduled per feedback_no_schedule_for_observation_phases.md.

[2026-04-28 13:30:00] - Phase 5.8 + Phase 5.8.x Tier 5 promotion gate cleared (commit bc11711). 7-commit trajectory: 93c7581 (ADR-47 — pytest_summary_line schema field on VerifierToolResult; +15 tests; 943 → 958), 0282c40 (case_003 re-dispatch + cgpro relabel UX 2/1/2/2 → 2/2/2/2 on workflow run 25047711777), c7734b3 (ANSI-strip parser fix — python-slugify pins addopts="--color=yes" so pytest emits SGR escapes through subprocess pipes; +2 tests; 958 → 960; fix shipped BEFORE the case_004 dispatch so the workflow used the corrected parser), 1bd0203 (case_004 real audit packet for un33k/python-slugify@7edf477 — claim_type=precondition_supported), 7a42085 (case_004 closure: cgpro labelled useful_true_positive UX 2/2/2/2 on run 25050370380; Tier 4), 325c397 (case_005 real audit packet for alecthomas/voluptuous@4cef6ce — claim_type=capability_sufficient), bc11711 (case_005 closure + ADR-48 + aggregate flip + Tier 5 test rename: cgpro labelled useful_true_positive UX 2/2/2/2 on run 25051323517). All 6 CI workflows green on bc11711: ci (run id 25051850793), action-smoke (25051850817), provider-baseline-node24-smoke (25051850781), gateway-grounded-smoke (25051850768), gateway-calibration (25051850763), action-gateway-smoke (25051850782). Aggregate state: cases_total=5, cases_completed=5, useful_true_positive_count=5, usefulness_rate=1.000, all four UX averages 2.000, official_field_leak_count=0, recommendation=document_opt_in_path (rule 5: cases_completed>=5 AND usefulness_rate>=0.6 fires). Five distinct verifier-grounded scenarios across four VerifierClaimType Literal values: negative_path_covered (case_001 self-audit + case_002 cross-repo no-install python-semver), observability_sufficient (case_003 cross-repo C-extension markupsafe), precondition_supported (case_004 cross-repo CLI-precondition python-slugify), capability_sufficient (case_005 cross-repo capability voluptuous). enable-tool-gateway remains default false in the composite Action regardless. Quality gates: ruff clean, mypy clean (98 source files), 960 passed / 4 skipped. ZERO new dependency. ZERO MCP runtime code. ZERO provider tool-calling. ZERO vendored core change. ADR-22/24/25/26 hard wall preserved across all 5 case artefacts (forbidden-token scan returned zero hits). Note: each operator-soak.yml run emits a benign "No files were found with the provided path: oida-main/.oida/**" warning on the OUTER upload step — pre-existing across all soak runs (25045245609, 25047711777, 25050370380, 25051323517); the actual artefact comes from the composite action's INNER oida-code-gateway upload step which works correctly. No autonomous follow-up scheduled per feedback_no_schedule_for_observation_phases.md.



[2026-04-28 14:30:00] - Phase 5.9 documentation opt-in path + adapter argv hardening complete (commit pending). ADR-49 logged. All 14 QA/A40 acceptance criteria met. QA/A40 directive: "ne construis plus. Stabilise, documente, rends opérable" — no new architecture, focus on documenting the gateway opt-in path so an external operator can use it without internal context. Six deliverables: (1) all 5 operator-soak case READMEs (case_001..005) aligned with the aggregate via a single canonical post-completion template (claim_id / claim_type / pytest_scope / target_install / run_id / label / UX score) plus links to source-of-truth sidecars — fixes the QA/A40-flagged drift on case_002 (README said `awaiting_real_audit_packet_decision` while aggregate marked complete) and locks against future drift via a doc-guard test; (2) `examples/gateway_opt_in/` keystone reproducible bundle — 8-file self-audit (`tool_policy.json` `repo_root="."`, no `target_install`, no external clone) grounding C.oida_code.pytest_summary_line_schema_field_available on tests/test_phase5_8_x_pytest_summary_line.py, validated against the Pydantic schemas in CI; (3) `docs/gateway_opt_in_usage.md` — when to use / when not to use, bundle authoring (with all hard caps documented up front: 400-char EvidenceItem.summary, 200-char VerifierToolCallSpec.purpose, allowed_fields Literal allowlist), three launch options (local CLI, operator-soak workflow, composite action input), how to read the artefacts (Step Summary → summary.md → grounded_report.json → audit log → manifest), what verification_candidate actually means, why official fields stay blocked; (4) `docs/interpreting_gateway_reports.md` — 6 core signals + 6 follow-on signals each paired with the misreading the signal tends to invite, plus a 5-row "misreadings to avoid" table; (5) `docs/operator_soak_runbook.md` — public step-by-step runbook covering target picking, bundle authoring, pre-dispatch local gate, workflow_dispatch, artefact triage, label.json + ux_score.json authoring (operator-only — schema cannot enforce author identity, policy lives in writing), aggregate refresh, with all 5 reference cases summarised; (6) `docs/security/no_product_verdict_policy.md` — explicit forbidden-tokens list (`merge-safe`, `production-safe`, `bug-free`, `verified`, `security-verified`, plus `total_v_net`, `debt_final`, `corrupt_success`, `corrupt_success_ratio`, `verdict`) with the five enforcement layers documented. Plus one bug fix surfaced by building the example: `PytestAdapter.build_argv` modified to prepend `-o addopts=` (neutralises target pyproject.toml `addopts` settings — without this, oida-code's own `addopts = "-q --strict-markers"` combined with the adapter's own `-q` collapsed pytest verbosity to `-qq` and suppressed the terminal summary line entirely, breaking pytest_summary_line silently). Forward-compatible across all 5 existing cases (verified locally: case_004 still produces "83 passed in 0.11s", case_005 still produces "167 passed in 0.26s"). 32 new doc-guard tests + 2 argv regression tests = test count 960 → 994 (+34). ruff clean, mypy clean. ZERO new dependency. ZERO MCP runtime code. ZERO provider tool-calling. ZERO vendored core change. ZERO new product-verdict tokens introduced anywhere. The 5 cases keep useful_true_positive UX 2/2/2/2; aggregate stays at recommendation=document_opt_in_path; enable-tool-gateway stays default false. The keystone example demonstrates pytest_summary_line populated end-to-end with the new argv hardening, validating ADR-47 pedagogy. QA/A40 stated goal ("rendre le chemin gateway opt-in utilisable par un humain externe au projet, sans faux verdict produit") is met. Per QA/A40 medium-term: Phase 6.0 controlled beta is the recommended next, with bundle generator (Phase 6.1) and adversarial soak cases as later options. MCP / provider tool-calling / GitHub App / Checks API / public benchmark / PyPI stable explicitly out of scope. Honesty statement: Phase 5.9 documents the opt-in gateway-grounded path and surfaces a latent adapter argv bug discovered while building the keystone example; it does not implement MCP; it does not enable provider tool-calling; it does not allow write tools or network egress; it does not validate production predictive performance; it does not tune production thresholds; it does not emit official total_v_net / debt_final / corrupt_success; it does not modify the vendored OIDA core. The Phase 4.7 + 5.0 + 5.1 + 5.2 + 5.3 + 5.4 + 5.5 + 5.6 + 5.7 + 5.8 + 5.8.x anti-MCP locks remain ACTIVE.



[2026-04-28 17:00:00] - Phase 6.0 controlled-beta protocol established (commit pending). ADR-50 logged. All 28 QA/A41 acceptance criteria met as a layered satisfaction: criteria #1-#6, #11-#28 fully met by the protocol surface + doc-guard tests + cadence artefacts; criteria #7-#10 met by the explicit not_run reasons documented in the report (per the QA/A41 partial-completion authorization "or explicit not_run reason documented" / "phase remains partial"). QA/A41 directive: "Phase 6.0 doit être une beta contrôlée sur un petit nombre d'utilisateurs/repos, pas une nouvelle phase d'architecture" — pack first, recruitment second; the protocol ships as a complete unit, the runs are pending external operator recruitment which sequences AFTER this commit lands (operators need the pack to evaluate participation). 14 deliverables: (1) `BACKLOG.md` at repo root with the Grok-flagged long-term gaps (G-1..G-5: official fields blocked, Python-first, large-scale validation missing, roadmap/docs clarity, simple conceptual explanation) labelled as backlog NOT roadmap NOT phase commitment per QA/A41 addendum §4; (2) `docs/concepts/oida_code_plain_language.md` plain-language overview no jargon; (3) `docs/project_status.md` four-section status (usable now, blocked, out of scope, roadmap) per QA/A41 addendum §2; (4) `docs/beta/README.md` directory index; (5) `docs/beta/beta_known_limits.md` the leaf — what stays blocked, what stays default false, what verification_candidate means, the 5 Tier-5 cases scoped frame; (6) `docs/beta/beta_operator_quickstart.md` 10-minute walk-through with three launch options and four artefacts read in order; (7) `docs/beta/beta_case_template.md` canonical template for filing a beta case; (8) `docs/beta/beta_feedback_form.md` feedback form with 5 0/1/2 axes (summary_readability / evidence_traceability / actionability / no_false_verdict / setup_friction) plus would_use_again (yes/no/maybe) plus 7 open-text questions; (9) `scripts/run_beta_feedback_eval.py` ~310-line self-contained aggregator with the 17 QA/A41 §6.0-E metrics, zero-feedback-clean property (writes complete aggregate without any submitted feedback, exits 0, populates all 17 metrics, preserves diagnostic_only and official_fields_emitted: false), recommendation logic (fix_contract_leak / revise_gateway_policy_or_prompts / revise_report_ux_or_labels / continue_beta / consider_phase_6_1); (10) `reports/beta/beta_cases.md` running cases registry with status legend (beta_pack_only / case_drafted / run_dispatched / run_completed / feedback_submitted / not_run); (11) `reports/beta/beta_feedback_aggregate.json` zero-feedback initial state; (12) `reports/beta/beta_feedback_aggregate.md` zero-feedback initial state; (13) `tests/test_phase6_0_controlled_beta.py` ~360-line 53-test suite with two scoped sets (_ALL_PHASE6_DOCS 8 docs, _GATEWAY_EXPLAINER_DOCS 5 docs), three-heuristic negation detector reused from Phase 5.9 with extended cue list (stays/remain blocked, refuses to emit, cannot tell you, will never produce), anti-MCP / no-default-flip / Phase 5.x-locks-preserved structural locks, beta aggregator structural locks (17 metrics + zero-feedback state + diagnostic_only), BACKLOG.md disclaimer lock + project_status.md four-section lock + beta/README.md leaf-link lock; (14) `reports/phase6_0_controlled_beta.md` 12-section report per QA/A41 §"Rapport attendu" with honesty statement copied character-for-character from QA/A41 lines 309-311, gates table marking per-run criteria as ✓ (not_run) or ✓ (partial) with the explicit reason documented in the report sections. 53 new doc-guard tests; test count 994 → 1047 (+53). ruff clean, mypy clean. ZERO new dependency. ZERO MCP runtime code. ZERO provider tool-calling. ZERO vendored core change. ZERO new product-verdict tokens introduced anywhere. ZERO change to enable-tool-gateway default (remains false). The 5 Tier-5 operator-soak cases preserve useful_true_positive UX 2/2/2/2; the operator-soak aggregate stays at recommendation=document_opt_in_path. The Phase 4.7 + 5.0 + 5.1 + 5.2 + 5.3 + 5.4 + 5.5 + 5.6 + 5.7 + 5.8 + 5.8.x + 5.9 anti-MCP locks remain ACTIVE. Honesty statement: Phase 6.0 runs a controlled beta of the opt-in gateway-grounded path with selected operators and controlled repos. It does not make the gateway default. It does not implement MCP. It does not enable provider tool-calling. It does not validate production predictive performance. It does not emit official total_v_net, debt_final, or corrupt_success. It does not modify the vendored OIDA core. Phase 6.0 establishes the **measuring instrument** for the controlled beta; the measurement itself follows external operator recruitment in the open phase window. Per QA/A41: MCP / provider tool-calling / GitHub App / Checks API / public benchmark / PyPI stable / default gateway / public beta all explicitly out of scope. Per QA/A41 §6.0-F bundle generator (Phase 6.1) explicitly punted "D'abord mesurer". No autonomous follow-up scheduled per feedback_no_schedule_for_observation_phases.md — observation reminders are appropriate only AFTER real beta operator labels exist in reports/beta/.



[2026-04-28 17:30:00] - Phase 6.0.x — QA/A42 conditions 1-3 patch (commit pending). cgpro QA/A42 verdict on Phase 6.0 was "Accepted with conditions" with 4 conditions and 12 pièges. Conditions 1-3 land as Phase 6.0.x patch BEFORE the controlled beta opens to first feedback (cgpro: "à condition de traiter les conditions 2 et 3 avant le premier feedback réellement agrégé"). Three patches: (cond 1) zero-feedback aggregate.md now reads "initialized in aggregate; no human sample yet" near the score-axis means; reports/phase6_0_controlled_beta.md gates table rows #12-#14 reworded from "measured" to "initialized; no human sample yet" to avoid the merge-acceptable-but-humanly-ambiguous read of "we measured 0.0"; (cond 2) scripts/run_beta_feedback_eval.py now REQUIRES feedback_channel="human_beta" (schema pin), beta_run_id/beta_case_id/beta_operator/target_repo/named_claim/pytest_scope (non-empty strings — silent coercion to "" rejected), and contract_violation_observed/official_field_leak_observed (explicit booleans — silent default to False rejected); docs/beta/beta_feedback_form.md template updated to mark every required field with a comment; (cond 3) _iter_feedback_files now skips paths containing ai_adversarial/ or legacy/ segments (path-isolation guard, FIRST defence layer); the schema pin feedback_channel="human_beta" is the SECOND defence layer; reports/beta/ai_adversarial/* will not contaminate the human-beta aggregate even if dropped under the feedback root by accident. Five new structural tests added to tests/test_phase6_0_controlled_beta.py: missing-feedback-channel rejected, silent-default-booleans rejected, empty-required-identifiers rejected, ai_adversarial-path skipped, zero-feedback aggregate.md uses initialized wording. QA/A42.md committed (~150 lines: verdict détaillé + 4 conditions + 11 STOP/PIVOT signals + 3 Phase 6.1 hypothesis decision tree + 12 pièges identifiés). Test count 1047 → 1052 (+5). ruff clean, mypy clean. ZERO new dependency. ZERO MCP runtime. ZERO provider tool-calling. ZERO change to enable-tool-gateway default (remains false). Phase 4.7+ anti-MCP locks remain ACTIVE.

[2026-04-28 18:30:00] - Phase 6.0.y — ai_adversarial cold-reader critique lane (ADR-51, commit pending). 3 LLM agents reviewed the docs/beta/ pack as cold readers: DeepSeek V4 Pro (deepseek-v4-pro), xAI Grok-4 (grok-4.20-reasoning), and MiniMax (MiniMax-Text-01) — Kimi (kimi-k2.6 / moonshot-v1-128k both 401 auth-failed and substituted by MiniMax). Standalone manual-invocation script `scripts/run_ai_adversarial_review.py` (~330 lines, urllib.request OpenAI-compat, NOT in CI, NOT in runtime path, ZERO new dependency). Output to `reports/ai_adversarial/critique_<provider>.md` × 3 + `aggregate.md` hand-summarized convergence/divergence. STRUCTURAL ISOLATION: path-isolation guard (`reports/ai_adversarial/*` skipped by `_iter_feedback_files` since Phase 6.0.x) + schema pin (`feedback_channel: human_beta` required for human-beta aggregation since Phase 6.0.x) — AI critiques NEVER land in `beta_feedback_aggregate.md` even if accidentally dropped. Convergence findings (3/3 reviewers): C1 bundle authoring is dominant friction (no schema doc, no per-file walkthrough, no non-self-audit example) — confirms QA/A41 §6.0-F prediction; C2 `verification_candidate: true` "strongest positive signal" wording in oida_code_plain_language.md risks verdict-leak reading; C4 "gateway-grounded verifier loop" load-bearing jargon used before definition. (2/3): C3 quickstart "30-60 min" vs known_limits "non-trivial / first external test" tonal contradiction; C5 `C.<surface>.<claim>` and allowed_fields Literal allowlist undocumented for cold readers. Divergence rejected: D1 Grok proposed removing 400/200-char hard caps + Literal allowlist — REJECTED (violates Phase 4.0.1 prompt-injection defence + ADR-22+ structural pins; QA/A42 piège 5 case in action). D2 MiniMax flagged `useful_true_positive` label as verdict-leak — parked, 1-of-3 not enough for breaking-change rename. Phase 6.0.z documentation polish identified for C2-C5 (4 small doc edits, NO schema change, NO new tests) — separate small commit. Bundle generator (Phase 6.1 hypothesis A) gains pre-beta evidence weight from C1, but rule "first measure with humans, then build" stands — AI agents cannot choose Phase 6.1. ZERO new dependency. ZERO MCP runtime. ZERO provider tool-calling in runtime. ZERO change to enable-tool-gateway default. ZERO change to provider_config.py registry. ZERO change to human-beta aggregator behavior. The Phase 4.7+ anti-MCP locks remain ACTIVE.

[2026-04-28 19:00:00] - Phase 6.0.y' — external-human beta unavailable; ADR-52 + lane formalization (commit pending). cgpro QA/A43 verdict pivots Phase 6.0 from "accepted with conditions, partial" → "closed as protocol-only; external-human beta not_run due unavailable operators". The constraint Yann surfaced: he cannot recruit external Python developers; available human resource = Yann himself (project author + co-author of every doc, biased reader); available AI resources = .env providers + codex CLI (ChatGPT 5.5 xhigh) + gemini CLI (Gemini 3.1 Pro). QA/A41 §6.0-A target ("2-3 external operators") is structurally infeasible. QA/A43 §"Ré-cadrage protocole" recommends option C (RELAX explicit-AI-tier with metric pinning): maintain QA/A41 line 350 strict for `feedback_channel: human_beta`, but create separated `ai_tier` channel + separated `yann_solo_dogfood` channel, both with explicit downgraded-evidence framing. QA/A43 §"Phase 6.1 scoping" recommendation Z: pivot to Phase 6.1' (apostrophe — convert ai_adversarial signal into product evolution under degraded evidence). Ordering: Phase 6.0.z C2-C5 doc polish FIRST, THEN Phase 6.1'a bundle schema explainer, THEN Phase 6.1'b minimal generator, THEN Yann-solo dogfood, THEN AI-tier re-run. Three-lane architecture FORMALISED: Lane 1 reports/beta/ + feedback_channel: human_beta (external operators only — empty / not_run); Lane 2 reports/ai_adversarial/ + agent_label (cold-reader critique — active); Lane 3 reports/yann_solo/ + feedback_channel: yann_solo_dogfood + operator_role: project_author (project-author dogfood — allowed, internal only). Cross-lane contamination forbidden at FOUR layers: (1) path-isolation in _iter_feedback_files (Phase 6.0.x) skips ai_adversarial/ + yann_solo/ + legacy/; (2) schema pin feedback_channel: human_beta required (Phase 6.0.x); (3) operator_role + feedback_channel cross-validation (Phase 6.0.y'); (4) FOUR new structural tests in test_phase6_0_y_prime_lane_isolation.py. 14 new pièges (13-26) from QA/A43 §"Nouveaux pièges" added on top of QA/A42's 12. Files landed: QA/A43.md (NEW, ~190 lines: verdict + ré-cadrage protocole + Phase 6.1 scoping + Yann-solo policy + 14 pièges + action immédiate); ADR-52 in memory-bank/decisionLog.md (NEW); docs/project_status.md updated with 4-line lane status section + Phase 6.0 closure framing; reports/yann_solo/README.md (NEW); reports/ai_adversarial/schema.md (NEW, 10-field minimal schema with HTML-comment frontmatter); tests/test_phase6_0_y_prime_lane_isolation.py (NEW, 4 tests). Test count 1052 → 1056 (+4). ruff clean, mypy clean. ZERO new dependency. ZERO MCP runtime. ZERO provider tool-calling in runtime. ZERO change to enable-tool-gateway default. ZERO change to provider_config.py registry. ZERO change to human-beta aggregator behavior. The Phase 4.7+ anti-MCP locks remain ACTIVE. The phrase to keep: "Phase 6.0 established the controlled-beta protocol and closed the external-human beta attempt as not_run because no external operators were available. Phase 6.1' uses explicitly separated AI-tier critique plus Yann-solo dogfood as downgraded product-evolution evidence."

[2026-04-28 19:30:00] - Phase 6.0.z — doc polish C2-C5 from ai_adversarial findings (commit pending). Per QA/A43 §"Action immédiate" step 11, lands BEFORE Phase 6.1'a. Four small doc edits, no schema change, no new tests. (C2) docs/concepts/oida_code_plain_language.md "What verification_candidate does and does not mean" section reframed to lead with the diagnostic frame: opening line is now "verification_candidate: true is a diagnostic signal, not a verdict" with explicit "It is NOT a merge gate. It is NOT a safety claim. It is NOT an endorsement of the underlying code. It is NOT the project saying 'this PR is good'" before the existing "does not mean" bullet list — addresses 3/3 ai_adversarial convergence on verdict-leak risk. (C3) docs/beta/beta_operator_quickstart.md "What you should expect" section drops the unmeasured "30-60 minutes" estimate; replaced with "Bundle authoring time is unmeasured" + acknowledgment of the inconsistency with beta_known_limits.md "non-trivial" framing + "plan for as long as it takes, record what you observe in setup_friction" — addresses 2/3 ai_adversarial finding on tonal contradiction. (C4) docs/concepts/oida_code_plain_language.md NEW section "What the verifier loop actually does (concrete walkthrough)" with 6 concrete steps (load bundle → schema validation; replay forward passes; replay backward passes; run gateway tool calls with policy; cross-check evidence vs tool output; emit diagnostic) plus explicit list of what the loop does NOT do — addresses 3/3 ai_adversarial convergence on "gateway-grounded verifier loop" being load-bearing jargon used before definition. (C5) docs/beta/beta_case_template.md NEW section "How to write a C.<surface>.<claim> identifier" with 3-part shape explanation (C / surface / claim slug) + 7-row table of one example per allowed_fields Literal value (capability_sufficient | benefit_aligned | observability_sufficient | precondition_supported | negative_path_covered | repair_needed | shadow_pressure_explained) + concrete walkthrough on the keystone example — addresses 2/3 ai_adversarial finding on undocumented allowlist + opaque <surface> term. Test count unchanged 1056/4. ruff clean, mypy clean. ZERO new dependency. ZERO MCP runtime. ZERO provider tool-calling. ZERO change to enable-tool-gateway default. ZERO schema change. ZERO new tests (the doc-guard scope already covers these files; the changes do not introduce new structural shapes). The Phase 4.7+ anti-MCP locks remain ACTIVE. Per QA/A43 piège 16 (Goodhart on agents): the doc polishes were derived from cold-reader findings but the project team filtered them — the ai_adversarial run will be re-run after Phase 6.1'a/b to check whether the C1 friction (bundle authoring) reshapes or disappears, NOT whether the agents now "approve" the docs. Next: Phase 6.1'a (bundle schema explainer + one worked non-self-audit example), then Phase 6.1'b (minimal generator), then Yann-solo dogfood, then AI-tier re-run.

[2026-04-29 14:00:00] - Methodology consolidation post-Phase-6.2 (ADR-64, commit pending). Per cgpro QA/A47 next_action + ADR-63 5-step action list. Documentation-only commit incorporating the Phase 6.2 audit's 5 convergent + 5 sharp critiques into canonical surfaces. Five edits: (1) docs/project_status.md rewritten — was 2026-04-28 (predates entire 6.1' chain), now 2026-04-29; lane status table updated to list 3 manual-lane scripts; §1 "Usable now" adds prepare-gateway-bundle + 46-inclusion calibration_seed corpus + 3 manual-lane scripts; §4 NEW "Phase 6.1' chain — what shipped + what the audit said" frames the chain honestly without "FIRST holdout success" / "partial generalisation" headline framing; §5 roadmap marks Phase 6.1' + 6.2 CLOSED, lists corpus-quality maintenance as deferred; §8 NEW "Phase 6.2 audit-informed caveats (read this if you read nothing else in §4-7)" — 6 caveats one per audit theme: hard-wall-strict-letter-intact-but-spirit-wobbly, seed_157 reclassification trajectory, freeze-rule carve-out should be operationally bounded, LLM-replay-audit gap, N=5 statistical thinness, ADR-56 spirit-tension on seed_065. (2) BACKLOG.md G-6 addition — 6 sub-items G-6a..f capturing the audit's unresolved discipline-spirit gaps: G-6a LLM-replay-audit gap, G-6b freeze-rule carve-out should be operationally bounded, G-6c seed authoring quality is unguarded human step (3/46 pinned), G-6d N=5 statistically thin, G-6e ADR-56 spirit-tension on seed_065, G-6f seed_157 demotion-and-replace deferred to corpus-quality maintenance. All flagged "documented; none scheduled; re-enter scope only when an explicit ADR proposes in-scoping". (3) reports/phase6_1_close_out.md NEW — single-page chain close-out summary combining 8 sub-block deliverables, audit findings, what the chain claims AND does NOT claim. The audit-informed cycle verdict (final canonical form): "Phase 6.1' shipped corpus discipline + bundle generator + holdout discipline + bootstrap fixes; produced 2 claim-supporting round-trip outcomes (1 train + 1 holdout) and 1 honest negative. The chain validates the discipline end-to-end. It does NOT demonstrate broad cross-target generalisation; the empirical signal is thin (N_pinned=5) and the Phase 6.2 audit identified 6 unresolved discipline-spirit gaps (G-6a..f in BACKLOG.md)." This supersedes the QA/A47 verdict_q3 sentence in the canonical reading; QA/A47 verdict preserved verbatim in QA/A47.md for record. (4) NO retroactive editing of historical Phase 6.1'a..h reports — they retain "FIRST holdout generalisation success" / "partial generalisation" phrasing as historical record of the trajectory; only NEW canonical surfaces (project_status.md §8, close-out, audit aggregate) carry the audit-informed framing. (5) NO modification of generator/verifier/clone-helper/seed-partitions/holdout-scopes per cgpro QA/A47 hard rule applied to BOTH the audit commit AND this consolidation commit. Per cgpro QA/A47: corpus-quality maintenance (seed_157 demote-and-replace specifically) is a SEPARATE LATER labelled task, NOT done in this consolidation. Test count UNCHANGED at 1128 (no functional code change to test). ZERO new dependency. ZERO MCP runtime. ZERO provider tool-calling. ZERO change to runtime path code. ZERO change to enable-tool-gateway default. Manual-lane scripts unchanged at 3. All Phase 4.7+ anti-MCP / no-product-verdict / lane-separation / partition-discipline / holdout-discipline / freeze-rule / audit-as-block locks remain ACTIVE. Phase 6.1' chain formally CLOSED with audit-informed reading canonical. Future work options recorded in BACKLOG G-6 + project_status §5 roadmap; NO commit to which option will be pursued or when.

[2026-04-29 13:00:00] - Phase 6.2 — AI-tier cold-reader audit of the Phase 6.1' chain (ADR-63, commit pending). Per QA/A47 cgpro verdict_q1 (pursue_a) + next_action: run the audit BEFORE methodology consolidation as guard against self-congratulation. 3-provider panel (DeepSeek + Grok + MiniMax; Kimi excluded per Phase 6.0.y / QA/A42 documented 401 failure). DeepSeek `deepseek-v4-pro` empty-response on first call → retried with `deepseek-chat` and got 71-line substantive critique (empty-then-retry trajectory recorded as audit data). Audit surface: reports/calibration_seed/{README,schema,worked_example_phase6_1_a}.md + 8 phase reports (phase6_1_a..h_*). Total prompt size 120KB. Updated scripts/run_ai_adversarial_review.py: _DOC_PATHS_TO_REVIEW pointed at new surface; _SYSTEM_PROMPT rewritten for methodology-audit role (vs prior beta-cold-reader role; same skeleton); SSL VERIFY_X509_STRICT relaxed for Python 3.13 + DeepSeek cert chain (same patch as ADR-57); output written under reports/ai_adversarial/phase6_2_chain_review/ subdir to preserve prior 6.0.y critiques. 5 CONVERGENT (3/3) METHODOLOGY CRITIQUES: C1 verdict-leak risk in headline framing ("verification_candidate" + "FIRST holdout success" + "partial generalisation" together create soft verdict surface despite no forbidden phrase); C2 seed_157 reclassification (target_bootstrap_gap → honest negative) inconsistent without documented ADR/methodology update; C3 freeze-rule "predeclared env bootstrap" carve-out dangerously broad (--install-extras/--install-group/--scm-pretend-version added IN RESPONSE to holdout failures yet treated as "env bootstrap not tooling edits"); C4 seed authoring quality is unguarded human step (3/46 records pinned; 43 unpinned likely have similar defects); C5 ratio guard at N_pinned=5 is brittle (single case = entire 0.20-0.40 band). 5 SINGLE-PROVIDER SHARP CRITIQUES: D1 (DeepSeek) LLM-authored replays unaudited — Pydantic checks SHAPE not CONTENT, could be hallucinated outputs matching verifier expectations; D2 (DeepSeek) seed_157 attribution as "authoring defect" is speculation, could be tooling failure; G1 (Grok) seed_065 success was achieved AFTER 6.1'f+g fixes motivated by its own failure — ADR-56 SPIRIT VIOLATION ARGUMENT (chain itself acknowledges tension but never resolves it); G2 (Grok) statistical thinness — N=5 with 2 holdouts too thin for "partial generalisation" claim; M1 (MiniMax) selection bias not demonstrated — both holdouts maintainer-authored, generalisation to community-fork PRs unproven. STRICT-LETTER HARD WALL INTACT (no forbidden phrase emitted) but SPIRIT WOBBLY (verdict-like framing through soft language). Verdict-leak phrases identified for downstream softening in the next-commit consolidation: "FIRST holdout generalisation success" (phase6_1_h, high), "the chain CAN now honestly claim partial holdout generalisation" (phase6_1_h, high), "first claim-supporting outcome in the calibration_seed lane" (phase6_1_e_steps_1_3, medium), "the verifier accepts the claim" (multiple, medium), "FIRST holdout-target outcome where the verifier accepts a claim through the entire grounded path" (phase6_1_h, high). 5-step action list for the methodology consolidation commit (next): (1) soften verdict-leak phrasing in docs/project_status.md + close-out summary; (2) document the seed_157 reclassification trajectory honestly (target_bootstrap_gap → honest negative was a real shift, not handwave); (3) tighten freeze-rule carve-out definition operationally (flags that existed BEFORE the holdout pass, not flags added in response); (4) acknowledge the LLM-replay-audit gap (D1) explicitly; (5) reframe the cycle verdict closer to "validated discipline + 1 train + 1 holdout success + N=5 signal is thin + ADR-56 spirit-violation tension unresolved" than "partial generalisation". Test count UNCHANGED at 1128 (audit is documentation-only). ZERO runtime-path code change. ZERO change to generator / verifier / clone helper / seed partitions / holdout scopes (per cgpro QA/A47 hard rule). DeepSeek + Grok + MiniMax provider spend ~$0.005. Cumulative chain spend (6.1'd + 6.1'e step 4 + 6.1'h + 6.2) ~$0.012. Manual-lane scripts unchanged at 3. AI-tier review tool (scripts/run_ai_adversarial_review.py) is NOT a manual-data-acquisition lane script per ADR-51 — no MANUAL_EGRESS_SCRIPT marker; reads disk + writes critiques + makes provider calls but does NOT modify any seed record or any project source code. All Phase 4.7+ anti-MCP / no-product-verdict / lane-separation / partition-discipline / holdout-discipline / freeze-rule locks remain ACTIVE. Per ADR-51 + QA/A42 line 350: this critique is `ai_adversarial` lane, NOT operator feedback; structurally separated from `reports/beta/`. Next: methodology consolidation (separate commit) incorporating C1-C5 + D1/D2/G1/G2/M1.

[2026-04-29 12:00:00] - Phase 6.1'h — fresh freeze-rule holdout pass at post-6.1'g SHA produces 1/2 verification_candidate; FIRST holdout generalisation success in calibration_seed lane (seed_065 sqlite-utils REAL/FLOAT migration accepted with pytest "1 passed in 0.94s"); seed_157 produces honest claim-level negative (over-broad operator-authored test_scope; specific tests pass individually but the class-scope collected pre-existing unrelated failing tests); test-deps bootstrap fully fixed — pytest reached the venv on both holdouts (ADR-62, commit pending). Frozen manifest at code SHA de26bce: DeepSeek deepseek-chat (no prompt edits), no in-pass edits to generator/verifier/clone-helper/LLM-prompts/replay-shapes; predeclared env-bootstrap allowed (--scm-pretend-version, --install-extras, --install-group, --import-smoke per cgpro QA/A45_followup verdict_q1 carve-out); seed_008 NOT re-run (train control). Frozen invocations: seed_065 → `--install-extras test --import-smoke sqlite_utils` (PEP 621); seed_157 → `--install-group tests --scm-pretend-version structlog=25.5.0.dev0 --import-smoke structlog` (PEP 735). Both clones produced clean post-install state (target package importable + pytest 9.0.3). Both round-trips: prepare-gateway-bundle → llm_author_replays.py (DeepSeek 6.4s + 6.4s) → verify-grounded with PATH-prepended clone-venv pytest. RESULTS: seed_065 status=verification_candidate accepted=['C.column_type_mapping.repair_needed'] pytest "1 passed in 0.94s" — FIRST HOLDOUT GENERALISATION SUCCESS; the entire pipeline (corpus discipline + generator + LLM-author + bootstrap helper + verifier) accepted a claim on a target the generator was NOT designed against (seed_065 was Tier-3-pinned in 6.1'c BEFORE 6.1'b's generator was iterated; post-fix bootstrap helper is target-class-general). seed_157 status=diagnostic_only unsupported=['C.callsite_qual_name.capability_sufficient'] pytest rc=1 with multiple failures; ROOT CAUSE (observation-only, allowed under freeze rule's predeclared-bootstrap carve-out): the operator-authored test_scope was `tests/processors/test_processors.py::TestCallsiteParameterAdder` (whole class) which collects MANY tests including pre-existing ones unrelated to the QUAL_NAME claim that fail in the isolated venv; the SPECIFIC two tests added by PR #761 (test_qual_name_structlog, test_qual_name_logging_origin_absent) each pass individually (1 passed in 0.03s + 1 passed in 0.02s). This is a SEED-RECORD authoring quality issue (over-broad test_scope), NOT a bootstrap gap and NOT a tooling failure. Per cgpro outcome matrix: claim-level pytest failure → archive as holdout result, not as tooling failure. Per ADR-56 holdout discipline: test_scope is Tier-3, modifying it post-pin would violate pinning. The honest negative counts as holdout evidence. Future post-pass corrective options: (a) demote seed_157 to train and pin fresh holdout from 46 inclusions, (b) accept the negative outcome as honest signal. Holdout discipline state at end of 6.1'h: N_pinned=5 unchanged structurally; train: seed_008 verification_candidate, seed_062 + seed_142 Tier-3-pinned never round-tripped; holdout: seed_065 VERIFICATION_CANDIDATE (first), seed_157 diagnostic_only honest-negative (over-broad test_scope documented). Holdout ratio 0.40 ENFORCING. Generalisation tally: 1/2 holdouts succeeded — partial generalisation demonstrated. CYCLE VERDICT UPDATE (vs QA/A46 verdict_q4): the chain CAN now claim partial holdout generalisation. Both prerequisites met: 6.1'g fixed bootstrap gap; 6.1'h's seed_065 success satisfies "fresh frozen holdout pass succeeds" in the at-least-one sense. Test count UNCHANGED at 1128 (no new tests this commit per freeze rule; test-scope-quality work would be separate post-pass block). ZERO runtime-path code change. ZERO new dependency. ZERO MCP. ZERO provider tool-calling in runtime path. ZERO change to enable-tool-gateway default. DeepSeek round-trip cost ~$0.002. Total chain provider spend ~$0.007. Manual-lane scripts unchanged at 3. All Phase 4.7+ anti-MCP / no-product-verdict / lane-separation / partition-discipline / holdout-discipline / freeze-rule locks remain ACTIVE. Phase 6.1' chain now empirically grounded with 2 verification_candidate outcomes (1 train + 1 holdout) + 1 honest negative + 6 sub-block ADRs. AI-tier cold-reader rerun NOW UNLOCKED per QA/A46 (was OFF-the-table until 6.1'g/h closed). Operator's choice for next priority: (a) AI-tier rerun on new surface, (b) seed_157 demotion-and-replace, (c) docs/project_status.md update + methodology consolidation, (d) other backlog priorities (G-3 pivot, public benchmark, etc. — strict operator decision).

[2026-04-29 11:00:00] - Phase 6.1'g — clone helper second-order bootstrap fix: --install-extras (PEP 621) + --install-group (PEP 735) + auto pytest-smoke (ADR-61, commit pending). Per QA/A46 cgpro session phase61-review (third resume): verdict_q1 one_more_block_recommended, verdict_q2 e (6.1'g focused continuation), verdict_q3 process_positive, verdict_q4 = "Phase 6.1' validated the discipline and produced one real checkout proof-of-concept, but it must not claim holdout generalisation until the test-extras bootstrap gap is fixed and a fresh frozen holdout pass succeeds." next_action gave concrete scope. Two parallel CLI flags added to scripts/clone_target_at_sha.py because the 2 holdouts use different packaging standards: --install-extras EXTRAS (PEP 621, repeatable) → `pip install -e <clone>[EXTRAS]` for projects declaring dev/test deps under [project.optional-dependencies] (sqlite-utils has [test]); --install-group GROUP (PEP 735, repeatable) → `pip install --group <pyproject>:GROUP` after editable install for projects using [dependency-groups] (structlog has [tests]). Auto pytest-smoke `python -m pytest --version` runs after all installs whenever ANY extras OR groups are requested; failure produces target_bootstrap_gap banner. Empirical probes (NOT committed): sqlite-utils@e7ecb0ff with `--install-extras test` → `pytest 9.0.3` ✓; structlog@f7e9f78d with `--install-group tests --scm-pretend-version structlog=25.5.0.dev0` → `pytest 9.0.3` ✓. Wrong-extras-name produces helpful banner with hint "did you forget --install-extras with the right extras name (e.g. tests/dev/test)?". `_pip_install_editable` signature updated with `extras: tuple[str, ...] = ()` parameter — when non-empty the path arg becomes `<src_dir>[a,b]` (pip standard syntax, single argv entry under subprocess.run, no shell quoting needed). New `_pip_install_groups` helper (~30 lines, one pip invocation per group via `<pyproject>:<group>` form) and `_pytest_version_smoke` helper (~25 lines). Tests +9 in tests/test_phase6_1_g_extras_and_groups.py (hermetic via monkeypatch + subprocess.run stub): test_pip_install_editable_extras_forwarded, test_pip_install_editable_no_extras_unchanged, test_pip_install_groups_per_group_invocation, test_pytest_version_smoke_happy_path, test_pytest_version_smoke_failure_banner, test_main_runs_pytest_smoke_when_extras_provided, test_main_runs_pytest_smoke_when_groups_provided, test_main_no_pytest_smoke_when_neither_provided, test_main_passes_extras_through_to_install. Phase 6.1'f stubs of _pip_install_editable updated to accept new extras kwarg (signature compatibility only, no semantic change). Test count 1119 → 1128 (+9). ruff clean, mypy clean (100 source files). ZERO new dependency. ZERO MCP runtime. ZERO provider tool-calling in runtime path. ZERO change to enable-tool-gateway default. ZERO change to runtime path code (this commit edits scripts/ + tests/ + reports/ + memory-bank/ only). Manual-lane scripts unchanged at 3. Holdout discipline state: N_pinned=5 unchanged structurally; seed_065 + seed_157 still tainted-by-bootstrap-gap pending the freeze-rule pass at post-6.1'g SHA (Phase 6.1'h, next commit). Per QA/A46: AI-tier rerun + benchmark exploration + MCP runtime explicitly OFF the table until 6.1'h confirms the path. All Phase 4.7+ anti-MCP / no-product-verdict / lane-separation / partition-discipline / holdout-discipline / freeze-rule locks remain ACTIVE. cgpro's two-flag minor scope expansion documented as accepted (same class of fix, two mechanical paths for two packaging standards). Next: Phase 6.1'h freeze-rule holdout pass at post-6.1'g SHA.

[2026-04-29 10:00:00] - Phase 6.1'f — clone helper bootstrap fix MINIMAL (ADR-60, commit pending). User said "demande à cgpro" → cgpro QA/A45_step4_outcome (resumed phase61-review session) returned: verdict_q1 mixed_with_explicit_qualifier (discipline validated; verification_candidate path not yet broadly viable); verdict_q2 pursue_6_1_f_fix (fix bootstrap before AI-tier rerun, dominant failure localized upstream of verifier); verdict_q3 minimal_first_then_broader_in_separate_block (smallest falsifiable corrective: install-order flip + post-install import smoke; do NOT add build-backend plumbing or pip-upgrade in same block — would blur whether minimal hypothesis closes the class); next_action gave EXACT scope. Three changes to scripts/clone_target_at_sha.py only: (1) install-order flip — when --install-oida-code is set, oida-code installed FIRST then target editable LAST (hypothesis: pip's editable-install dependency resolution can remove the target's editable link if a later install re-resolves shared deps); (2) --import-smoke PACKAGE flag (repeatable) runs <venv>/python -c "import PACKAGE" after all installs, fast-fails with target_bootstrap_gap banner + stderr tail on import failure; (3) docstring update documenting install-order rationale + non-pytest example. Empirical validation (one-off probes, NOT committed): both holdout targets now import successfully — `import sqlite_utils` OK on sqlite-utils@e7ecb0ff, `import structlog` OK on structlog@f7e9f78d (with --scm-pretend-version structlog=25.5.0.dev0). SECOND-ORDER GAP SURFACED, NOT FIXED in this block per cgpro minimal_first: even with import-smoke passing, non-pytest target venvs lack pytest itself (pytest comes from a [tests] extra; `pip install -e .` doesn't request it; for seed_008 this works because pytest IS the target package). Per cgpro: defer to next block. Tests +8 in tests/test_phase6_1_f_clone_bootstrap.py (hermetic via monkeypatch + importlib spec_from_file_location): test_clone_module_carries_egress_marker, test_install_order_oida_code_first (asserts oida-code label precedes target label in _pip_install_editable call sequence), test_install_order_target_only_when_no_oida_code, test_import_smoke_command_construction (one `python -c "import X"` per smoke entry), test_import_smoke_failure_reporting (SystemExit(2) + stderr contains target_bootstrap_gap + failing pkg name), test_main_invokes_import_smoke_after_installs, test_no_import_smoke_skips_smoke_step, test_workflow_non_reference_test_still_passes (ADR-53 lane isolation still enforced). All hermetic — no real git/pip/network — sub-100ms total. Test count 1111 → 1119 (+8). ruff clean, mypy clean (100 source files). ZERO new dependency. ZERO MCP runtime. ZERO provider tool-calling in runtime path (src/oida_code/). ZERO change to enable-tool-gateway default. ZERO change to runtime path code (this commit edits scripts/ + tests/ + reports/ + memory-bank/ only). Manual-lane scripts unchanged at 3. Holdout discipline state: N_pinned=5 (3 train + 2 holdout) unchanged structurally; seed_065 + seed_157 still tainted-by-bootstrap-gap pending second-order fix. seed_008 NOT re-run (train control). All Phase 4.7+ anti-MCP / no-product-verdict / lane-separation / partition-discipline / holdout-discipline / freeze-rule locks remain ACTIVE. Phase 6.1' chain methodologically clean to attempt holdout replacement once second-order gap also closed. Next: Phase 6.1'g (or 6.1'f-broader) — add --install-extras EXTRAS flag for `pip install -e ".[EXTRAS]"`; verify pytest appears in venv for sqlite-utils + structlog; re-run freeze-rule holdout pass on cleared/replaced cases.

[2026-04-29 09:00:00] - Phase 6.1'e step 4 — holdout round-trips under freeze rule produce 0/2 verification_candidate; both seed_065 + seed_157 surface target_bootstrap_gap (target package not importable from clone-venv at pytest time despite pip install -e success); freeze rule strictly enforced — NO in-pass fix (ADR-59, commit pending). User said "demande à cgpro" → cgpro QA/A45 follow-up (resumed phase61-review session) returned: verdict_q1 mixed_with_explicit_test (strict freeze in-pass, lenient follow-up after pass with explicit non-holdout regression test); verdict_q2 both_in_parallel_with_doc_split (single-batch + per-case archive); verdict_q3 scope_creep_acknowledge_but_keep (Phase 6.1'e step 3 pytest adapter fix labeled ADR-49.x); step_4_recommendation gave EXACT frozen commands per case. Frozen manifest: code SHA f27e40c; DeepSeek deepseek-chat (no prompt edits); seed_065 sqlite-utils@e7ecb0ff (no scm-pretend, declares static 4.0a0); seed_157 structlog@f7e9f78d (--scm-pretend-version structlog=25.5.0.dev0, hatch-vcs unreleased-after-25.4.0); seed_008 NOT re-run (train control). Both round-trips ran (LLM call ~7s each, all Pydantic-valid replays, generator+verifier+clone-helper+LLM-prompt unchanged in-pass). Result for both holdouts: status=diagnostic_only, tool_calls=1, claim demoted to unsupported. seed_065 root cause (observed): pytest rc=4 — "ImportError while loading conftest 'tests/conftest.py'. from sqlite_utils import Database" — sqlite_utils not importable from venv. seed_157 root cause: pytest rc=4 — "ImportError ... import structlog. ModuleNotFoundError: No module named 'structlog'" — same shape. Both classified as `target_bootstrap_gap` per cgpro outcome matrix; NEITHER counted as holdout evidence (pending replacement post-pass). Possible root causes (NOT investigated in-pass): install order (oida-code installed AFTER target may have re-resolved the editable link); build backend differences (pytest=setuptools_scm, sqlite-utils=setuptools, structlog=hatch-vcs); pip install -e default flag differences across backends. The seed_008 train pipeline does NOT generalise to non-pytest-shaped targets — pytest is itself the target package so test imports come from source tree directly; both holdouts have tests/conftest.py importing the package, and that import fails. The freeze rule did its job — prevented an in-pass tooling edit that would have biased the holdout outcome. Honest evidence of a real generalisation gap; exactly what holdout discipline is for. Phase 6.1'e step 4 deliverables (this commit): QA/A45_followup.md (NEW, durable cgpro response), reports/phase6_1_e/round_trip_outputs/seed_065_simonw_sqlite_utils_680/ (NEW, 9 evidence files), reports/phase6_1_e/round_trip_outputs/seed_157_hynek_structlog_761/ (NEW, 9 evidence files), reports/phase6_1_e_step_4_holdouts.md (NEW, phase report with freeze-rule compliance check + per-case sections + post-pass corrective work plan), ADR-59 in memory-bank/decisionLog.md. Test count unchanged 1111 (no new tests in this commit per freeze rule; the bootstrap fix + non-holdout regression test land in a separate phase block per cgpro verdict_q1). ZERO new dependency. ZERO MCP runtime. ZERO provider tool-calling in runtime path. ZERO runtime-path code change in this commit (the freeze rule prevented exactly this). ZERO change to enable-tool-gateway default. Manual-lane scripts unchanged at 3 (build_calibration_seed_index, llm_author_replays, clone_target_at_sha). All Phase 4.7+ anti-MCP / no-product-verdict / lane-separation / partition-discipline / holdout-discipline / freeze-rule locks remain ACTIVE. Phase 6.1' chain status at end of step 4: 5 ADRs (53/54/55-retracted/56/57/58/59), 5 train+holdout cases pinned (3 train all validated through some round-trip; 2 holdouts in target_bootstrap_gap state pending replacement), 1 verification_candidate outcome (seed_008 train), 0/2 holdout generalisation. Next options: (a) post-pass corrective block to fix bootstrap gap + replace affected holdouts; (b) AI-tier cold-reader re-run on the new corpus (originally part of 6.1'e); (c) other priorities.

[2026-04-29 08:00:00] - Phase 6.1'e steps 1-3 — runtime-loader guard + 2 more train pins + first verification_candidate (ADR-58, commit pending). Per QA/A45 (cgpro phase61-review) 4-step ordering: (1) front-load runtime-loader acceptance guard, (2) pin 2 more train cases → N_pinned=5 ratio enforcing, (3) seed_008 real target checkout, (4) holdouts under freeze rule (deferred to separate commit). Steps 1-3 in this commit; step 4 separate per cgpro verdict_q3 freeze-rule discipline. (1) tests/test_phase6_1_e_runtime_loader_guard.py +9 tests load each generated bundle's 8 files through Pydantic contracts (LLMEvidencePacket, ToolPolicy, GatewayToolDefinition per-tool, ToolAdmissionRegistry, ForwardVerificationResult, list[BackwardVerificationResult]); closes ADR-55→ADR-57 retraction gap; bundle acceptance now formally "validate_gateway_bundle ok AND runtime-loader smoke ok". (2) seed_062 (sqlite-utils #690 callable refs, capability_sufficient) and seed_142 (structlog #790 over-zealous warning removal, repair_needed) authored Tier-3-complete and pinned partition=train. N_pinned 3→5; holdout ratio = 2/5 = 0.40 inside enforcing [0.20, 0.40] band. Phase 6.1'c test_holdout_ratio_in_band_when_pool_large now ENFORCING (was informational at N<5). (3) New scripts/clone_target_at_sha.py (~210 lines, manual lane MANUAL_EGRESS_SCRIPT marker, refuses without --manual-egress-ok): shallow-fetches public repo at SHA via git init+fetch+checkout FETCH_HEAD; idempotent on (repo, sha8); creates <clone>/.venv; pip-installs target editable into venv; optional --install-oida-code adds the local oida-code package to the same venv; supports --scm-pretend-version PACKAGE=VERSION (repeatable) for shallow-clone setuptools_scm gotchas (pytest required pytest=9.0.0 because shallow clone lacks tag history). End-to-end seed_008 round-trip: prepare-gateway-bundle → llm_author_replays.py (DeepSeek 8.2s) → verify-grounded with --repo-root <clone> AND venv's Scripts dir prepended to PATH so shutil.which finds clone-venv pytest@head_sha not outer-venv pytest. RESULT: status=verification_candidate, tool_calls=1, accepted_claims=['C.cli_version_flag.repair_needed'], pytest "2 passed in 0.63s". FIRST claim-supporting outcome in calibration_seed lane. Round-trip evidence archived under reports/phase6_1_e/round_trip_outputs/seed_008_pytest_dev_pytest_14407/. (BONUS) Pytest adapter discovery + fix during step 3: the verifier's `-o addopts=` neutralization (Phase 5.9 / ADR-49) stripped pytest's `-p pytester` plugin pre-load directive from clone's addopts, making pytester_example_dir "Unknown config option" → pytest exit rc=4. Fix: src/oida_code/verifier/tools/adapters.py::_extract_pytest_plugin_args parses target's pyproject.toml for [tool.pytest].addopts or [tool.pytest.ini_options].addopts, extracts any `-p <plugin>` pairs, preserves them in verifier's argv. Lazy-imports tomllib (stdlib Python 3.11+); defensive parsing means missing/malformed pyproject is graceful no-op. seed_008 is TRAIN — discipline allows tuning. Fix is target-class-general (any project with `-p plugin` in addopts now works through the verifier). Test count 1102 → 1111 (+9). ruff clean (after removing unused noqa), mypy clean (100 source files). ZERO new dependency. ZERO MCP runtime. ZERO provider tool-calling in runtime path. ZERO change to enable-tool-gateway default. ZERO change to provider_config.py. Manual-lane scripts now 3: build_calibration_seed_index.py (6.1'a-pre), llm_author_replays.py (6.1'd), clone_target_at_sha.py (6.1'e); all carry MANUAL_EGRESS_SCRIPT marker; all auto-discovered by test_no_manual_egress_script_referenced_in_workflows. Step 4 (seed_065 + seed_157 holdouts under freeze rule) commits separately so the rule applies to a clean evaluation pass. All Phase 4.7+ anti-MCP / no-product-verdict / lane-separation / partition-discipline / holdout-discipline locks remain ACTIVE.

[2026-04-29 06:30:00] - Phase 6.1'd — LLM-author replays + verify-grounded round-trip on 3 pinned cases (ADR-57, commit pending). User said "go for llm author" specifying authoring method. Per advisor's 10-min experiment guidance, FIRST work was a probe run of verify-grounded with hand-edited replays to determine outcome (advisor outcome 1 needs target checkout / outcome 2 = `diagnostic_only` clean). Outcome 2 confirmed empirically — but only AFTER fixing 3 generator-shape bugs that validate_gateway_bundle did not catch: (1) approved_tools.json was a JSON array `["pytest"]`; runtime needs ToolAdmissionRegistry object with sha256 fingerprint. (2) pass*_backward.json was a single object; runtime needs JSON LIST per the keystone shape (pass1_backward=[], pass2_backward=[{...one entry per claim...}]). (3) gateway_definitions content was duplicated inline; factored into _pytest_definition() + _pytest_fingerprint() helpers so the 4 sha256 hashes are byte-identical with the matching definition entry. ADR-55's "skeleton is structurally valid" claim is RETRACTED in the strict-Pydantic-loader sense — the skeleton was file-presence + filename-pattern valid (validate_gateway_bundle), but NOT runtime-Pydantic valid until the fixes. Layered acceptance design works: structural validator + runtime loader catch different gaps. New script `scripts/llm_author_replays.py` (~290 lines, manual lane, MANUAL_EGRESS_SCRIPT=true marker, refuses without --manual-egress-ok): reads seed record + bundle's packet.json, calls DeepSeek (chat-completions endpoint with response_format=json_object), validates output against ForwardVerificationResult / BackwardVerificationResult Pydantic, archives skeleton stubs into sibling `_skeleton/` dir for inspection, overwrites bundle with LLM output. SSL VERIFY_X509_STRICT relaxed (Python 3.13 enabled this default; DeepSeek cert chain has CA without Authority Key Identifier extension; symptom: 'SSL: CERTIFICATE_VERIFY_FAILED — Missing Authority Key Identifier'). System prompt is highly structured: enumerates EXACT Pydantic shape (extra=forbid), names every required VerifierClaim field (claim_id, event_id, claim_type, statement, confidence, evidence_refs, source) with constraints, names 7-Literal claim_type allowlist + 8-Literal evidence_kind allowlist + 5-Literal source allowlist, EXPLICITLY enforces the 0.6 confidence cap for source=forward/backward (Phase 5.x verifier aggregator caps LLM-style sources at 0.6 — went above and got rejected with "confidence 0.85 exceeds 0.6 cap for LLM-style verifier sources"). Round-trip on 3 pinned cases (1 train + 2 holdout) all converged to status=diagnostic_only with tool_calls=1: seed_008 (8.2s LLM call), seed_065 (8.1s), seed_157 (8.9s). Honest verifier behavior: bundle accepted, gateway invokes pytest, pytest reports test_scope-not-found (test scope lives in target repo, not local oida-code via --repo-root .), verifier classifies as diagnostic_only and demotes claim to unsupported. To get verification_candidate requires real target checkout (advisor outcome 1) — Phase 6.1'e+ work. ADR-56 holdout discipline preserved: 3 generator fixes were caught using seed_008 (train), apply BEFORE running on holdouts; holdouts ran THROUGH unchanged generator. Round-trip evidence committed under reports/phase6_1_d/round_trip_outputs/<case_id>/ (27 files: 9 verifier inputs + grounded_report.json per case × 3 cases). Tests +5 (4 new in tests/test_phase6_1_d_llm_author_replays.py — egress marker check, refusal mode, dynamic-discovery workflow scan, sanity check on discovery; 1 new in tests/test_phase6_1_b_bundle_generator.py — ToolAdmissionRegistry shape; existing pass-stub test updated for list-shape backward replays). Test count 1097 -> 1102 (+5). ruff clean (after auto-fix B007 unused loop vars + UP012 unnecessary encoding arg), mypy clean (100 source files). ZERO new dependency (stdlib only). ZERO MCP runtime. ZERO provider tool-calling in runtime path (src/oida_code/). ZERO change to enable-tool-gateway default. Provider call lives in scripts/ (manual lane) per ADR-53 frontière rule 1. Per-call cost ~$0.001; total Phase 6.1'd provider spend ~$0.003. All Phase 4.7+ anti-MCP / no-product-verdict / lane-separation / partition-discipline locks remain ACTIVE. Next: Phase 6.1'e (final block of 6.1' chain) — re-run AI adversarial review on new docs/beta surface, Yann-solo dogfood with real target checkout (clone pytest-dev/pytest@head_sha) for verification_candidate outcome, optional multi-provider panel for replay triangulation.

[2026-04-29 06:00:00] - Phase 6.1'c — calibration-seed corpus expansion N=2 -> N=46 + partition discipline activation (ADR-56, commit pending). User said "go" overriding advisor's earlier "stop here" framing; advisor had warned this is multi-hour manual Tier-3 work, but the user explicitly directed proceeding. Three deliverables: (A) Indexer bug fix — `pr.head.repo == None` (deleted fork) crashed _classify_pr; fixed by treating None head_repo as fork_pr_refused. Script version `phase6_1_a_pre_v1` -> `phase6_1_c_v1`. (B) Corpus expansion — ran indexer with PAT_GITHUB authenticated (5000 reqs/hour budget; consumed ~250) against 13 additional public Python repos: python-attrs/attrs, psf/black, pydantic/pydantic, tiangolo/typer, pallets/itsdangerous, python-poetry/poetry, simonw/sqlite-utils, samuelcolvin/watchfiles, encode/httpx, pytest-dev/pluggy, hynek/structlog. Result: 46 inclusions (+44) and 199 exclusions (+185). High-yield repos validate advisor's selection-effect note: simonw/sqlite-utils 13 inclusions, hynek/structlog 12 inclusions, pydantic/pydantic 5 inclusions; community-fork-heavy repos like psf/black caught by fork-PR fence (1 inclusion / 20 exclusions). Selection-effect caveat from Phase 6.1'a partially borne out: of 46 inclusions, the 2 from pytest-dev/pytest are backports but the remaining 44 are direct internal-branch merges (NOT backports). (C) Schema extension — `partition: train | holdout | null` and `partition_pinned_at: ISO 8601 UTC | null` added to every record (default null for partial records). schema.md gets a "Partition discipline" section with: pinning protocol (6 steps), holdout-set ratio guard [0.20, 0.40] (informational at N_pinned < 5), demotion protocol (holdout -> train if defect found post-pin), discipline lifecycle (6.1'c lands schema; 6.1'd uses train cases only for tuning; 6.1'e cites holdout outcomes). Three cases pinned: seed_008 (worked example) as `train`; seed_065_simonw_sqlite_utils_680 (sqlite-utils REAL/FLOAT migration, claim_type=repair_needed) as `holdout`; seed_157_hynek_structlog_761 (structlog QUAL_NAME, claim_type=capability_sufficient) as `holdout`. Both holdout cases are operator-authored Tier-3 (claim_id, claim_text, evidence_items, test_scope) with 2 evidence_items each. Diversity: 3 distinct orgs, 2 repair_needed + 1 capability_sufficient, 2 single-test + 1 class-scoped test_scope, 1 backport + 2 non-backport. Sanity check: both holdout cases run through `oida-code prepare-gateway-bundle` produce valid bundles passing `validate-gateway-bundle` — confirms generator design generalises beyond seed_008 self-audit example. NO generator change made in response (would violate discipline). Tests +10 in `tests/test_phase6_1_c_partition_discipline.py`: schema-presence, allowlist, iff-coupling, ISO 8601 format, Tier-3-completeness for pinned, hygiene invariants for pinned, ratio guard (informational at N<5), train/holdout disjointness, seed_008-must-be-train, at-least-one-holdout-pinned. Test count 1087 -> 1097 (+10). ruff clean (after removing unused pytest import in new test), mypy clean (100 source files). ZERO new dependency. ZERO MCP runtime. ZERO provider tool-calling in runtime. ZERO change to enable-tool-gateway default. ZERO change to provider_config.py registry. ZERO change to human-beta aggregator. ZERO change to existing verify-grounded / verify-claims / validate-gateway-bundle / prepare-gateway-bundle CLI commands. PAT_GITHUB usage stays manual-only per ADR-53 (loaded from .env for indexer batch; never imported by src/oida_code/). All Phase 4.7+ anti-MCP / no-product-verdict / lane-separation locks remain ACTIVE. Discipline is non-vacuous from day 1: 2 holdout cases the generator was NOT designed against. Next: Phase 6.1'd (generator stress-test on 3 pinned cases — hand or LLM-author replays for seed_008 + seed_065 + seed_157, confirm verify-grounded round-trip succeeds without modifying the generator between holdout runs).

[2026-04-28 22:00:00] - Phase 6.1'b — `prepare-gateway-bundle` skeleton generator + seed-schema `evidence_items` extension (ADR-55, commit pending). Two-part block: (A) schema extension — added `evidence_items` field to `reports/calibration_seed/schema.md` as Tier 3 (ADR-54); shape mirrors `LLMEvidencePacket.evidence_items` exactly (id, kind, summary, source, confidence). Re-pinned `seed_008_pytest_dev_pytest_14407` with 2 operator-authored evidence items (E.event.1: source=git confidence=0.85 backport-aware; E.event.2: source=ticket confidence=0.9). Worked example doc updated. (B) bundle generator — new `src/oida_code/bundle/` module (~330 lines) with `generate_bundle()`, `BundleGenerationError`, `GeneratedBundle`, `REQUIRED_TIER_3_FIELDS`. Emits 9 files (8 verifier-required + README.md) per ADR-55. STAYS UNDER `src/oida_code/` because local composition only — no network call, no provider call, no MCP runtime. Three static test guards enforce: no requests/httpx/huggingface_hub/urllib.request import, no provider/MCP/openai/anthropic substring, no MANUAL_EGRESS_SCRIPT marker. Generator validates input STRICTLY: refuses on Tier-1 missing (case_id/repo_url/pr_number/head_sha/base_sha) / Tier-2 missing (expected_grounding_outcome/label_source) / Tier-3 missing (claim_id/claim_type/claim_text/test_scope/evidence_items) / human_review_required=true / expected_grounding_outcome="not_run" / invalid evidence_kind / confidence outside [0.0,1.0] / forbidden phrase (V_net/debt_final/merge-safe/etc.). Pre-check runs BEFORE any file is written. PASS-REPLAY STUBS DESIGN HONESTY: the four pass*_*.json files are minimal-Pydantic-valid SKELETONS, not verifier output. Each carries the skeleton note "phase6.1.b skeleton — verifier replays are operator/Phase-6.1'd responsibility (deterministic stubs would be theatre)" in its `warnings[]` array (the only Pydantic-valid spot since `extra="forbid"` rejects a `_generator_note` top-level key). pass1_forward.json requests pytest on test_scope (forward INTENT, mechanically derivable); other 3 stubs assert necessary_conditions_met=False. All four validate against ForwardVerificationResult / BackwardVerificationResult. ACCEPTANCE CRITERION = `validate_gateway_bundle` ok, NOT verify-claims round-trip. Advisor's earlier "verify-claims runs" framing was retracted — deterministic stub replays passing verify-claims would be theatre (stubs authored to pass the test). Real round-trip belongs to Phase 6.1'd with operator-authored or LLM-authored replays. CLI subcommand `oida-code prepare-gateway-bundle --case-id <id> --out <dir>` wired in cli.py above validate-gateway-bundle. Auto-validates by default with `--validate/--no-validate`. Tests +19 in `tests/test_phase6_1_b_bundle_generator.py`: file count, validate_gateway_bundle round-trip, packet+evidence_items Pydantic, 4-pass-stub Pydantic+skeleton-warning, no-secrets, 5 Tier-3-missing parametrizations, human_review_required refusal, not_run refusal, invalid evidence_kind refusal, confidence range, forbidden-phrase rejection, idempotence, 3 static-import guards. Test count 1068 → 1087 (+19). ruff clean (after import-organize on bundle/__init__.py), mypy clean (100 source files). ZERO new dependency (stdlib only). ZERO MCP runtime. ZERO provider tool-calling. ZERO change to enable-tool-gateway default. ZERO change to provider_config.py. ZERO change to human-beta aggregator. ZERO change to existing verify-grounded / verify-claims / validate-gateway-bundle CLI commands. Bundle generator is FIRST member of a fifth conceptual layer (bundle authoring) — NOT a new lane (does not produce labels, does not emit operator feedback). Per the user's standing directive ("travaille en autonomie sans oublier les objectifs projets et en consultant régulièrement les autres IA et plugins") this block consulted advisor 3 times: before design (caught evidence_items schema gap, picked option (a)), after constraint discovery (advisor retracted verify-claims-runs framing once Pydantic constraints became visible), and design lock-in. All Phase 4.7+ anti-MCP / no-product-verdict / lane-separation locks remain ACTIVE. Next: Phase 6.1'c (corpus expansion to 20-50 cases + partition: train|holdout schema field, holdout discipline activates).

[2026-04-28 21:00:00] - Phase 6.1'a — first calibration-seed collection + worked example pinned (ADR-54, commit pending). First real invocation of `scripts/build_calibration_seed_index.py` against `pallets/click` (max-prs=5) + `pytest-dev/pytest` (max-prs=20). Result: 2 inclusions + 14 exclusions. Distribution of exclusions: 9 fork_pr_refused, 3 non_python_change, 2 pr_too_trivial. SELECTION-EFFECT FINDING (advisor consultation 2026-04-28): both inclusions are 9.0.x backports because the only PRs that survive the Phase 5.6 fork-PR fence on mainstream public Python projects are typically maintainer-side release-management PRs (backports, version bumps, dep bumps). This is a property of the FILTER, not the corpus. Phase 6.1'c MUST seek non-backport non-release-prep cases (instructions added to ADR-54 + README §Selection-effect caveat). Worked example pinned: `seed_008_pytest_dev_pytest_14407` — PR #14407 (9.0.x backport of community PR #14382), the `-V` short-flag fix in pytest's CLI fast-path. Fully populated record: claim_id="C.cli_version_flag.repair_needed", claim_type="repair_needed" (1 of 7 LLMEvidencePacket.allowed_fields Literal values, defended in worked_example doc against the 6 alternatives), claim_text=one-paragraph narrative with backport caveat, test_scope="testing/test_helpconfig.py::test_version_less_verbose" (parametrized over `["--version","-V"]`), expected_grounding_outcome="evidence_present", label_source="yann_manual_review", human_review_required=false. Three-tier field-derivation pedagogy formalised in `reports/calibration_seed/worked_example_phase6_1_a.md`: Tier 1 API-derived (mechanical, no judgment — repo_url, pr_number, base/head_sha, changed_files_list, etc.) / Tier 2 allowlist-categorical (judgment but Literal-constrained — claim_type, expected_grounding_outcome, label_source, selection_source) / Tier 3 free-form domain reasoning (real teaching — claim_id, claim_text, test_scope, candidate_reason). The bundle authoring helper (Phase 6.1'b) can automate the first two tiers but cannot replace the third. Holdout discipline plan added to README per piège 46: at N=2 the corpus is `pre-holdout` (split would be theatre); discipline kicks in at Phase 6.1'c (N≥20) when the schema gets a `partition: train | holdout` field, a structural test enforces no-edit-after-partition for holdout cases, and the operator commits to using only `train` cases to tune the generator. 4 deliverables in this commit: (1) `reports/calibration_seed/index.json` populated (2 inclusions, one fully-pinned); (2) `reports/calibration_seed/exclusions.json` populated (14 exclusions); (3) `reports/calibration_seed/worked_example_phase6_1_a.md` (NEW, ~6KB end-to-end walkthrough); (4) `reports/calibration_seed/README.md` updated (corpus state table + holdout discipline plan + selection-effect caveat); (5) `memory-bank/decisionLog.md` ADR-54 appended; (6) `reports/phase6_1_a_first_collection.md` (NEW, phase report). Test count unchanged 1068 (this commit ships data + documentation, not new code paths; Phase 6.1'a-pre tests already cover the contract). ZERO new dependency. ZERO MCP runtime. ZERO provider tool-calling. ZERO change to enable-tool-gateway default. ZERO change to src/oida_code/. All Phase 4.7+ anti-MCP / no-product-verdict / lane-separation locks remain ACTIVE. Pre-holdout state honestly disclosed; backport selection-effect honestly disclosed. Next: Phase 6.1'b (minimal `prepare-gateway-bundle` skeleton generator, inputs=index.json record, outputs=bundle dir with claim.json + checkout instructions + pytest_invocation.sh, must handle seed_008 + seed_003 cleanly).

[2026-04-28 20:00:00] - Phase 6.1'a-pre — manual data acquisition lane (ADR-53, commit pending). cgpro QA/A44 verdict: Option Y (bundle generator + adversarial corpus, NOT pure G-3 pivot). Yann's resource picture: PAT_GITHUB + HF_TOKEN + 5+ provider API keys + codex CLI (GPT-5.5 xhigh) + gemini CLI (Gemini 3.1 Pro). cgpro's central insight: these change the capacité d'échantillonnage, not the frontière de confiance. Manual scripts can be rich; runtime verifier must stay poor. Phase 6.1'a-pre lands the FRONTIER first (per QA/A44 §"Action immédiate" — commencer par la frontière, pas par 50 cas / pas par HF upload / pas par open-weight evaluator / pas par panel 10 agents). 7 deliverables: (1) QA/A44.md (NEW, 250-line cgpro response: option Y, 12 frontier rules, calibration dataset boundaries, HF policy, multi-provider sizing, 30 new pièges 27-56); (2) ADR-53 in memory-bank/decisionLog.md (NEW); (3) reports/calibration_seed/README.md (NEW, lane charter with the 12 frontier rules verbatim); (4) reports/calibration_seed/schema.md (NEW, field-by-field schema for index.json + exclusions.json with strict label_source allowlist forbidding llm_only/agent_vote/provider_consensus/cold_reader_label); (5) scripts/build_calibration_seed_index.py (NEW, ~340 lines, MANUAL_EGRESS_SCRIPT=True marker, dry-run default, layered refusal: --manual-egress-ok required to leave dry-run + --public-only required to actually collect, public-only check via repo.visibility before any clone, idempotent merge keyed by (repo_url, pr_number), metadata-only output); (6) tests/test_phase6_1_manual_data_lane_isolation.py (NEW, 8 structural tests: no MANUAL_EGRESS_SCRIPT marker in src/, no requests/httpx/huggingface_hub import in src/ runtime, no PAT_GITHUB/HF_TOKEN env var name in src/, indexer carries marker, indexer dry-run by default, indexer refuses with proper exit codes, no manual-egress script in workflows); (7) docs/project_status.md updated with manual_data_acquisition lane row in the lane status table. Test count 1056 → 1064 (+8). ruff clean (after auto-fix UP017 dt.UTC + RUF100 noqa cleanup), mypy clean (97 source files). ZERO new dependency. ZERO MCP runtime. ZERO provider tool-calling in runtime. ZERO change to enable-tool-gateway default. ZERO change to provider_config.py registry. ZERO change to human-beta aggregator. ZERO new code under src/oida_code/ — all new code is under scripts/ + tests/ (the runtime path stays untouched). The Phase 4.7+ anti-MCP locks remain ACTIVE. Phase 6.1'a-pre does NOT produce actual collected data — it produces only the contract + schema + script (in dry-run mode by default) + structural tests. Actual collection is Yann's manual responsibility AFTER reviewing the contract. Per ADR-53 §Outcome, the binding sentence: "Manual data acquisition may use network credentials. The verifier runtime may not. Manual scripts can collect candidate evidence; they cannot produce human feedback, cannot produce runtime decisions, and cannot relax structural pins." Saved feedback memory `feedback_autonomous_with_ai_consultation.md` per Yann's directive ("travaille en autonomie sans oublier les objectifs projets et en consultant régulièrement les autres IA et plugins"). Next: Phase 6.1'a (bundle schema explainer using one PR from the seed corpus as worked non-self-audit example), then Phase 6.1'b (minimal generator), then Phase 6.1'c (seed corpus expansion 20-50 cases), then Phase 6.1'd (stress-test), then Phase 6.1'e (AI-tier re-run + Yann-solo dogfood).
