# Bundle for `seed_065_simonw_sqlite_utils_680` (Phase 6.1'b skeleton)

This bundle was auto-generated by `oida-code prepare-gateway-bundle` from a Tier-3-complete record in `reports/calibration_seed/index.json`.

**Source:** https://github.com/simonw/sqlite-utils PR #680

**Claim:** `C.column_type_mapping.repair_needed` (type=`repair_needed`)

**Base SHA:** `fb93452ea8e677f0a2fb002f9c2483072432a6cd`

**Head SHA:** `e7ecb0ffdfcb15a879e0da202a00966623f1e79c`

**Test scope:** `tests/test_cli.py::test_csv_detect_types_creates_real_columns`

**Expected grounding outcome:** `evidence_present`

## What this bundle is

A skeleton — the four input files (`packet.json`, `tool_policy.json`, `gateway_definitions.json`, `approved_tools.json`) are complete and Pydantic-valid against the verifier contracts. The four `pass*_*.json` files are minimal-schema-valid stubs whose `warnings` array explicitly says the verifier replays are operator / Phase-6.1'd responsibility. The skeleton bundle is structurally valid against `validate-gateway-bundle` but is NOT a runnable verifier round-trip. That is Phase 6.1'd's job.

## What this bundle is NOT

* It is NOT proof that the upstream change is correct.
* It is NOT a product verdict (no merge-safe / production-safe / bug-free / verified language).
* It is NOT external-human beta evidence (per ADR-52 the human-beta lane is `not_run`).
* It is NOT runnable through verify-grounded without hand-authored or LLM-authored replay JSONs replacing the four `pass*_*.json` skeleton stubs.

## How to use it (Phase 6.1'd preview)

In Phase 6.1'd, the operator OR an LLM-as-replayer will overwrite the four `pass*_*.json` stubs with meaningful verifier replies, then invoke `oida-code verify-grounded` with this directory. The `warnings` array in each stub names the deferred responsibility explicitly.

## Cross-references

* Seed record: `reports/calibration_seed/index.json::seed_065_simonw_sqlite_utils_680`
* Worked-example walk-through: `reports/calibration_seed/worked_example_phase6_1_a.md`
* ADR-55: `memory-bank/decisionLog.md`
* Generator: `src/oida_code/bundle/generator.py`
