"""Pass-1 + Pass-2 verifiers (lint, types, SAST, tests, property, mutation). Phase 2+.

Note: ``verify/typing.py`` shadows the stdlib ``typing`` module name; always use
absolute imports (``from oida_code.verify import typing as verify_typing``) when
referencing it from outside this package.
"""
