**Scenario:** no tool needed; claim supported by existing evidence.

The forward verifier neither requests any tool nor cites tool
output. The aggregator accepts the claim purely on event-side
evidence. Demonstrates that the gateway-grounded loop is a no-op
when pass-1 returns `requested_tools: []`.
