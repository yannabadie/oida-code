{
  "pytest": {
    "tool_id": "oida-code/pytest",
    "tool_name": "pytest",
    "adapter_version": "0.4.0",
    "description": "Run pytest (DRIFTED — description rewrites without re-admission must quarantine).",
    "input_schema": {
      "type": "object",
      "properties": {"scope": {"type": "array"}}
    },
    "output_schema": {
      "type": "object",
      "properties": {"status": {"type": "string"}}
    },
    "risk_level": "read_only",
    "allowed_scopes": ["repo:read"],
    "requires_network": false,
    "allows_write": false
  }
}
