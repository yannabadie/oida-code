# Product Strategy Reset

Date: 2026-04-30
Status: active product-direction document
Decision source: cgpro review `repo-product-vision-review`,
conversation `69f329be-0dd4-838f-8687-d68190f21e7d`

This document is the short product compass for the current repo state.
It overrides older roadmap language when there is a conflict about
active product direction. `PLAN.md` remains useful historical and
research context, but this file plus `docs/project_status.md` define
what the next work blocks are allowed to optimize.

## One Sentence

`oida-code` is a diagnostic second opinion for Python reviewers who
need to know whether claims about an AI-authored diff are supported,
contradicted, or unsupported by executable evidence.

It is not a merge gate and it does not certify code as ready for
production.

## Primary User

The primary user for the next 30 days is a Python maintainer, tech
lead, or CI reviewer who receives diffs produced or modified by coding
agents.

Secondary users, including OIDA researchers, platform teams, and future
SaaS/GitHub App operators, are out of scope for product prioritization
until the primary user path is understandable and useful.

## Job To Be Done

When a change generated by an AI agent appears correct, help the
reviewer answer four questions quickly:

1. What exact claim is being made about the diff?
2. Which tool output supports that claim?
3. Which tool output contradicts or weakens it?
4. What still needs human inspection?

The output must be evidence-linked. It must not imply a final product
verdict.

## North Star Outcome

Within 10 minutes, an external Python reviewer can run or read the
diagnostic path, identify at least one load-bearing claim as supported,
contradicted, or unsupported, and understand that the report is not a
merge or production decision.

## Active Product Path

The active product path is:

1. A local CLI diagnostic report for a Python repo/diff.
2. A GitHub composite Action that publishes diagnostic artifacts.
3. An opt-in gateway-grounded diagnostic path, still default-false.
4. A small human-readability validation loop over the diagnostic report.

Everything else is research or backlog until this path is clear.

## Current 30-Day Priorities

1. Make the repo front door coherent: README, this document, and
   `docs/project_status.md` must agree.
2. Keep `PLAN.md` from acting as an active roadmap when it conflicts
   with diagnostic-only status.
3. Keep agent handoff files current so autonomous development does not
   restart closed work.
4. Fix the Windows CLI help path.
5. Apply ADR-75 during G-6d candidate selection: reject or defer
   `requirements/*.txt` / `tox.ini` test-dependency-only candidates
   before freeze.
6. Resume corpus pinning toward N>=20 without broad product claims.

## Non-Goals

The next work blocks must not optimize for:

- merge readiness or production readiness;
- stable PyPI release;
- GitHub App or SaaS;
- Checks API custom annotations;
- MCP runtime;
- provider tool-calling in the runtime path;
- default-enabled gateway execution;
- non-Python language ecosystems;
- public benchmark claims;
- autonomous repair planning;
- official OIDA fusion fields.

Official fields remain blocked or not emitted:

- `total_v_net`
- `debt_final`
- `corrupt_success`
- `corrupt_success_ratio`
- `verdict`

## Vocabulary

Use these terms for current product output:

- `supported_claim`: the named claim has relevant executable evidence.
- `contradicted_claim`: the named claim conflicts with tool output.
- `unsupported_claim`: the run did not establish support or contradiction.
- `diagnostic_only`: the report helps a reviewer inspect evidence.

Avoid turning research concepts into product promises. In particular,
`verification_candidate` is an internal diagnostic status, not a user
promise that the code is correct.

## G-6d Position

G-6d remains scientifically important. The live corpus after ADR-73 is
still N=14 (10 train, 4 holdout), and the target remains N>=20.

However, G-6d is not the immediate product strategy. The next G-6d
block must wait until the repo records a pre-freeze policy for targets
that expose test dependencies through `requirements/*.txt` or `tox.ini`
rather than PEP 735 dependency groups.

ADR-75 records the current policy: reject or defer such candidates
before freeze for G-6d.4. Do not add requirements-file install support
as a post-freeze rescue for a failing case. Any future support for
requirements-file / tox-only dependency patterns requires a separate ADR
before candidate selection and a structural update to the predeclared
clone-helper flag tests.

## Authority

When documents conflict:

1. `docs/product_strategy.md` defines active product direction.
2. `docs/project_status.md` defines current verified repo state.
3. `AGENTS.md` defines autonomous-agent continuity rules.
4. `BACKLOG.md` records acknowledged gaps, not commitments.
5. `PLAN.md` is historical and aspirational unless explicitly refreshed
   by a new ADR.
