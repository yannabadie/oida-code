# Secrets — never commit keys
.env
.env.*
!.env.example

# Python bytecode
__pycache__/
*.py[cod]
*$py.class

# Virtual environments
.venv/
venv/
env/
ENV/

# Distribution / build
build/
dist/
wheels/
*.egg-info/
*.egg
pip-wheel-metadata/

# Type / test / coverage caches
.mypy_cache/
.pytest_cache/
.ruff_cache/
.tox/
.nox/
.coverage
.coverage.*
coverage.xml
htmlcov/

# Project-specific
.oida/
memory-bank/*.tmp

# Phase 4.8-C (QA/A25.md) — private_holdout_v1 cases MAY contain
# operator code that must NOT enter the public repo. The schema
# (README + manifest.example.json) IS committed; the actual case
# directories are operator-private and must stay local.
datasets/private_holdout_v1/cases/

# IDE / editor
.vscode/
.idea/
*.swp
*.swo
*~

# OS
.DS_Store
Thumbs.db
