# QA/A47 — Phase 6.1' chain post-success priority decision (Phase 6.1' DECLARED COMPLETE)

**Date:** 2026-04-29.
**Cycle reviewed:** Phase 6.1' chain end-state at commit `e57d2cc`.
**6/6 CI green** on `e57d2cc`.
**cgpro session:** `phase61-review` (resumed fourth time;
conversation `69f1bde2-70c0-8387-9c89-743f8780cb14`).

## What was asked

Three questions about the chain's post-success priorities:

1. Which priority next — AI-tier rerun / seed_157 demote-replace
   / methodology consolidation / backlog pivot / other?
2. Is the 6.1' chain methodologically complete now?
3. Updated one-sentence cycle verdict (replaces QA/A46 verdict_q4).

## cgpro verdict (verbatim, JSON)

```json
{
  "verdict_q1": "pursue_a; run the AI-tier cold-reader rerun before seed replacement or docs consolidation because the chain now has the prerequisite partial holdout success, while the remaining negative is already documented as real holdout evidence plus seed-record authoring defect. A fresh adversarial panel is the best guard against self-congratulation before rewriting status docs or choosing any new holdout. The project status and backlog are visibly stale, but that argues for a docs follow-up after critique, not before it.",
  "verdict_q2": "chain_complete_pivot_now; the inside-chain blocker from QA/A46 is closed: ADR-61 added extras/groups plus pytest smoke, and ADR-62 records a fresh frozen holdout pass with seed_065 as the first holdout verification_candidate and seed_157 as an honest negative. Another inside-chain block would risk turning disciplined evaluation into success-chasing; seed_157 demotion/replacement should be a later labelled corpus-quality maintenance task, not Phase 6.1' completion criteria.",
  "verdict_q3": "Phase 6.1' is complete: it validated the discipline, fixed bootstrap blockers, produced one train and one holdout verification_candidate, and preserved one honest negative.",
  "next_action": "Next commit at SHA e57d2cc: run scripts/run_ai_adversarial_review.py as a Phase 6.2/audit-style AI-tier cold-reader rerun over reports/calibration_seed/, ADR-54..62, bundle generation, clone helper, and freeze-rule reports; do not edit generator, verifier, clone helper, seed partitions, or holdout scopes in that commit. After the review is archived, do a separate methodology-consolidation commit updating docs/project_status.md, BACKLOG.md, and a close-out summary with the critique incorporated. Do not demote or replace seed_157 until after those two steps, and if it happens, label it corpus-quality maintenance rather than rewriting the Phase 6.1' result."
}
```

(`--web` mode citation tokens stripped from each verdict.)

## Verdict_q3 — the post-Phase-6.1' cycle one-liner

> "Phase 6.1' is complete: it validated the discipline, fixed
> bootstrap blockers, produced one train and one holdout
> verification_candidate, and preserved one honest negative."

This sentence supersedes QA/A46 verdict_q4 ("...must not claim
holdout generalisation until the test-extras bootstrap gap is
fixed and a fresh frozen holdout pass succeeds.") and is the
public reading of the chain's net standing as of `e57d2cc`.

## Updated cycle status

* **Phase 6.1' status:** COMPLETE.
* **Empirical signal:** 2 verification_candidate (seed_008
  train, seed_065 holdout) + 1 honest negative (seed_157
  holdout — over-broad operator-authored test_scope).
* **Discipline:** validated end-to-end (corpus discipline +
  partition + freeze rule + holdout discipline + manual-lane
  separation).
* **Bootstrap:** fixed at both layers (install order in 6.1'f,
  test-extras + groups + pytest smoke in 6.1'g).
* **What 6.1' did NOT prove:** broad (2/2) holdout
  generalisation — the empirical sample is N=2 and the negative
  is a documented seed authoring defect, not a tooling failure.

## What this verdict directs

### Phase 6.2 — AI-tier cold-reader rerun (next commit)

Per cgpro next_action, exactly:

* Run `scripts/run_ai_adversarial_review.py` as a Phase
  6.2/audit-style AI-tier rerun.
* Review surface: `reports/calibration_seed/`, ADRs 54-62,
  bundle generator (`src/oida_code/bundle/`), clone helper
  (`scripts/clone_target_at_sha.py`), freeze-rule reports
  (Phase 6.1'd-h).
* **Do NOT edit:** generator, verifier, clone helper, seed
  partitions, holdout scopes. The script's input list (the
  `_DOC_PATHS_TO_REVIEW` array or equivalent) IS allowed to be
  updated — it's how operator runs the review tool, not a
  tooling edit per cgpro's carve-out.
* Archive output under `reports/ai_adversarial/<date>/`
  matching the existing keystone (`critique_*.md` per provider
  + `aggregate.md`).

### Methodology consolidation (separate commit, AFTER Phase 6.2)

* Update `docs/project_status.md` (currently dated 2026-04-28;
  predates the entire 6.1' chain).
* Refresh `BACKLOG.md` if needed.
* Close-out summary that INCORPORATES the AI-tier critique
  from Phase 6.2.
* No new code, no new tests.

### What is explicitly DEFERRED

* **seed_157 demotion + replacement.** Per cgpro: "Do not
  demote or replace seed_157 until after those two steps, and
  if it happens, label it corpus-quality maintenance rather
  than rewriting the Phase 6.1' result." The honest negative
  stays in the record.
* Multi-provider replay panel for round-trip authoring.
* Public benchmark exploration / G-3 pivot.
* MCP runtime introduction (project-rule 2 keeps it off the
  critical path indefinitely).

## Cross-references

* QA/A45 / QA/A45_followup / QA/A45_step4_outcome / QA/A46:
  prior cgpro cycles.
* ADR-58 / ADR-59 / ADR-60 / ADR-61 / ADR-62: Phase 6.1'
  e1-3 / e4 / f / g / h.
* Phase reports: `reports/phase6_1_a_first_collection.md` →
  `reports/phase6_1_h_freeze_pass.md`.
* AI-tier review script: `scripts/run_ai_adversarial_review.py`.
* AI-tier review prior outputs: `reports/ai_adversarial/`.
