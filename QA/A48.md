# QA/A48 — post-corpus-quality-v1 + G-6b state of play, what's next?

**Date:** 2026-04-29.
**Cycle reviewed:** Phase 6.1' chain end-state at commit
`97fe278` (corpus-quality v1 ADR-65 + G-6b structural pin
ADR-66 both shipped, 6/6 CI green).
**cgpro session:** `phase61-review` (resumed fifth time;
conversation `69f1bde2-70c0-8387-9c89-743f8780cb14`).

## What was asked

Three questions about the chain's net state after the
operator's "a + b" 2-commit plan was fully delivered:

1. Highest-value next priority (a-g options surfaced).
2. Is this a natural pause point or is one more block needed?
3. Updated one-sentence cycle verdict.

## cgpro verdict (verbatim, JSON)

```json
{
  "verdict_q1": "pursue_f; the highest-value next move is methodology consolidation v2 because the canonical docs/backlog lag the actual ADR-65/66 state: ADR-65 records seed_157 demotion, seed_018 as the new narrow-scope holdout, and a second holdout verification_candidate, while ADR-66 records the predeclared-bootstrap structural pin; BACKLOG/project_status still present G-6b/f-style work as future or deferred. Fix the public/canonical state before opening another empirical block.",
  "verdict_q2": "one_more_block_before_pause; not another verifier/corpus/provider block, but one docs-only close-out block should land before pausing so the repository's canonical status matches the now-green state at 97fe278. After that, this is a natural pause point: G-6a and G-6d remain real, but they are next-phase choices, not unfinished cleanup from the current 2-commit plan.",
  "verdict_q3": "Phase 6.1' plus corpus-quality v1 produced two holdout claim-supporting round-trips—one entangled, one independent—and bounded the bootstrap carve-out; replay-content audit and larger-N validation remain open.",
  "next_action": "Immediate next commit at SHA 97fe278: docs-only methodology consolidation v2 updating docs/project_status.md, BACKLOG.md, memory-bank/progress.md, and a short close-out note/report to mark G-6b and G-6f closed, G-6c/G-6e partial, and G-6a/G-6d open. Do not touch src/, scripts/, seed partitions, replay prompts, or verifier/generator logic in that commit; no new provider calls are needed. After that pause, the next empirical priority should be G-6a replay-content audit before corpus expansion, because replay validity is more load-bearing than N growth."
}
```

(`--web` mode citation tokens stripped from each verdict.)

## Verdict_q3 — the updated cycle one-liner

> "Phase 6.1' plus corpus-quality v1 produced two holdout
> claim-supporting round-trips—one entangled, one
> independent—and bounded the bootstrap carve-out;
> replay-content audit and larger-N validation remain open."

This sentence supersedes QA/A47 verdict_q3 in the canonical
reading. It makes the entanglement-vs-independence distinction
explicit (per audit G-6e) and names the two open priorities
(G-6a replay-content audit, G-6d statistical thinness).

## What this verdict directs

### Phase methodology consolidation v2 (docs-only, next commit)

Per cgpro next_action — strict scope:

1. Update `docs/project_status.md`:
   * §4 (Phase 6.1' chain — what shipped + audit) — append the
     corpus-quality v1 + G-6b structural pin outcomes.
   * §8 (Phase 6.2 audit-informed caveats) — update bullet for
     freeze-rule carve-out (now operationally bounded), seed_157
     reclassification (now demoted-and-replaced), and the
     entanglement distinction.
   * §5 (current roadmap) — Corpus-quality v1 + G-6b structural
     pin both marked CLOSED; G-6a (replay-content audit) named as
     next empirical priority before corpus expansion (G-6d).
2. Update `BACKLOG.md`:
   * G-6b status → CLOSED by ADR-66.
   * G-6f status → CLOSED by ADR-65.
   * G-6c status → PARTIAL (seed_018 demonstrates audit-informed
     authoring).
   * G-6e status → PARTIAL (seed_018 success causally
     independent; seed_065 entangled).
   * G-6a, G-6d remain OPEN.
3. Append a one-line update to `memory-bank/progress.md`
   summarising the consolidation timestamp.
4. Write a short close-out report:
   `reports/phase6_1_close_out_v2.md` — appends the
   audit-finding-by-finding state to the v1 close-out.
5. ADR-67 documenting the consolidation v2 decisions.

### What is explicitly OFF the table in this commit

* `src/oida_code/` — no runtime change.
* `scripts/` — no manual-lane edits.
* `reports/calibration_seed/index.json` — no seed partition or
  Tier-3 changes.
* Any provider call.
* Any new generator / verifier / replay-prompt edit.

### After consolidation v2: NATURAL PAUSE POINT

Per cgpro verdict_q2: this is the natural pause point. G-6a
and G-6d remain open but are next-phase choices, not unfinished
cleanup. The empirical priority for any future work is G-6a
(replay-content audit) BEFORE corpus expansion (G-6d), because
"replay validity is more load-bearing than N growth".

## Cross-references

* QA/A47 (prior cgpro verdict): `QA/A47.md`
* QA/A45 / QA/A45_followup / QA/A45_step4_outcome / QA/A46:
  prior cycles.
* ADR-58 / ADR-59 / ADR-60 / ADR-61 / ADR-62 (Phase 6.1'
  chain): `memory-bank/decisionLog.md`
* ADR-63 (Phase 6.2 audit): `memory-bank/decisionLog.md`
* ADR-64 (consolidation v1): `memory-bank/decisionLog.md`
* ADR-65 (corpus-quality v1): `memory-bank/decisionLog.md`
* ADR-66 (G-6b structural pin): `memory-bank/decisionLog.md`
* AI-tier audit aggregate:
  `reports/ai_adversarial/phase6_2_chain_review/aggregate.md`
* Current canonical project status: `docs/project_status.md`.
* BACKLOG: `BACKLOG.md`.
