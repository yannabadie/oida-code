Oui : D0.1 validé. Go Block D proper.

Le commit ac7685e corrige bien le bug identifié : le graphe scanne maintenant la surface d’impact et non plus seulement le diff brut, score-trace expose bien --surface impact|changed, et le cas inverse changed db → importer app → edge db→app est couvert comme test central. Le commit documente aussi les gates ruff, mypy et 142/142 tests verts.

Le code CLI confirme le point important : normalize dérive la surface, extrait les obligations depuis cette surface, puis crée une copie interne du request avec scope.changed_files = surface avant d’appeler obligations_to_scenario. Le request public reste donc fidèle au diff brut, mais le mapper et le graphe travaillent sur la vraie surface d’audit.

Tu peux lui envoyer ceci :

Validation D0.1

D0.1 validé.

Le point critique est maintenant réglé :

raw diff:
  AuditRequest.scope.changed_files

audit surface:
  changed_files ∪ bounded impact cone

mapper / graph / score-trace:
  use audit surface

public request:
  preserves raw diff

Le reverse-case crown jewel est accepté :

changed_files = ["src/db.py"]
src/app.py imports src.db

--surface impact:
  obligations: db + app
  graph: db -> app supportive direct_import

--surface changed:
  obligations: db only
  graph: no db -> app edge

Le fix sur les deux spellings d’import est aussi validé. build_impact_cone conserve maintenant à la fois le module complet et l’alias sans src., ce qui couvre mieux les formes from src import db, from src.db import x et les imports sans préfixe src.

Go Block D proper

Tu peux lancer Block D proper, avec cet objectif :

Block D validates structural measurement.
Block D does not validate statistical outcome prediction.

Le bloc se découpe ainsi :

D1 — full paper sanity
D2 — 8–10 hermetic code-domain traces
D3 — real-repo structural smoke

Pas de LLM.
Pas de PyPI.
Pas de corrélation commits > 0.
Pas de claim graph-aware V_net.
Pas de modification du vendored OIDA core.

Le papier Explore/Exploit mesure exploration/exploitation à partir des seules actions observées, sans accès à la policy interne, sur un environnement 2D partiellement observable + task DAG. Notre Block D doit donc prouver deux choses séparées : d’abord que la métrique reste fidèle au papier, ensuite que notre mapping code-domain est structurellement sain.

D1 — full paper sanity

Le 7/7 stale actuel est bien, mais insuffisant pour dire “paper sanity complet”.

Je veux un rapport D1 qui couvre explicitement :

1. Case 1 / Case 2 / Case 3 / Case 4 / terminal
2. progress reset
3. paper_gain vs progress_event
4. no-progress segment boundaries
5. c_t cyclomatic component
6. e_t edge-reuse component
7. n_t node-reuse component
8. S_t = c_t + e_t + n_t
9. undirected edge budget = 2
10. exploration_error / exploitation_error normalization

Livrable :

reports/paper_sanity_report.md

Le rapport doit inclure :

- repo jjj-madison commit SHA utilisé
- tests reproduits
- expected vs ours
- mismatches éventuels
- conclusion limitée:
  "validates paper math, not code-domain mapping"

Le repo des auteurs est publié sous licence MIT et fournit justement le code associé au papier, donc c’est la bonne source pour ce sanity check.

Si D1 révèle un mismatch mathématique non expliqué : stop immédiat. Ne pas continuer D2/D3 sur une métrique divergente.

D2 — hermetic code-domain sanity

Créer 8 à 10 traces hermétiques, chacune avec mini-repo Python, request, trace et expected outcome.

Format recommandé :

tests/fixtures/code_traces/<name>/
  repo/
  request.json
  trace.jsonl
  expected.json
  README.md

Chaque expected.json doit contenir au minimum :

{
  "expected_surface_mode": "impact",
  "expected_audit_surface": [],
  "expected_obligation_count_min": 0,
  "expected_edges": [],
  "expected_dominant_label": "",
  "expected_progress_min": 0,
  "expected_repair": {
    "reopen": [],
    "audit": []
  }
}

Scénarios minimum :

Fixture	Ce qu’elle doit prouver
clean_success	lecture utile, obligation close, erreurs basses
exploration_miss	surface critique non lue
exploitation_miss	bonne info lue mais non utilisée
stale_cycling	répétition structurelle sans progrès
blind_edit_no_observation	edit sans read ne consomme pas U(t)
repeated_edit_error	répétition d’edit pénalisée
import_dependency_missed	impact voit ce que changed rate
supportive_test_audit	supportive edge ⇒ audit, pas reopen
migration_without_rollback	grounding partiel, rollback non vérifié
corrupt_plausible_success	apparence verte mais préconditions critiques ouvertes

Pour chaque trace pertinente, comparer :

oida-code score-trace trace.jsonl --request request.json --surface changed
oida-code score-trace trace.jsonl --request request.json --surface impact

Le but est de prouver que impact améliore la surface sans créer du faux progrès.

Attention : corrupt_plausible_success ne doit pas encore émettre un vrai verdict global corrupt_success. Il doit seulement montrer :

high apparent completion
partial grounding
open critical preconditions
repair/audit path present
V_net/debt_final remain null

La Phase 3 avait déjà montré que le signal réel précédent était confondu par la longueur de session et par le proxy commits > 0; il ne faut pas répéter cette erreur dans D2.

D3 — real-repo structural smoke

Faire un smoke sur au moins :

1. oida-code self
2. un petit repo Python externe simple
3. si possible, un repo Python avec imports/tests non triviaux

Ce n’est pas une validation statistique. Pas de Spearman, pas de corrélation, pas de “succès prédit”.

Mesures attendues :

- normalize ne crashe pas
- score-trace ne crashe pas
- impact_surface non vide
- impact_surface != changed_files sur au moins un cas
- obligations non vides
- graph non vide quand imports/tests existent
- no unknown parent ids
- no self edges
- supportive vs constitutive séparés
- double_loop_repair démontre reopen vs audit
- V_net/debt_final restent null

Le rapport doit documenter les limites du cône d’impact : il est borné, max_depth=1, max_files=50, il ignore stdlib/external/site-packages, et il ne prétend pas être un call graph complet. Cette limite est saine et cohérente avec ADR-21.

Critères d’acceptation Block D
1. D1 paper sanity complet écrit.
2. Aucun mismatch mathématique non expliqué.
3. 8–10 traces hermétiques code-domain.
4. Chaque trace a expected outcome explicite.
5. changed vs impact comparés sur au moins 4 traces.
6. import_dependency_missed prouve que impact > changed.
7. supportive_test_audit prouve audit != reopen.
8. migration_without_rollback garde grounding partiel.
9. real-repo smoke sur au moins 2 repos.
10. aucun unknown parent id.
11. aucun self edge.
12. V_net/debt_final restent null.
13. rapport dit explicitement:
    "Block D validates structural measurement, not statistical outcome prediction."
14. ruff clean.
15. mypy clean.
16. pytest full green.
Rapport attendu

Créer :

reports/block_d_validation.md

Structure :

1. Diff résumé
2. D1 paper sanity table
3. D2 hermetic traces table
4. D2 changed vs impact ablation
5. D3 real-repo smoke table
6. Repair propagation examples
7. Known limitations
8. Honesty statement
9. Gates

Honesty statement obligatoire :

Block D validates that the measurement pipeline is structurally grounded:
audit surface, obligations, graph edges, trajectory cases, and repair propagation.

It does not validate statistical prediction of real-world success.
It does not unlock graph-aware V_net.
It does not justify Phase 4 LLM verifier unless D1/D2/D3 all pass.
Après Block D

Si Block D passe, la prochaine étape n’est pas encore “LLM verifier complet”.

La suite logique sera :

E0 — fusion readiness review
E1 — decide whether graph-aware fusion is separate layer or vendored-core change
E2 — only then Phase 4 verifier forward/backward

Le LLM doit arriver après un signal mesurable, pas avant.