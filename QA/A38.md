workflow séparé operator-soak.yml, pas modification de action-gateway-smoke.yml.

action-gateway-smoke doit rester un test de régression stable. Le soak opérateur est une expérience humaine, paramétrable, avec case-id, bundle-dir, target-ref, artefacts, labels et UX scores. Mélanger les deux rendrait les runs moins lisibles.

GitHub supporte bien les inputs sur workflow_dispatch, mais le workflow doit être présent sur la branche par défaut pour être déclenchable ; ensuite il peut être lancé manuellement depuis l’UI, la CLI ou l’API, avec des inputs et même un --ref côté CLI. Donc la bonne architecture est : workflow sur main, target branch en input, et non un smoke workflow bricolé.

Voici le message que je lui enverrais tel quel.

Oui, prépare Phase 5.8-prep, mais pas Phase 5.8 complète.

Décision : crée un workflow séparé `operator-soak.yml`.

Ne modifie pas `action-gateway-smoke.yml`, sauf si tu dois ajouter un commentaire disant qu’il reste réservé au smoke de régression. `action-gateway-smoke` doit rester stable et non paramétrique. Le soak opérateur est une surface expérimentale distincte.

Raison :
- `action-gateway-smoke.yml` = smoke reproductible, fixture fixe, régression CI.
- `operator-soak.yml` = workflow manuel, inputs, cas humains, artefacts, labels, UX scores.
- Mélanger les deux rendrait les résultats difficiles à interpréter.

GitHub permet `workflow_dispatch` avec inputs, mais le workflow doit exister sur la branche par défaut pour être déclenchable. Le run peut ensuite être lancé via UI/CLI/API avec inputs et `--ref`. Donc :
- ajoute `operator-soak.yml` sur `main`;
- ajoute un input `target-ref`;
- le workflow checkout ce `target-ref`;
- le bundle est lu depuis ce checkout;
- permissions minimales : `contents: read`;
- aucun secret;
- aucun provider externe;
- aucun MCP;
- aucun provider tool-calling.
Ce que je t’autorise à faire maintenant
1. Créer `operator-soak.yml` séparé.
2. Générer un vrai audit packet uniquement pour case_001.
3. Scaffolder case_002 et case_003, mais sans prétendre qu’ils sont prêts.
4. Préparer les bundles et le RUNBOOK.
5. Créer les CGPRO_REQUEST nécessaires.
6. T’arrêter avant tout workflow_dispatch.
Ce que je ne t’autorise pas encore à faire
- ne déclenche pas `gh workflow run`;
- n’écris aucun label.json;
- n’écris aucun ux_score.json;
- ne marque aucun cas comme completed;
- ne simule aucun workflow_run_id;
- ne simule aucun artifact_url;
- ne force pas recommendation à document_opt_in_path;
- ne marque pas Phase 5.8 comme acceptée.
Travail précis attendu
1. operator-soak.yml

Crée :

.github/workflows/operator-soak.yml

Déclenchement :

on:
  workflow_dispatch:
    inputs:
      case-id:
        required: true
        type: string
      target-ref:
        required: true
        type: string
      bundle-dir:
        required: true
        type: string
      output-dir:
        required: false
        type: string
        default: ".oida/operator-soak"

Règles :

- permissions: contents: read
- pas de pull_request
- pas de pull_request_target
- pas de schedule
- pas de provider externe
- pas de secrets
- pas de MCP
- pas de provider tool-calling
- pas de write/network

Tous les inputs contrôlés par l’opérateur doivent être passés via env: puis utilisés comme $VAR dans Bash. Aucune expression ${{ inputs.* }} ne doit être interpolée directement dans un bloc run:. Les docs GitHub rappellent que certains contextes/actions peuvent contenir de l’input non fiable, donc on garde la règle Phase 4.5.1.

2. Case 001

Crée ou prépare :

operator-soak/case-001-docstring

Changement minimal :

- docstring ou wording uniquement;
- aucun changement fonctionnel lourd;
- aucun workflow;
- aucun provider;
- aucun MCP.

Génère un vrai audit packet pour case_001_oida_code_self, parce que c’est le seul cas actuellement faisable sans setup externe complexe.

Mets à jour :

operator_soak_cases/case_001_oida_code_self/fiche.json

avec :

{
  "case_id": "case_001_oida_code_self",
  "status": "awaiting_operator_dispatch",
  "repo": "yannabadie/oida-code",
  "branch": "operator-soak/case-001-docstring",
  "intent": "Controlled low-risk wording/docstring change for operator soak.",
  "expected_risk": "low",
  "gateway_bundle": "operator_soak_cases/case_001_oida_code_self/bundle",
  "workflow_run_id": null,
  "artifact_url": null,
  "operator_label": null,
  "ux_score": null
}
3. Case 002 et case 003

Ne génère pas encore les vrais packets complexes.

Crée les dossiers :

operator_soak_cases/case_002_python_semver/
operator_soak_cases/case_003_markupsafe/

Statut :

awaiting_real_audit_packet_decision

Ne laisse pas croire que les bundles seedés sont des bundles réels.

Dans chaque README, écris clairement :

This case is scaffolded only.
The committed bundle is not yet a real audit packet.
Human/cgpro decision required:
- generate real audit packet,
- replace this case,
- or mark insufficient_fixture after operator review.
4. CGPRO_REQUEST à créer maintenant

Crée une demande cgpro pour l’approbation du dispatch de case_001, mais ne dispatch pas encore.

CGPRO_REQUEST
- title: Approve workflow_dispatch for operator soak case_001
- phase: 5.8-prep
- case_id: case_001_oida_code_self
- task_type: workflow_dispatch_approval
- why_human_required: Running workflow_dispatch creates a visible GitHub Actions run on Yann's repository and executes the opt-in gateway path. The project policy requires human approval for shared-state actions.
- inputs_to_review:
  - operator_soak_cases/case_001_oida_code_self/README.md
  - operator_soak_cases/case_001_oida_code_self/fiche.json
  - operator_soak_cases/case_001_oida_code_self/bundle/
  - .github/workflows/operator-soak.yml
- exact_decision_needed: Approve or reject dispatching operator-soak case_001 with target-ref operator-soak/case-001-docstring.
- allowed_answers:
  - approve_dispatch
  - reject_dispatch
  - request_changes
- files_to_update_after_response:
  - operator_soak_cases/case_001_oida_code_self/fiche.json
- blocking: true
- deadline_or_priority: high
END_CGPRO_REQUEST

Si cgpro répond approve_dispatch, tu ne dois pas lancer le run automatiquement. Tu dois d’abord me rapporter :

cgpro approved dispatch for case_001.
Ready command:
gh workflow run operator-soak.yml --ref main \
  -f case-id=case_001_oida_code_self \
  -f target-ref=operator-soak/case-001-docstring \
  -f bundle-dir=operator_soak_cases/case_001_oida_code_self/bundle \
  -f output-dir=.oida/operator-soak/case_001_oida_code_self

Ensuite seulement, si je réponds explicitement :

go dispatch case_001

tu peux exécuter la commande.

Pourquoi cette double garde :

cgpro donne l’approbation humaine;
Yann garde la décision finale sur l’action shared-state;
Claude exécute seulement une commande explicitement approuvée.
5. Runbook

Crée :

operator_soak_cases/RUNBOOK.md

Il doit expliquer :

1. Comment choisir un cas.
2. Comment vérifier la branche.
3. Comment lancer workflow_dispatch.
4. Où lire les artefacts.
5. Comment écrire label.json.
6. Comment écrire ux_score.json.
7. Comment relancer scripts/run_operator_soak_eval.py.

Ajoute les templates.

label.json :

{
  "operator_label": "useful_true_positive",
  "operator_rationale": [
    "Line 1: what the gateway surfaced.",
    "Line 2: why it was or was not useful.",
    "Line 3: what action the operator would take."
  ]
}

ux_score.json :

{
  "summary_readability": 0,
  "evidence_traceability": 0,
  "actionability": 0,
  "no_false_verdict": 0,
  "notes": ""
}
6. Après préparation

Arrête-toi avec ce statut :

Phase 5.8-prep complete.

Prepared:
- operator-soak.yml
- case_001 branch
- case_001 real audit packet
- case_001 cgpro dispatch request
- case_002 scaffold
- case_003 scaffold
- RUNBOOK.md

Not done by design:
- no workflow_dispatch run
- no workflow_run_id
- no artifact_url
- no label.json
- no ux_score.json
- no completed cases
- recommendation remains continue_soak
Points de sécurité à maintenir
enable-tool-gateway reste false par défaut;
operator-soak reste workflow_dispatch only;
permissions: contents: read;
pas de pull_request_target;
pas de fork PR gateway;
pas de provider externe;
pas de secret;
pas de MCP;
pas de provider tool-calling;
pas de write/network tools;
pas de total_v_net / debt_final / corrupt_success;
pas de merge-safe / production-safe / bug-free.

GitHub recommande de limiter les permissions du GITHUB_TOKEN au minimum, avec accès read-only quand c’est suffisant, et de ne pas compter uniquement sur la redaction automatique des secrets.

Décision sur case_002 / case_003

Ne demande pas encore à cgpro de trancher seeded bundle vs real audit packet pour case_002/case_003.

On commence par un seul vrai cas :

case_001 -> vrai packet -> vrai workflow -> vrais artefacts -> cgpro label/UX

Après ça seulement, on décidera si case_002/case_003 valent l’effort de génération real audit packet.

Motif : si on dispatch aujourd’hui avec des bundles seedés, les labels humains seront probablement insufficient_fixture, ce qui est honnête mais peu informatif. On veut d’abord prouver un cycle opérateur utile.