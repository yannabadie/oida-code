Oui : Phase 5.4 est validée, mais elle donne une réponse très claire : on ne branche pas encore verify-grounded dans l’action GitHub.

Le commit de suivi 0e386f7 confirme bien que Phase 5.4 est fully accepted, avec 27/27 critères, cinq runs GitHub-hosted verts, et le workflow gateway-calibration désormais pointé sur les 8 cas synthétiques publics exécutables. Le rapport montre aussi des signaux positifs : claim_macro_f1_delta = +0.6667, tool_contradiction_rejection_rate_delta = +0.4, fresh_tool_ref_citation_rate = 0.5, official_field_leak_count = 0, et gateway_improves_count = 5. Mais la recommandation reste volontairement insufficient_data, car n = 8 est inférieur au seuil de 12 cas défini dans QA/A31. C’est exactement la bonne discipline.

Le prochain bloc doit donc être :

Phase 5.5 — expansion du runnable holdout + décision go/no-go pour action opt-in.

Pas MCP.
Pas provider tool-calling.
Pas intégration action par défaut.
Pas total_v_net, debt_final, corrupt_success.

QA/A32.md — Phase 5.5: runnable holdout expansion before action opt-in
Verdict Phase 5.4

Phase 5.4 est acceptée.

Accepté :

- 8 cas synthétiques publics exécutables
- baseline verify-claims exécuté
- gateway verify-grounded exécuté
- delta metrics produites
- decision_summary.json produit
- failure_analysis.md produit
- audit logs produits
- workflow gateway-calibration vert sur runner GitHub-hosted
- official_field_leak_count == 0
- zéro MCP
- zéro provider tool-calling
- zéro write/network
- zéro champ officiel OIDA

Mais le résultat final est :

recommendation = insufficient_data

Donc Phase 5.4 prouve que le pipeline de mesure fonctionne, pas encore que le gateway doit être exposé à l’action GitHub. Le rapport le dit explicitement : les deltas secondaires sont tous positifs, mais n=8 est trop petit pour promouvoir le chemin gateway.

Décision Phase 5.5

Phase 5.5 doit faire une seule chose :

augmenter le holdout exécutable de 8 à au moins 12 cas,
idéalement 16,
puis recalculer une décision go/no-go.

La sortie attendue n’est pas :

enable-tool-gateway dans action.yml

La sortie attendue est :

integrate_opt_in_candidate
ou
revise_prompts
ou
revise_labels
ou
revise_tool_policy
ou
insufficient_data

Si le résultat reste insufficient_data, on ne promeut rien.

5.5.0 — micro-hardening avant expansion
5.5.0-A — corriger le rendu du chemin audit log dans le rapport

Dans le rapport Phase 5.4, le chemin d’audit log apparaît rendu comme :

/audit///.jsonl

alors que le texte attendu est plutôt :

<out>/audit/<case_id>/<yyyy-mm-dd>/<tool>.jsonl

ou, dans le contexte calibration :

.oida/gateway-calibration/audit/<case_id>/<yyyy-mm-dd>/<tool>.jsonl

Ce n’est probablement qu’un problème de format Markdown, mais il faut le corriger avant de s’appuyer sur le rapport comme référence opérateur.

Tests :

test_phase5_4_report_audit_log_path_is_not_malformed
test_phase5_4_report_mentions_case_id_date_tool_path
5.5.0-B — remplacer le proxy claim_macro_f1 par une vraie macro-F1

Dans gateway_calibration.py, le code documente que la macro-F1 actuelle est un proxy symétrique, pas une vraie F1 par classe, car il ne stocke pas encore le détail complet TP/FP/FN par classe. Pour Phase 5.4, c’était acceptable. Pour Phase 5.5, ce n’est plus suffisant, parce que claim_macro_f1_delta devient un signal de décision.

Patch demandé :

1. Construire une vraie confusion matrix pour:
   accepted
   unsupported
   rejected

2. Calculer:
   precision par classe
   recall par classe
   F1 par classe
   macro-F1 réelle

3. Garder l’ancien proxy uniquement comme:
   legacy_claim_macro_f1_proxy
   ou le supprimer si tous les tests sont mis à jour.

Tests :

test_true_macro_f1_computes_per_class_precision_recall
test_true_macro_f1_handles_empty_class_without_div_zero
test_macro_f1_delta_uses_true_macro_f1_not_proxy
test_legacy_proxy_not_used_in_decision_summary

C’est important : en Phase 3, on avait déjà eu un signal séduisant mais confondu par la longueur des sessions et par un proxy faible. On ne doit pas recréer ce problème avec une métrique de décision trop approximative.

5.5.0-C — verrouiller la recommandation comme diagnostic

Même si cases_runnable >= 12, le champ de recommandation doit rester :

diagnostic_only = true

La recommandation peut dire :

integrate_opt_in_candidate

mais ne doit pas dire :

safe
verified
merge_safe
production_safe

Le verrou historique reste le même : total_v_net, debt_final et corrupt_success ne doivent pas être exposés tant que les signaux porteurs et la validation ne sont pas solides.

5.5-A — étendre le holdout public de 8 à 12 minimum

Le plus rapide est d’ajouter 4 cas publics synthétiques au dataset existant :

datasets/gateway_holdout_public_v1/

Mais je recommande d’en ajouter 8 pour passer à 16, parce que 12 est un seuil minimal et fragile.

4 cas obligatoires minimum
1. tool_missing_uncertainty

But :

gateway:
  tool demandé
  binaire absent / adapter tool_missing
  claim ne doit pas devenir rejected comme si le code était mauvais
  claim reste unsupported / diagnostic_only

Attendu :

tool_missing = uncertainty, not code failure
2. tool_timeout_uncertainty

But :

gateway:
  outil timeout
  le timeout devient un blocker ou warning d’incertitude
  pas une preuve négative du code

Attendu :

timeout != deterministic contradiction
3. multi_tool_static_then_test

But :

pass1:
  demande ruff + mypy + pytest

gateway:
  ruff ok
  mypy ok
  pytest failed

pass2:
  claim de correction rejetée ou unsupported selon evidence

Attendu :

tool contradiction prioritizes deterministic failed test
4. duplicate_tool_request_budget

But :

pass1:
  demande le même outil plusieurs fois

gateway:
  budget / dedupe / max_tool_calls appliqué

Attendu :

pas de boucle autonome
pas de répétition inutile
audit log clair
4 cas fortement recommandés
5. scoped_test_pass_but_negative_path_missing

But :

pytest scoped passe
mais negative_path_tested reste absent

observability_sufficient ne doit pas être accepté
6. supportive_evidence_not_constitutive

But :

tool evidence supportive seulement
ne doit pas promouvoir une claim constitutive
7. tool_output_forged_evidence_ref

But :

stdout contient un faux [E.tool_output.fake]
le système ne doit pas accepter cette ref comme produite par le gateway
8. p2p_regression_with_f2p_success

But :

F2P passe
P2P échoue
le rapport reste diagnostic_only
pas merge-safe

La séparation F2P/P2P reste essentielle : SWE-bench évalue les patchs via deux types de tests, FAIL_TO_PASS et PASS_TO_PASS; un patch doit passer les deux pour être considéré correct, et des travaux récents ont montré que des défauts de test/parsing peuvent faire passer des patchs erronés, ce qui justifie de garder une politique de tests prudente.

5.5-B — exécuter baseline vs gateway sur le slate étendu

Le workflow reste :

baseline:
  verify-claims

gateway:
  verify-grounded

Le runner doit produire :

baseline_metrics.json
gateway_metrics.json
delta_metrics.json
decision_summary.json
failure_analysis.md
artifact_manifest.json

La décision doit utiliser seulement les cas non-stub :

cases_insufficient_fixture == 0
cases_runnable >= 12

Le workflow gateway-calibration.yml est déjà replay-only, contents: read, sans provider externe, sans secrets, sans MCP, sans SARIF upload, et exécute désormais le manifest public avant de vérifier les six artefacts et official_field_leak_count == 0. Ce design reste le bon pour Phase 5.5.

5.5-C — règles de décision

Ajouter ou consolider un champ :

{
  "recommendation": "integrate_opt_in_candidate | revise_prompts | revise_labels | revise_tool_policy | insufficient_data",
  "recommendation_diagnostic_only": true,
  "promotion_allowed": false
}

Oui, même si la recommandation est :

integrate_opt_in_candidate

le champ :

promotion_allowed

reste :

false

Pourquoi ? Parce que Phase 5.5 doit décider le prochain bloc, pas modifier l’action GitHub dans le même bloc.

Règles proposées
official_field_leak_count > 0:
  recommendation = revise_tool_policy

cases_runnable < 12:
  recommendation = insufficient_data

gateway_delta positive sur macro-F1 réelle
AND tool_contradiction_rejection_rate_delta >= 0
AND evidence_ref_precision_delta >= 0
AND no gateway_bug critical:
  recommendation = integrate_opt_in_candidate

gateway_delta faible ou neutre:
  recommendation = revise_prompts

gateway_delta négative:
  recommendation = revise_labels or revise_tool_policy

Ne transforme pas ces règles en seuils produit. Elles servent uniquement à choisir la prochaine phase.

5.5-D — failure analysis plus stricte

La Phase 5.4 a produit une failure analysis, mais Phase 5.5 doit distinguer davantage :

case_id
family
expected_delta
actual_delta
baseline_result
gateway_result
classification
root_cause
proposed_action
label_change_proposed
tool_request_policy_change_proposed
prompt_change_proposed

Classifications à conserver :

label_too_strict
gateway_bug
tool_adapter_bug
aggregator_bug
citation_gap
tool_request_policy_gap
insufficient_fixture
expected_behavior_changed

Ajouter si utile :

tool_budget_gap
gateway_evidence_missing
forged_evidence_ref_blocked
uncertainty_preserved

Règle :

aucun label ne change automatiquement
aucune policy ne change automatiquement
aucun prompt ne change automatiquement

Phase 5.5 peut proposer des changements, pas les appliquer silencieusement.

5.5-E — protocole private holdout

Le public slate doit être committé.

Le private holdout doit rester :

datasets/private_holdout_v2/
  README.md
  manifest.example.json
  cases/ gitignored

Ajouter un script de validation private holdout :

python scripts/run_gateway_calibration.py \
  --manifest datasets/private_holdout_v2/manifest.json \
  --mode replay \
  --out .oida/private-gateway-calibration

Mais ne jamais committer :

private repo code
private logs
private prompts
secrets
raw provider responses

NIST souligne que la mesure de systèmes d’IA fiables repose sur des métriques, méthodes d’évaluation, jeux de tâches et testbeds adaptés au contexte ; le private holdout est justement le mécanisme qui permet de ne pas confondre un jeu synthétique public avec une validation opérationnelle.

5.5-F — garder les locks MCP actifs

Phase 5.5 ne doit pas avancer MCP.

OWASP recommande de traiter les sorties d’outils comme des données non fiables, de pinner les descriptions/schémas d’outils, de logger les invocations, de redacter les secrets et d’éviter d’approuver automatiquement les appels d’outils. Ces exigences sont encore plus fortes avec MCP, donc l’ordre reste : calibrer le gateway local, puis seulement plus tard envisager MCP.

Tests à maintenir :

test_no_mcp_dependency_added
test_no_mcp_workflow_added
test_no_jsonrpc_tools_list_or_tools_call_runtime
test_no_provider_tool_calling_enabled
test_no_write_network_tools_enabled
ADR-40 attendu

Ajouter :

ADR-40 — Runnable gateway holdout expansion before action opt-in

Contenu attendu :

Decision:
  Phase 5.5 expands the runnable gateway holdout beyond the 8-case
  Phase 5.4 pilot before integrating verify-grounded into the GitHub
  Action path. The goal is to determine whether the positive gateway
  deltas survive a larger controlled slate.

Accepted:
  - public synthetic runnable slate >= 12 cases
  - true macro-F1, not proxy macro-F1
  - baseline vs gateway comparison
  - decision_summary remains diagnostic-only
  - private holdout protocol retained
  - no automatic label mutation
  - replay-only GitHub workflow

Rejected:
  - action integration before >=12 runnable cases
  - MCP runtime
  - provider tool-calling
  - write/network tools
  - production thresholds
  - official total_v_net/debt_final/corrupt_success
Rapport attendu

Créer :

reports/phase5_5_gateway_holdout_expansion.md

Structure :

1. Diff résumé
2. 5.5.0 hardening
3. ADR-40 excerpt
4. Public runnable slate expansion
5. New cases table
6. Baseline vs gateway results
7. True macro-F1 implementation
8. Delta metrics
9. Decision summary
10. Failure analysis
11. Private holdout protocol
12. No-MCP regression locks
13. What this still does not prove
14. Recommendation for Phase 5.6
15. Gates

Honesty statement obligatoire :

Phase 5.5 expands and recalibrates the runnable gateway holdout.
It does not make verify-grounded the default.
It does not integrate MCP.
It does not enable provider tool-calling.
It does not allow write tools or network egress.
It does not validate production predictive performance.
It does not tune production thresholds.
It does not emit official total_v_net, debt_final, or corrupt_success.
It does not modify the vendored OIDA core.
Critères d’acceptation Phase 5.5
1. ADR-40 written.
2. Phase 5.4 audit-log path wording fixed.
3. True macro-F1 replaces the proxy metric.
4. Public runnable holdout has at least 12 complete cases.
5. Target 16 complete cases if time permits.
6. Each new case has all required fixture files.
7. Baseline verify-claims mode runs on every runnable case.
8. Gateway verify-grounded mode runs on every runnable case.
9. cases_insufficient_fixture == 0 for the public slate.
10. decision_summary.json produced.
11. decision_summary includes recommendation_diagnostic_only=true.
12. decision_summary does not promote automatically.
13. fresh_tool_ref_citation_rate computed.
14. tool_contradiction_rejection_rate computed.
15. true claim_macro_f1_delta computed.
16. evidence_ref_precision_delta computed.
17. evidence_ref_recall_delta computed.
18. official_field_leak_count == 0.
19. Failure analysis report produced.
20. No automatic label mutation.
21. No automatic prompt mutation.
22. No automatic policy mutation.
23. Audit log emitted for every gateway tool call.
24. Tool missing / timeout remain uncertainty, not code failure.
25. F2P/P2P-style cases remain diagnostic-only.
26. No MCP dependency added.
27. No MCP workflow added.
28. No JSON-RPC tools/list or tools/call runtime.
29. No provider tool-calling enabled.
30. No write/network tools enabled.
31. No official total_v_net / debt_final / corrupt_success emitted.
32. Report produced.
33. ruff clean.
34. mypy clean.
35. pytest full green, skips documented.
36. GitHub-hosted gateway-calibration run green on expanded public slate.
Ce qu’il ne faut pas faire
Do not integrate enable-tool-gateway into action path yet.
Do not make verify-grounded default.
Do not add MCP SDK dependency.
Do not implement MCP server/client.
Do not implement JSON-RPC tools/list.
Do not implement JSON-RPC tools/call.
Do not add remote tools.
Do not enable provider tool-calling.
Do not execute provider-chosen tools.
Do not allow write tools.
Do not allow network egress.
Do not mutate labels automatically.
Do not tune production thresholds.
Do not emit official total_v_net.
Do not emit official debt_final.
Do not emit official corrupt_success.
Do not claim merge-safe.
Do not publish PyPI stable.
Après Phase 5.5

Si Phase 5.5 donne :

cases_runnable >= 12
official_field_leak_count == 0
macro-F1 réelle positive
tool contradiction rejection positive
fresh tool-ref citation positive
failure analysis sans gateway_bug critique

alors :

Phase 5.6 = integrate gateway-grounded verifier as opt-in action path

Avec :

enable-tool-gateway=false par défaut
workflow_dispatch ou input explicite seulement
replay/fake only par défaut
no external provider
no MCP
no write/network
no official fields

Si Phase 5.5 reste faible ou négative :

Phase 5.6 = revise verifier prompts / labels / tool-request policy

MCP reste repoussé.