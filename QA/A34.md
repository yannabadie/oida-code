Oui : Phase 5.6 est validée.

Le commit e2327f0 documente bien les six runs GitHub-hosted verts sur 8fca666, dont le nouveau action-gateway-smoke, et confirme que le gateway est maintenant exposé dans l’action uniquement via enable-tool-gateway: "true" avec bundle explicite.
Le rapport Phase 5.6 confirme aussi les garde-fous essentiels : gateway désactivé par défaut, bundle validé, PR/fork guard, artefacts gateway, résumé diagnostic-only, aucun MCP, aucun provider tool-calling, aucun champ officiel total_v_net / debt_final / corrupt_success.

Sur la question de Claude :

“Want me to /schedule a background agent in 2 weeks…?”

Ma réponse serait : non, pas maintenant.

Pas parce que l’idée est mauvaise, mais parce que Phase 5.7 n’a pas encore commencé à produire de télémétrie. Programmer un agent dans deux semaines risque de créer un faux sentiment de suivi alors qu’il faut d’abord définir exactement quoi collecter, sur quels repos, avec quels labels opérateur, et quelles conditions d’arrêt. GitHub Actions permet bien les workflows planifiés via schedule, mais les workflows planifiés tournent sur la branche par défaut, peuvent être retardés ou supprimés en période de forte charge, et ne remplacent pas un protocole d’observation bien défini.

La bonne réponse courte à Claude :

Non, ne schedule pas encore un background agent. Phase 5.6 est acceptée. La prochaine étape est Phase 5.7 : operator soak sur 3–5 repos/PRs contrôlés. D’abord on définit le protocole, les labels opérateur, les artefacts à collecter et les critères FP/FN. Après les premiers runs réels, on pourra ajouter un rappel ou workflow planifié pour relire la télémétrie. Pas de background agent autonome pour l’instant.

QA/A34.md — Phase 5.7: operator soak on real repos
Verdict Phase 5.6

Phase 5.6 est acceptée.

Accepté :

- enable-tool-gateway implémenté dans action.yml
- enable-tool-gateway=false par défaut
- gateway-bundle-dir explicite
- bundle validation
- PR / pull_request_target guard
- fork PR guard
- no external provider
- no secrets
- no MCP
- no provider tool-calling
- no write/network tools
- gateway artifacts
- gateway audit logs
- diagnostic-only Step Summary
- action-gateway-smoke vert sur GitHub-hosted runner
- official total_v_net / debt_final / corrupt_success absents

L’action expose bien enable-tool-gateway, gateway-bundle-dir, gateway-output-dir, gateway-fail-on-contract, ainsi que les outputs gateway-report-json, gateway-summary-md, gateway-audit-log-dir, gateway-status et gateway-official-field-leak-count.
Le rapport Phase 5.6 rappelle explicitement que le gateway ne tourne pas sur pull_request / pull_request_target, car pytest peut exécuter du code du dépôt ; cette garde est cohérente avec la prudence GitHub sur les secrets, permissions minimales et usages risqués des workflows.

Décision Phase 5.7

Phase 5.7 doit être un soak opérateur contrôlé.

Objectif :

Exécuter l’action opt-in gateway sur 3 à 5 repos / PRs contrôlés,
collecter les artefacts,
faire labelliser les résultats par un opérateur,
mesurer FP/FN,
et décider si le chemin gateway est assez utile pour devenir une option documentée.

Non-objectifs :

- ne pas activer enable-tool-gateway par défaut
- ne pas exécuter sur PR fork
- ne pas utiliser pull_request_target
- ne pas appeler provider externe
- ne pas ajouter MCP
- ne pas activer provider tool-calling
- ne pas créer GitHub App
- ne pas utiliser Checks API custom
- ne pas émettre total_v_net officiel
- ne pas émettre debt_final officiel
- ne pas émettre corrupt_success officiel
- ne pas dire merge-safe / production-safe / bug-free

La raison reste la même depuis le début : la Phase 3 avait déjà montré qu’un signal apparemment propre pouvait être confondu par la longueur de session et des proxies faibles.
Le verrou null / blocked sur V_net, debt_final et corrupt_success reste donc non négociable tant que la validation empirique n’est pas solide.

5.7-A — sélectionner 3 à 5 repos / PRs contrôlés

Choisir des cas où tu peux interpréter le résultat sans ambiguïté.

Minimum
3 repos / PRs contrôlés
Cible
5 repos / PRs contrôlés
Types recommandés
1. oida-code self — PR contrôlée avec changement mineur
2. mini repo Python hermétique — bug simple + test
3. repo Python réel simple — changement avec import/test
4. repo avec migration/config — risque structurel
5. repo avec fail-to-pass / pass-to-pass explicite

Éviter pour cette phase :

- monorepo massif
- repo sans tests
- repo avec dépendances lourdes
- repo privé contenant secrets/logs sensibles
- PR fork
- PR non contrôlée
5.7-B — protocole opérateur

Pour chaque repo / PR, produire une fiche :

operator_soak_cases/<case_id>/README.md

Format :

case_id:
repo:
branch:
commit:
operator:
intent:
expected_risk:
gateway_bundle:
workflow_run_id:
artifact_url:
notes:

Chaque cas doit avoir un label opérateur après lecture des artefacts :

operator_label:
  useful_true_positive
  useful_true_negative
  false_positive
  false_negative
  unclear
  insufficient_fixture

Avec une justification obligatoire :

operator_rationale:
  texte court, 3–10 lignes

Règle :

Aucun label ne doit être généré par LLM.
Aucun label ne doit être déduit automatiquement du gateway-status.
5.7-C — exécution action

Pour chaque cas, utiliser l’action composite avec :

enable-tool-gateway: "true"
gateway-bundle-dir: "<bundle explicite>"
gateway-output-dir: ".oida/gateway-soak/<case_id>"
gateway-fail-on-contract: "false"
llm-provider: "replay"

Règles :

- workflow_dispatch ou push contrôlé uniquement
- permissions: contents: read
- no pull_request_target
- no external provider
- no secrets
- no MCP
- no provider tool-calling
- no write/network tools

Le gateway-fail-on-contract doit rester "false" pendant le soak, sauf pour tester volontairement une violation de contrat. Un soak opérateur doit mesurer l’utilité du rapport, pas casser le workflow à chaque diagnostic.

5.7-D — artefacts à collecter

Pour chaque run :

grounded_report.json
summary.md
action_outputs.txt
audit/
artifacts/manifest.json
GitHub Step Summary
workflow run URL

Stockage recommandé :

.oida/operator-soak/<case_id>/

Et côté repo :

reports/operator_soak/
  case_<id>.md
  aggregate.md

Interdits :

- raw prompt
- raw provider response
- secret
- token
- private log non redacted
5.7-E — métriques soak

Créer :

scripts/run_operator_soak_eval.py

Ou un rapport manuel si tu veux rester léger.

Métriques minimales :

cases_total
cases_completed
useful_true_positive_count
useful_true_negative_count
false_positive_count
false_negative_count
unclear_count
insufficient_fixture_count
contract_violation_count
official_field_leak_count
gateway_status_distribution
operator_usefulness_rate

Définitions :

operator_usefulness_rate =
  (useful_true_positive + useful_true_negative) / completed_cases

false_positive =
  gateway signale un problème important mais l’opérateur juge que ce n’est pas utile/actionnable

false_negative =
  opérateur trouve une faille importante que le gateway n’a pas surfacée

unclear =
  artefacts pas assez lisibles ou preuve insuffisante

insufficient_fixture =
  bundle / replay / policy pas adapté au cas
5.7-F — seuils de décision

Ne pas faire de seuil produit.

Utiliser seulement des règles de décision de phase :

Si cases_completed < 3:
  recommendation = continue_soak

Si official_field_leak_count > 0:
  recommendation = fix_contract_leak

Si false_negative_count >= 2:
  recommendation = revise_gateway_policy_or_prompts

Si false_positive_count >= 2:
  recommendation = revise_report_ux_or_labels

Si usefulness_rate >= 0.6 sur au moins 5 cas:
  recommendation = document_opt_in_path

Sinon:
  recommendation = continue_soak

Même si document_opt_in_path est atteint :

enable-tool-gateway reste false par défaut.
5.7-G — UX qualitative

L’opérateur doit répondre à quatre questions par cas :

1. Est-ce que le résumé GitHub suffit pour comprendre le résultat ?
2. Est-ce que summary.md explique clairement ce qui est prouvé / non prouvé ?
3. Est-ce que grounded_report.json contient assez d’éléments pour auditer ?
4. Est-ce que le rapport propose une action utile sans faux verdict produit ?

Échelle :

0 = non
1 = partiellement
2 = oui

Métriques :

summary_readability_avg
evidence_traceability_avg
actionability_avg
no_false_verdict_avg
5.7-H — fork PR smoke, enfin

Tu avais gardé ce point not_run plusieurs phases.

Phase 5.7 doit le traiter si possible :

Créer une PR depuis fork de test
enable-tool-gateway=true
vérifier que l’action bloque avant exécution
aucun secret consulté
aucun gateway artifact produit
message ::error:: clair

Si tu n’as pas de fork disponible :

status = not_run
reason = no controlled fork available

Ne pas faker ce test.

GitHub documente le besoin de limiter les permissions du GITHUB_TOKEN et recommande le read-only par défaut avec élévation seulement quand nécessaire ; le fork PR smoke doit préserver cette discipline.

5.7-I — pas de background agent autonome

Ne pas lancer d’agent qui “surveille” librement le repo.

Autorisé :

- issue GitHub "Phase 5.7 operator soak tracking"
- workflow_dispatch manuel
- scheduled reminder workflow read-only, si nécessaire
- tableau de bord Markdown mis à jour manuellement

Non autorisé :

- agent autonome qui relance des audits sans protocole
- provider externe automatique
- exécution sur PR fork
- accès secrets
- MCP
- outil write/network

Si tu veux un rappel dans deux semaines, fais-le comme issue / reminder, pas comme agent opératoire.

Exemple GitHub Actions possible plus tard :

on:
  schedule:
    - cron: "17 6 * * 1"

Mais seulement pour générer un rappel / vérifier qu’un rapport existe. GitHub indique que les schedules tournent sur la branche par défaut et peuvent être retardés ou supprimés en période de charge ; donc ce n’est pas un mécanisme fiable de pilotage expérimental.

ADR-42 attendu

Ajouter :

ADR-42 — Operator soak before wider gateway adoption

Contenu :

Decision:
  Phase 5.7 measures the opt-in gateway-grounded action path on
  controlled real repos / PRs before documenting wider use.

Accepted:
  - 3–5 controlled repos / PRs
  - human operator labels
  - FP/FN tracking
  - artifact usefulness scoring
  - fork PR block smoke if available
  - enable-tool-gateway remains default false
  - no autonomous background agent

Rejected:
  - scheduled autonomous audits
  - provider external by default
  - fork PR gateway execution
  - MCP runtime
  - provider tool-calling
  - merge-safe labels
  - official total_v_net / debt_final / corrupt_success
Rapport attendu

Créer :

reports/phase5_7_operator_soak.md

Structure :

1. Diff résumé
2. ADR-42 excerpt
3. Case selection
4. Run table
5. Artifact collection
6. Operator labels
7. FP/FN analysis
8. UX qualitative scoring
9. Fork PR smoke
10. What this still does not prove
11. Recommendation for Phase 5.8
12. Gates

Honesty statement obligatoire :

Phase 5.7 evaluates the opt-in gateway-grounded action path on controlled
operator-selected repos and PRs. It does not make the gateway default. It does
not run on fork PRs. It does not implement MCP. It does not enable provider
tool-calling. It does not validate production predictive performance. It does
not emit official total_v_net, debt_final, or corrupt_success. It does not
modify the vendored OIDA core.
Critères d’acceptation Phase 5.7
1. ADR-42 written.
2. At least 3 controlled cases selected.
3. Target 5 controlled cases if available.
4. Each case has intent / repo / commit / bundle documented.
5. Action runs with enable-tool-gateway=true on each completed case.
6. enable-tool-gateway remains default false.
7. No provider external in soak runs.
8. No MCP dependency added.
9. No MCP workflow added.
10. No provider tool-calling enabled.
11. No write/network tools enabled.
12. No pull_request_target.
13. Fork PR block smoke run or explicitly not_run with reason.
14. Artifact bundle collected for each completed case.
15. Human operator label recorded for each completed case.
16. FP/FN counts computed.
17. UX qualitative scores recorded.
18. official_field_leak_count == 0.
19. No raw prompt / raw response / secret in artifacts.
20. Report produced.
21. ruff clean.
22. mypy clean.
23. pytest full green, skips documented.
24. At least one GitHub-hosted action-gateway-smoke or soak workflow green after Phase 5.7 changes.
Ce qu’il ne faut pas faire
Do not schedule an autonomous background agent yet.
Do not make enable-tool-gateway default true.
Do not run gateway on fork PRs.
Do not use pull_request_target.
Do not call external provider in soak runs.
Do not add MCP SDK dependency.
Do not enable provider tool-calling.
Do not allow write tools.
Do not allow network egress.
Do not mutate labels automatically.
Do not tune production thresholds.
Do not emit official total_v_net.
Do not emit official debt_final.
Do not emit official corrupt_success.
Do not claim merge-safe.
Do not publish PyPI stable.
Après Phase 5.7

Si le soak est positif :

Phase 5.8 — document opt-in gateway usage + examples

Toujours :

enable-tool-gateway=false par défaut

Si le soak est mitigé :

Phase 5.8 — UX/report/prompt/tool-policy fixes

Si le soak révèle des faux négatifs sérieux :

Phase 5.8 — revise verifier policy before any broader adoption

MCP reste repoussé.