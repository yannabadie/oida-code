# QA/A52 - G-6d.1 corpus pinning protocol

**Date:** 2026-04-30.  
**Repo state before implementation:** commit `c7633bb`, after ADR-70
G-6d.0 planning.  
**cgpro session:** `phase6a-replay-audit`, conversation
`69f25185-5f94-8394-ad11-627a00d1741b`.

## Questions asked

Codex asked `cgpro` whether to continue with G-6d.1 pinning or add
another intermediate screening/reporting block first. After the
provenance issue surfaced, Codex asked a second question on the honest
`label_source` for autonomous Codex-authored pins. A final follow-up
asked how to keep ADR-70 guardrails after the live corpus advanced from
N=6 to N=10.

## cgpro verdicts (condensed)

```json
{
  "recommended_next": "Proceed to G-6d.1 actual pinning now.",
  "scope": {
    "new_pins": 4,
    "source_pool": "existing 40 unpinned records only",
    "fresh_harvest": false,
    "provider_calls": 0,
    "pat_github_required": false,
    "replay_outputs": 0
  },
  "required_order": [
    "metadata prescreen",
    "public base-to-head diff inspection",
    "ADR-70 authoring checklist",
    "freeze 3 train / 1 holdout before pytest",
    "post-freeze clone/scoped-pytest feasibility"
  ],
  "holdout_rule": "sort case_ids; lowest sha256('g6d1-holdout:' + case_id) is holdout",
  "acceptance": "resulting corpus must be 10 pinned, 7 train, 3 holdout, ratio 0.30"
}
```

```json
{
  "recommended_label_source": "ai_authored_public_diff_review",
  "schema_change_required": true,
  "human_review_required": true,
  "llm_assist_used": true,
  "do_not_use": [
    "yann_manual_review",
    "ai_candidate_human_confirmed",
    "deterministic_tool_output",
    "repository_metadata"
  ]
}
```

```json
{
  "adr70_after_g6d1": "historical planning snapshot, not live-current-count oracle",
  "update_adr70_tests": true,
  "update_script": true,
  "do_not_rewrite_plan_json_to_n10": true,
  "new_live_guard": "G-6d.1 tests assert N=10, 7 train, 3 holdout"
}
```

## Binding implementation scope

- Pin exactly four formerly unpinned cases:
  `seed_003_pytest_dev_pytest_14420`,
  `seed_064_simonw_sqlite_utils_683`,
  `seed_066_simonw_sqlite_utils_681`,
  `seed_155_hynek_structlog_763`.
- Freeze partitions at `2026-04-30T04:10:00Z`.
- Deterministic holdout: `seed_066_simonw_sqlite_utils_681`.
- Use `label_source=ai_authored_public_diff_review`,
  `human_review_required=true`, and `llm_assist_used=true`.
- Run post-freeze clone/scoped-pytest feasibility only after the
  partition freeze.
- Keep ADR-70 plan artifacts historical at N=6; move live N=10
  assertions to G-6d.1 tests.

## Explicit non-scope

Do not generate replay bundles, `round_trip_outputs`, verifier pass
JSON, grounded reports, provider-authored passes, runtime/provider
changes, MCP changes, gateway default changes, new clone-helper flags,
fresh GitHub harvesting, or product/generalisation claims.

G-6d remains open after this tranche; N=10 is progress toward the
N>=20 target, not closure.
