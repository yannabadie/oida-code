Merci, très bon travail. Je valide A2.2 terminal, A2.3 resource_id stale nodes, les tests de reachability, et l’alignement D1 sur les edges non orientés. Le choix d’edges non orientés est bien conforme au papier : leur métrique compte des arêtes non orientées avec budget de 2 traversées, ce que confirme aussi le repo des auteurs via _edge_key(a, b) = (min(a, b), max(a, b)). 
Mais je ne valide pas encore le passage direct à Block B. Il reste un micro-bloc A2.4 à faire avant.
Le problème : dans ton rapport tu dis avoir prouvé gain=True / progress=False, mais le test prouve en fait :
candidate_gain=Trueis_progress=False
Ce n’est pas pareil. Le code actuel dit encore :
compute_gain = entered U(t) OR closed obligationis_progress_event = compute_gain
et l’équation d’erreur utilise ce compute_gain étroit. Donc, au sens du papier, Gain et Progress restent encore confondus dans la branche qui calcule l’erreur. La PJ montre bien que candidate_gain existe maintenant, mais il reste diagnostique plutôt qu’opérationnel dans err(t). 
Je veux donc un patch A2.4 avant Block B :
A2.4 — reconcile paper Gain vs Progress1. rename current compute_gain() to compute_progress_gain()   or explicitly document it as progress_gain2. introduce compute_paper_gain()   paper_gain =     progress_event     OR first touch of a pending-obligation resource in the current no-progress segment     OR first relevant test run for a pending obligation     OR first direct-dependency inspection for a pending obligation3. err(t) must use paper_gain, not progress_gain4. progress_event still resets the no-progress segment5. candidate_gain may remain as a broader diagnostic if useful,   but it must not be confused with paper Gain6. add a test where:   paper_gain=True   progress_event=False   stale does not rise   => is_error=False7. add a repeated-action test where:   first relevant test/read = paper_gain=True   repeated identical test/read with no evidence delta = paper_gain=False   or stale rises   => eventually error
Le cœur attendu est :
progress = is_progress_event(state_before, action, state_after)paper_gain = compute_paper_gain(    state_before,    action,    state_after,    obligations,    no_progress_segment_state,)candidate_gain = compute_candidate_gain(...)if case == "terminal":    ...elif progress:    emit_progress(...)elif not paper_gain:    is_error = Trueelif target_set_size <= 1:    is_error = Falseelse:    is_error = stale > prev_stale
Je veux éviter une régression inverse : si candidate_gain=True pour “n’importe quel test_run pendant qu’une obligation est ouverte”, alors un agent peut relancer le même test 20 fois sans jamais être pénalisé, surtout quand |T| <= 1. Donc paper_gain doit être borné par segment : première inspection utile oui, répétition sans nouvelle preuve non.
Deuxième point : mets à jour le rapport D1 dans le repo. La PJ dit que D1 est passé de 4/7 à 7/7, mais le fichier public reports/paper_sanity_report.md sur main indique encore 4/7 et explique les mismatches directed vs undirected. Soit le rapport n’a pas été régénéré, soit il n’a pas été poussé. Il faut aligner le repo avec le statut annoncé. 
Après A2.4, tu peux lancer Block B.
Pour Block B, je veux le cadrer ainsi :
Block B — obligation -> preconditions multiplicity + weightsGoal:  stop treating Obligation == PreconditionSpec.Deliverables:  1. ADR-20: obligations are not isomorphic with preconditions  2. _preconditions_for() returns 1..N PreconditionSpec per obligation  3. total weight is conserved by default:       sum(child.weight) == obligation.weight  4. verification is per sub-precondition, not blindly inherited  5. tests prove grounding changes when only part of an obligation is verified
Règle importante : conserver le poids total. Si une obligation de poids 1.0 devient 4 préconditions, les 4 sous-poids doivent sommer à 1.0. Sinon on va artificiellement rendre les obligations complexes plus lourdes juste parce qu’on les a mieux décomposées.
Mapping minimal attendu :
precondition obligation:  - guard_exists  - guard_is_reachable  - negative_path_testedapi_contract obligation:  - route_or_function_declared  - input_shape_validated  - output_shape_or_status_validated  - error_path_defined  - regression_test_presentmigration obligation:  - schema_change_detected  - backward_compatibility_checked  - data_preservation_checked  - rollback_or_idempotency_checked  - migration_test_presentsecurity_rule obligation:  - rule_declared  - static_scan_clean  - taint_or_access_path_checkedobservability obligation:  - failure_mode_logged  - metric_or_trace_available  - alert_or_surface_defined
Pour les kinds encore stubs, ne pas inventer trop de granularité. Tu peux retourner une seule précondition unexpanded_<kind> avec verified=False et une note explicite.
Critères d’acceptation Block B :
- one obligation can produce multiple preconditions- child weights sum to parent weight- partial verification gives partial grounding- green pytest alone does not close every sub-precondition- existing round-trip tests still pass- no V_net/debt emitted if graph/fusion incomplete
Donc ordre exact :
1. A2.4 paper_gain/progress split2. regenerate D1 report so repo says 7/7 if true3. stop with short diff + tests4. then Block B mapper multiplicity5. Block C graph still waits until B is accepted

Mon verdict : A2 est très proche, mais ce détail candidate_gain ≠ paper Gain est trop important pour le laisser passer. S’il commence Block B maintenant, il risque de construire le mapper sur un scorer encore légèrement faux au cœur.
