Oui : Phase 4.7 est acceptée.

Le run provider-baseline #1 est bien vert sur GitHub-hosted runner, déclenché manuellement, avec l’artefact provider-baseline-deepseek, et le rapport Phase 4.7 confirme le point clé : DeepSeek V4 Pro a passé le contrat sur 4 appels réels avec official_field_leak_count = 0, zéro violation de schéma, zéro citation manquante, zéro phrase interdite et zéro timeout.

Le bon choix pour Phase 4.8 n’est pas MCP, ni une migration directe vers Pydantic-AI. Le bon choix est :

Phase 4.8 — provider regression deepening + private holdout protocol

L’écart DeepSeek vs replay est de la donnée, pas un verdict. Mais il faut maintenant comprendre pourquoi DeepSeek descend à estimator_status_accuracy = 0.25 et estimator_estimate_accuracy = 0.5 sur L001–L004 alors que le contrat technique est propre. Le rapport Phase 4.7 dit déjà que la réponse brute n’est pas stockée par design, donc Phase 4.8 doit introduire une capture redacted, opt-in, contrôlée pour diagnostiquer les erreurs sans casser la discipline secret / no raw prompt par défaut.

Je vois aussi un micro-correctif documentaire avant de partir : le README au commit final contient bien la section Phase 4.7, mais garde plus bas un paragraphe ancien disant encore que sarif-upload.yml utilise upload-sarif@v3 et que le provider baseline est not_run. Ce n’est pas bloquant fonctionnellement, mais il faut le nettoyer au début de 4.8 pour éviter une contradiction publique.

QA/A25.md — Phase 4.8: provider regression deepening + private holdout
Verdict Phase 4.7

Phase 4.7 est validée.

Accepté :

- SARIF upload migré vers github/codeql-action/upload-sarif@v4
- ingestion SARIF v4 confirmée sur runner réel
- provider-baseline.yml workflow_dispatch only
- replay baseline exécuté avant provider réel
- DeepSeek V4 Pro exécuté sur 4 cas réels
- official_field_leak_count == 0
- zéro violation de schéma
- zéro citation manquante
- zéro phrase interdite
- zéro timeout
- aucun MCP
- aucun provider tool-calling
- aucun V_net / debt_final / corrupt_success officiel

La migration SARIF v4 est bien visible dans le workflow au commit 0f8cfc6, et le run SARIF réel est vert sur c49a155.

Ce que Phase 4.7 ne prouve pas

Phase 4.7 ne prouve toujours pas :

- qu’un provider est “bon”
- qu’OIDA-code prédit le succès production
- qu’un seuil de shadow pressure est exploitable
- qu’un code est merge-safe
- que V_net peut être émis officiellement
- que corrupt_success peut être exposé publiquement

Cette prudence reste non négociable. La Phase 3 avait déjà montré qu’un signal séduisant pouvait être confondu par la longueur des sessions et par un proxy faible de succès ; il ne faut pas recréer ce piège avec les providers.

Phase 4.8 — décision

Je choisis :

Phase 4.8 = provider regression deepening + private holdout protocol

Je reporte :

Phase 4.9 = artifact UX polish
Phase 5.0 = MCP / provider tool-calling design ADR only

Pydantic-AI peut être étudié en spike, mais pas adopté comme migration principale en 4.8. La raison : Pydantic-AI propose bien des agents, des tools et plusieurs modes de structured output — Tool Output, Native Output, Prompted Output — mais ton pipeline actuel a déjà ses contrats de sécurité, ses providers et son agrégateur. Migrer maintenant brouillerait la mesure.

4.8.0 — micro-hardening avant provider deepening
4.8.0-A — README contradiction cleanup

Corriger le README :

- supprimer ou réécrire le paragraphe Phase 4.6 obsolète qui dit encore upload-sarif@v3
- supprimer ou réécrire le paragraphe qui dit provider baseline not_run
- conserver Phase 4.7 comme état actuel

Test attendu :

test_readme_phase47_does_not_contain_stale_sarif_v3_claim
test_readme_phase47_does_not_say_provider_baseline_not_run
4.8.0-B — provider-baseline Node24 smoke

Le run provider-baseline réel affiche encore les warnings Node 20 sur checkout, setup-python et upload-artifact. Ce n’est pas un échec, mais Phase 4.8 doit prouver que provider-baseline fonctionne aussi sous FORCE_JAVASCRIPT_ACTIONS_TO_NODE24=true, pas seulement la CI générale.

Ajouter un input manuel :

force-node24:
  default: "false"

Ou un workflow séparé replay-only :

provider-baseline-node24-smoke.yml

Règles :

- replay only par défaut
- pas de provider externe nécessaire
- FORCE_JAVASCRIPT_ACTIONS_TO_NODE24=true
- permissions: contents: read

Tests :

test_provider_baseline_has_node24_replay_smoke
test_provider_baseline_node24_smoke_no_external_provider
test_provider_baseline_node24_permissions_read_only
4.8-A — redacted provider I/O capture
Objectif

Comprendre l’écart accuracy sans stocker de secrets ni publier des prompts bruts par défaut.

Phase 4.7 dit explicitement que les réponses réelles ne sont pas stockées, ce qui empêche de distinguer :

- labels trop stricts
- provider réellement incorrect
- output valide mais sémantiquement ambigu
- mapping expected_status trop grossier
Ajout CLI

Ajouter un flag opt-in :

--store-redacted-provider-io

Sorties autorisées :

.oida/provider-baseline/<provider>/redacted_io/
  L001.response.redacted.json
  L001.prompt.hash.txt
  L001.packet.summary.json

Interdit par défaut :

- raw prompt
- raw provider response
- API key
- secret-like strings
- repo content complet

Contenu autorisé :

- provider response redacted
- prompt SHA256
- evidence_ref IDs
- field estimates
- unsupported_claims
- validation errors
- model id
- timing
- token usage si disponible

Tests :

test_redacted_io_disabled_by_default
test_redacted_io_requires_explicit_flag
test_redacted_io_contains_no_api_key
test_redacted_io_contains_no_raw_prompt_by_default
test_redacted_io_contains_response_after_secret_redaction
test_redacted_io_records_prompt_hash
test_redacted_io_records_model_id
4.8-B — label audit for L001–L004
Objectif

Expliquer l’écart observé :

replay estimator_status_accuracy = 0.5
DeepSeek estimator_status_accuracy = 0.25
replay estimator_estimate_accuracy = 1.0
DeepSeek estimator_estimate_accuracy = 0.5
Travail demandé

Créer :

scripts/audit_provider_estimator_labels.py

Ce script lit :

- expected.json
- provider redacted response
- metrics.json

Et produit :

reports/provider_label_audit_l001_l004.md

Avec une table :

case_id
expected_status
provider_status
field
expected range
provider value
evidence_refs expected
evidence_refs cited
classification:
  label_too_strict | provider_wrong | mapping_ambiguous | contract_gap

Règle :

Aucun changement de label sans justification écrite.

Critère important :

Si les labels sont modifiés, la replay baseline doit rester cohérente.
4.8-C — private holdout v1
Objectif

Créer un petit holdout privé pour éviter de calibrer uniquement sur calibration_v1, qui est synthétique et déjà visible dans le repo.

Phase 4.7 ne doit pas devenir un benchmark public. Le rapport rappelle que les métriques doivent rester contractuelles, pas prédictives.

Créer :

datasets/private_holdout_v1/
  manifest.json
  cases/

Mais ne pas le committer si les cas contiennent du code privé. Deux options :

Option A:
  committer uniquement le schema + manifest.example.json

Option B:
  committer des fixtures synthétiques privées-like sans secret

Je recommande :

commit:
  datasets/private_holdout_v1/README.md
  datasets/private_holdout_v1/manifest.example.json

gitignore:
  datasets/private_holdout_v1/cases/
Taille initiale
12 cas privés opérateur

Répartition :

4 capability/benefit/observability
3 prompt-injection / citation / unsupported_claims
3 tool-grounded / contradiction
2 code-outcome F2P/P2P

Chaque cas doit avoir :

provenance
contamination_risk = private
expected labels
no secrets
4.8-D — provider matrix
Providers à comparer

Ordre recommandé :

1. DeepSeek V4 Pro
2. DeepSeek V4 Flash
3. MiniMax M2.7
4. Kimi K2.6

DeepSeek est le bon premier axe parce que ses docs officielles confirment le format compatible OpenAI/Anthropic, les modèles deepseek-v4-flash et deepseek-v4-pro, et la dépréciation de deepseek-chat / deepseek-reasoner au 24 juillet 2026.

MiniMax est un bon deuxième axe parce que sa doc officielle indique que son API supporte le format OpenAI, avec OPENAI_BASE_URL=https://api.minimax.io/v1.

Kimi est intéressant pour les tâches long-horizon : la doc Kimi K2.6 annonce un contexte 256K, des capacités de codage long-horizon, un usage via SDK OpenAI avec base_url=https://api.moonshot.ai/v1, et des contraintes spécifiques quand le thinking mode et les tools sont combinés.

Workflow

Étendre provider-baseline.yml :

provider-profile:
  deepseek | kimi | minimax | custom_openai_compatible

model:
  optional override

max-provider-cases:
  default 4

Phase 4.8 doit exécuter au minimum :

DeepSeek V4 Pro: 8 cases
DeepSeek V4 Flash: 8 cases

MiniMax/Kimi peuvent être :

not_run with reason = no API budget

ou exécutés si budget disponible.

Ne pas faire
Ne pas publier “DeepSeek meilleur que Kimi”
Ne pas benchmarker publiquement
Ne pas ajuster les seuils production
Ne pas utiliser calibration_v1 comme leaderboard
4.8-E — repeated runs / stability
Objectif

Séparer :

- provider stable mais parfois incorrect
- provider instable
- labels ambigus

Ajouter :

--repeat-provider-runs 3

Mais avec budget strict :

default = 1
max = 3
workflow_dispatch only

Métriques :

schema_valid_rate_mean
schema_valid_rate_std
estimate_accuracy_mean
estimate_accuracy_std
status_accuracy_mean
status_accuracy_std
citation_precision_mean
official_field_leak_count_max

Règle bloquante :

official_field_leak_count_max > 0 => fail

Règle non bloquante :

accuracy_std high => data for report, not failure
4.8-F — Pydantic-AI spike, not migration
Objectif

Évaluer si Pydantic-AI peut remplacer ou simplifier une partie du provider layer sans changer le comportement.

Créer :

experiments/pydantic_ai_spike/
  README.md
  adapter_sketch.py

Ou un module non importé :

src/oida_code/experiments/pydantic_ai_spike.py

Tests snapshot uniquement si dépendance présente.

À comparer :

current OpenAICompatibleChatProvider
vs
Pydantic-AI Agent with structured output

Critères :

- même LLMEstimatorOutput
- mêmes caps de confiance
- mêmes forbidden phrases
- mêmes evidence_refs
- aucun tool calling
- aucune sortie authoritative

Pourquoi seulement un spike : Pydantic-AI est puissant, mais ses tools et toolsets peuvent exposer des capacités d’appel d’outils ; le projet a volontairement bloqué MCP et provider tool-calling en Phase 4.7.

ADR-33 attendu

Ajouter :

ADR-33 — Provider regression deepening before framework migration

Contenu :

Decision:
  Phase 4.8 deepens provider regression on calibration_v1 and a private holdout
  before adopting a new agent framework or enabling MCP/tool-calling.

Accepted:
  - redacted provider I/O capture behind explicit flag
  - label audit for L001-L004
  - private holdout protocol
  - DeepSeek Pro vs Flash comparison
  - optional MiniMax/Kimi runs if API budget exists
  - repeated-run stability metrics
  - Pydantic-AI spike only, no migration

Rejected:
  - public provider ranking
  - threshold tuning on calibration_v1
  - MCP integration
  - provider tool-calling
  - raw prompt / raw response artifacts by default
  - official V_net / debt_final / corrupt_success
Rapport attendu

Créer :

reports/phase4_8_provider_regression_deepening.md

Structure :

1. Diff résumé
2. 4.8.0 hardening
3. ADR-33 excerpt
4. Redacted provider I/O
5. L001-L004 label audit
6. Provider matrix
7. DeepSeek V4 Pro vs Flash
8. Optional MiniMax/Kimi status
9. Private holdout protocol
10. Repeat-run stability
11. Pydantic-AI spike result
12. What this still does not prove
13. Recommendation for Phase 4.9
14. Gates

Honesty statement obligatoire :

Phase 4.8 deepens provider regression and label diagnosis.
It does not rank providers publicly.
It does not validate production predictive performance.
It does not tune production thresholds.
It does not enable MCP or provider tool-calling.
It does not emit official V_net, debt_final, or corrupt_success.
It does not modify the vendored OIDA core.
Critères d’acceptation Phase 4.8
1. README stale Phase 4.6/4.7 contradictions fixed.
2. Provider-baseline Node24 replay smoke added or explicitly not_run with reason.
3. ADR-33 written.
4. --store-redacted-provider-io added and disabled by default.
5. Redacted provider I/O contains no API key.
6. Redacted provider I/O contains prompt hash, not raw prompt by default.
7. L001-L004 label audit report produced.
8. Every label change, if any, has written justification.
9. Private holdout protocol documented.
10. Private holdout fixtures are not committed if they contain private code.
11. DeepSeek V4 Pro run repeated on at least 8 cases.
12. DeepSeek V4 Flash run executed or marked not_run with reason.
13. MiniMax/Kimi runs executed or marked not_run with budget reason.
14. Repeated-run stability metrics implemented.
15. official_field_leak_count_max == 0.
16. No raw prompt/raw response artifact by default.
17. No MCP added.
18. No provider tool-calling enabled.
19. Pydantic-AI spike completed or explicitly deferred with reason.
20. Report produced.
21. ruff clean.
22. mypy clean.
23. pytest full green, skips documented.
24. At least one GitHub-hosted provider-baseline run green after 4.8 changes.
Ce qu’il ne faut pas faire
Do not add MCP.
Do not enable provider tool-calling.
Do not migrate wholesale to Pydantic-AI.
Do not publish a provider leaderboard.
Do not tune thresholds on calibration_v1.
Do not store raw prompts by default.
Do not store raw provider responses by default.
Do not emit official V_net.
Do not emit official debt_final.
Do not emit official corrupt_success.
Do not claim merge-safe.
Do not publish PyPI stable.
Après Phase 4.8

Si Phase 4.8 passe :

Phase 4.9 — artifact UX polish
  - Markdown report polish
  - SARIF category disambiguation
  - GitHub Step Summary compact scorecards
  - action output ergonomics

Phase 5.0 — MCP / provider tool-calling design ADR only
  - no code
  - OWASP MCP risk review
  - tool poisoning / prompt injection / rug-pull controls

Je garde MCP hors 4.8. Le projet a gagné en solidité parce qu’il a toujours privilégié mesure, preuve et garde-fous avant d’ajouter de l’autonomie. La question d’origine sur V_net, debt_final et corrupt_success reste le verrou : tant que les signaux porteurs ne sont pas fiables et validés, ces champs restent bloqués/null.