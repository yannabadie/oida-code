Oui : Phase 4.8 est acceptée.

Le commit 647b049 annonce bien le statut fully accepted, avec DeepSeek V4 Pro et V4 Flash exécutés sur 8 cas, official_field_leak_count == 0, SARIF v4 déjà migré, redacted I/O opt-in, audit L001–L008, protocole private holdout, repeat-run stability, et spike Pydantic-AI limité à la documentation. Le rapport Phase 4.8 confirme aussi le point clé : les résultats provider sont de la donnée de diagnostic, pas un verdict produit ni un classement public.

Le signal le plus important n’est pas “V4 Flash bat V4 Pro”. Le vrai signal est : les garde-fous contractuels tiennent, mais les labels et les prompts ne sont pas encore assez robustes pour transformer les sorties provider en mesure produit. DeepSeek documente bien les modèles deepseek-v4-flash et deepseek-v4-pro, l’API OpenAI-format, le contexte 1M, JSON Output et Tool Calls, et la dépréciation de deepseek-chat / deepseek-reasoner au 24 juillet 2026 ; donc le choix de basculer sur V4 Pro/Flash était correct.

Je valide donc :

Phase 4.8: accepted
Next: Phase 4.9 — Artifact UX polish
Parallel micro-follow-up: 4.8.1 — failure-path redacted I/O capture
Not next: MCP
Not next: provider tool-calling
Not next: official V_net / debt_final / corrupt_success

La raison reste la même que depuis la revue v0.4.1 : le projet a déjà identifié que les signaux peuvent être séduisants mais confondus, notamment en Phase 3 avec les effets de longueur de session ; et la consultation initiale posait déjà le verrou V_net/debt_final/corrupt_success comme dangereux tant que les champs porteurs ne sont pas fiables.

QA/A26.md — Phase 4.9: Artifact UX polish + failure-path diagnostics
Verdict Phase 4.8

Phase 4.8 est validée.

Accepté :

- DeepSeek V4 Pro réel: 8 cas × 2 runs
- DeepSeek V4 Flash réel: 8 cas × 1 run
- official_field_leak_count == 0
- schema / citation / safety / fenced checks propres
- redacted I/O opt-in
- private holdout protocol
- repeat-run stability
- Pydantic-AI spike documentaire seulement
- aucun MCP
- aucun provider tool-calling
- aucun V_net / debt_final / corrupt_success officiel

Point à retenir :

V4 Flash:
  meilleur alignement status sur calibration_v1

V4 Pro:
  plus conservateur / plus instable / 6 captures manquantes

Conclusion:
  data, not verdict

Le rapport Phase 4.8 dit explicitement que V4 Flash atteint estimator_status_accuracy = 0.625, comme le replay, alors que V4 Pro est à 0.1875 ± 0.0625; il précise aussi que V4 Pro a un vrai gap de capture redacted I/O sur les chemins d’échec, avec 6/8 réponses non capturées à cause d’exceptions LLMProviderInvalidResponse.

Décision Phase 4.9

La prochaine phase doit rendre les sorties lisibles, actionnables et sûres pour un utilisateur opérateur.

Phase 4.9 n’est pas une phase “plus de modèle”.
C’est une phase UX d’artefacts.

Objectif :

Transformer les nombreux artefacts JSON/Markdown/SARIF/Actions
en rapports exploitables, sans créer de faux verdict produit.

Non-objectifs :

- pas de MCP
- pas de provider tool-calling
- pas de GitHub App
- pas de Checks API custom
- pas de verdict merge-safe
- pas de V_net officiel
- pas de debt_final officiel
- pas de corrupt_success officiel
- pas de ranking public DeepSeek/MiniMax/Kimi
4.9.0 — intégrer 4.8.1 en préambule

Avant le polish UX, corriger le gap de diagnostic V4 Pro.

Problème

Actuellement, le provider ne capture le redacted I/O que sur chemin succès. Quand V4 Pro renvoie un 200 OK avec un shape inattendu ou une réponse qui déclenche LLMProviderInvalidResponse, le runner n’a rien à écrire dans redacted_io/.

Le rapport Phase 4.8 le documente comme limitation : les chemins d’échec sont justement ceux qu’on voudrait inspecter.

Patch demandé

Modifier la capture pour écrire un fichier redacted même quand :

- JSON invalide
- shape inattendu
- choices[0].message.content manquant
- schema violation
- provider transport error
- 4xx / 5xx redacted

Nouveau modèle possible :

class ProviderRedactedIO(BaseModel):
    case_id: str | None
    prompt_sha256: str
    redacted_response_body: str | None
    redacted_error: str | None
    model: str | None
    http_status: int | None
    wall_clock_ms: int
    response_id: str | None
    finish_reason: str | None
    failure_kind: Literal[
        "success",
        "invalid_json",
        "invalid_shape",
        "schema_violation",
        "transport_error",
        "timeout",
        "provider_unavailable",
    ]

Règles :

- toujours opt-in
- jamais de raw prompt
- jamais de clé API
- prompt hash seulement
- redaction avant écriture disque
- un fichier par case, même en échec

Tests attendus :

test_failure_path_redacted_io_invalid_shape_is_captured
test_failure_path_redacted_io_invalid_json_is_captured
test_failure_path_redacted_io_transport_error_is_captured
test_failure_path_redacted_io_contains_no_api_key
test_failure_path_redacted_io_contains_prompt_hash_not_prompt
test_v4pro_like_missing_choices_shape_writes_redacted_io

Critère :

V4 Pro redacted IO capture should become 8/8 or every missing capture must have a written reason.
4.9-A — Markdown report polish
Objectif

Le rapport Markdown doit être lisible par un développeur en PR, pas seulement par nous.

Créer ou améliorer :

src/oida_code/report/markdown_templates.py

ou une commande dédiée :

oida-code render-artifacts .oida/provider-baseline \
  --format markdown \
  --out .oida/report.md
Structure cible
# OIDA-code Diagnostic Report

## 1. Status card
- Mode: diagnostic-only
- Official V_net: blocked
- debt_final: blocked
- corrupt_success: blocked
- Provider: replay / deepseek-v4-flash / deepseek-v4-pro
- Evidence integrity: pass/fail
- Official field leaks: 0

## 2. What was measured
- audit surface
- tools run
- provider cases evaluated
- calibration family

## 3. Key findings
- missing citations
- schema violations
- unsupported claims
- tool contradictions
- shadow pressure anomalies

## 4. What this does NOT prove
- no merge-safe claim
- no production-safety claim
- no bug-free claim

## 5. Next actions
- inspect redacted provider failures
- rerun with private holdout
- fix missing evidence

Le rapport doit afficher un bandeau explicite :

Diagnostic only — not a merge verdict.

Tests :

test_markdown_report_has_diagnostic_only_banner
test_markdown_report_has_official_fields_blocked_section
test_markdown_report_does_not_contain_merge_safe
test_markdown_report_links_redacted_io_without_raw_prompt
test_markdown_report_provider_matrix_is_readable
4.9-B — GitHub Step Summary polish
Objectif

Le résumé GitHub Actions doit donner un état lisible sans ouvrir les artefacts.

Dans action.yml et les workflows :

$GITHUB_STEP_SUMMARY

doit contenir :

## OIDA-code summary

| Item | Status |
|---|---|
| Mode | diagnostic-only |
| Official field leaks | 0 |
| Provider | replay / deepseek-v4-flash |
| Cases evaluated | N |
| Schema valid rate | X |
| Missing citations | Y |
| Tool contradictions | Z |
| SARIF uploaded | yes/no |

Règles :

- pas de raw prompt
- pas de raw response
- pas de clé
- pas de V_net officiel
- pas de merge-safe
- pas de provider ranking public

Tests :

test_step_summary_contains_diagnostic_only
test_step_summary_contains_artifact_paths
test_step_summary_does_not_contain_secret_like_values
test_step_summary_does_not_contain_forbidden_product_claims
4.9-C — SARIF category disambiguation

GitHub permet de fournir un category lors de l’upload SARIF pour distinguer plusieurs analyses sur un même commit, et indique que plusieurs SARIFs avec même outil/catégorie peuvent se remplacer ou faire échouer l’upload si la configuration est ambiguë.

Travail demandé

Stabiliser les catégories SARIF :

oida-code/ruff
oida-code/mypy
oida-code/semgrep
oida-code/codeql
oida-code/oida-diagnostic

Si le workflow n’upload qu’un seul SARIF fusionné :

category: oida-code/combined

Mais le rapport doit expliquer ce choix.

Tests :

test_sarif_upload_category_is_explicit
test_sarif_category_uses_oida_prefix
test_sarif_multiple_categories_do_not_collide
test_sarif_report_contains_run_automation_details
Règle UX

Ne pas noyer Code Scanning avec 300 findings OIDA sans hiérarchie.

Créer des niveaux :

error:
  tool contradiction
  schema/citation contract violation
  official field leak attempt

warning:
  unsupported claim
  missing evidence
  missing negative path

note:
  diagnostic-only shadow pressure
4.9-D — Action outputs ergonomics
Objectif

L’action composite doit produire des outputs simples et stables.

Ajouter ou formaliser :

outputs:
  report-json:
    description: "Path to JSON report"
  report-md:
    description: "Path to Markdown report"
  report-sarif:
    description: "Path to SARIF report"
  calibration-metrics:
    description: "Path to calibration metrics"
  diagnostic-status:
    description: "blocked | diagnostic_only | contract_clean | contract_failed"
  official-field-leaks:
    description: "Integer leak count"

Interdit :

diagnostic-status = merge_safe
diagnostic-status = production_safe
diagnostic-status = verified

Tests :

test_action_outputs_include_stable_report_paths
test_action_diagnostic_status_enum_has_no_merge_safe
test_action_outputs_do_not_surface_vnet_debt_corrupt
test_action_outputs_are_documented_in_readme
4.9-E — Provider label audit UX

Le rapport d’audit L001–L008 est précieux, mais il doit devenir lisible.

Créer une table standard :

case_id
field
expected
provider_value
classification
evidence_refs
action

Classification :

match
label_too_strict
provider_wrong
mapping_ambiguous
contract_gap
missing_capture

Action recommandée :

label_too_strict:
  propose label revision, but do not apply automatically

provider_wrong:
  keep label, mark provider behaviour

contract_gap:
  improve prompt/contract or accept as provider behaviour

missing_capture:
  rerun after 4.9.0 failure-path capture

Tests :

test_label_audit_markdown_has_classification_table
test_label_audit_never_changes_expected_labels_automatically
test_label_audit_marks_label_changes_as_proposals
4.9-F — Report bundle manifest

Créer :

.oida/artifacts/manifest.json

Schéma :

class ArtifactBundleManifest(BaseModel):
    schema_version: str
    generated_at: str
    mode: Literal["diagnostic_only"]
    official_fields_emitted: Literal[False]
    files: tuple[ArtifactRef, ...]
    provider: str | None
    model: str | None
    warnings: tuple[str, ...]

Chaque artifact :

class ArtifactRef(BaseModel):
    kind: Literal[
        "json_report",
        "markdown_report",
        "sarif",
        "calibration_metrics",
        "redacted_io",
        "label_audit",
        "step_summary",
    ]
    path: str
    sha256: str
    contains_secrets: Literal[False]
    contains_raw_prompt: bool = False
    contains_raw_response: bool = False

Tests :

test_artifact_manifest_lists_all_outputs
test_artifact_manifest_hashes_existing_files
test_artifact_manifest_contains_secrets_false
test_artifact_manifest_official_fields_false
ADR-34 attendu

Ajouter :

ADR-34 — Artifact UX before MCP/tool-calling

Contenu :

Decision:
  Phase 4.9 standardizes OIDA-code's operator-facing artifacts:
  Markdown reports, GitHub step summaries, SARIF categories, action outputs,
  label-audit tables and artifact manifests.

Accepted:
  - diagnostic-only status cards
  - explicit blocked official fields
  - stable action outputs
  - SARIF category disambiguation
  - redacted failure-path diagnostics
  - artifact manifest with hashes
  - no raw prompt / raw response by default

Rejected:
  - merge-safe labels
  - provider leaderboard
  - V_net/debt/corrupt_success emission
  - MCP/tool-calling
  - GitHub App / Checks API
  - production thresholds
Rapport attendu

Créer :

reports/phase4_9_artifact_ux_polish.md

Structure :

1. Diff résumé
2. 4.8.1 failure-path redacted I/O
3. ADR-34 excerpt
4. Markdown report design
5. GitHub Step Summary design
6. SARIF category strategy
7. Action outputs
8. Label-audit UX
9. Artifact bundle manifest
10. Security / secret review
11. What this still does not prove
12. Recommendation for Phase 5.0
13. Gates

Honesty statement obligatoire :

Phase 4.9 improves operator-facing artifacts and diagnostics.
It does not validate production predictive performance.
It does not rank providers publicly.
It does not enable MCP or provider tool-calling.
It does not emit official V_net, debt_final, or corrupt_success.
It does not create a GitHub App or custom Checks API integration.
It does not modify the vendored OIDA core.
Critères d’acceptation Phase 4.9
1. ADR-34 written.
2. Failure-path redacted I/O capture implemented or explicitly deferred with reason.
3. Missing V4 Pro failure captures diagnosed.
4. Markdown report has diagnostic-only banner.
5. Markdown report shows official fields blocked/null.
6. Markdown report contains no merge-safe / bug-free / production-safe claim.
7. GitHub Step Summary polished.
8. Step Summary contains no secrets / raw prompts / raw responses.
9. SARIF category strategy documented.
10. SARIF upload category explicit.
11. Action outputs documented.
12. Action diagnostic-status enum has no official verdict labels.
13. Label-audit report table standardized.
14. Label audit does not mutate labels automatically.
15. Artifact bundle manifest produced.
16. Artifact manifest includes SHA256 for output files.
17. Artifact manifest marks official_fields_emitted=false.
18. No MCP added.
19. No provider tool-calling enabled.
20. No vendored OIDA core modification.
21. Report produced.
22. ruff clean.
23. mypy clean.
24. pytest full green, skips documented.
25. At least one GitHub-hosted action-smoke or provider-baseline run green after 4.9 changes.
Ce qu’il ne faut pas faire
Do not add MCP.
Do not enable provider tool-calling.
Do not adopt Pydantic-AI as runtime framework.
Do not create GitHub App.
Do not use Checks API custom annotations.
Do not rank providers publicly.
Do not tune production thresholds.
Do not store raw prompts by default.
Do not store raw provider responses by default.
Do not emit official V_net.
Do not emit official debt_final.
Do not emit official corrupt_success.
Do not claim merge-safe.
Do not publish PyPI stable.

MCP reste repoussé. OWASP documente les risques spécifiques MCP — tool poisoning, rug pulls et cross-origin/tool shadowing — donc le projet doit garder l’ordre actuel : artefacts lisibles et mesurables avant autonomie supplémentaire.

Après Phase 4.9

Si Phase 4.9 passe :

Phase 5.0 — MCP / provider tool-calling design ADR only

Pas de code MCP immédiatement.

Le livrable Phase 5.0 devra être un document de design :

- threat model MCP
- allowlist tool registry
- tool schema pinning / hash
- rug-pull detection
- prompt-injection review of tool descriptions
- no dynamic untrusted tool install
- no network egress by default
- no production integration

Pydantic-AI reste un spike, pas une migration. Sa surface tools/toolsets est intéressante, mais c’est précisément une surface qui peut réintroduire les risques que le projet a évités jusqu’ici.