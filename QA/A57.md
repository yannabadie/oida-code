# QA/A57 - G-6d.4 Pre-Freeze Stop and Autonomous Protocol

Date: 2026-04-30

## cgpro consultation

Thread: `autonomous-protocol-20260430`

Conversation: `69f3ba6f-60f0-838b-8028-a8af734b2d72`

Status: `cgpro status` reported a healthy session with GPT-5.5 Pro
available before the consultation.

## Research inputs

Codex refreshed the methodological and tool surface before asking cgpro:

- arXiv SWE-bench paper: repository-level software engineering tasks require
  multi-file context, execution, and long-context reasoning.
- arXiv Agentless: structured localization, repair, and validation can be a
  stronger baseline than over-complex agent scaffolds.
- OpenAI 2026 SWE-bench Verified note: public benchmark results can be
  contaminated or test-flawed, so local evidence discipline matters.
- arXiv SWE-Adept and RepoAudit: repository-level agents need deliberate
  codebase navigation, checkpoints, memory, and validation to reduce
  hallucination and false positives.
- Context7 pytest docs: focused pytest node IDs are the right mechanism for
  reproducible small checks.
- Context7 Typer docs: `typer.testing.CliRunner` with stable exit-code and
  output-fragment assertions remains the right CLI test pattern.

No direct provider API call was needed for this block.

## Decision requested

Codex asked whether G-6d.4 could freeze only two clean existing-pool
candidates, whether it should continue screening, or whether it should stop
before freeze.

The local screening state supplied to cgpro was:

- clean for possible future freeze: `seed_074_simonw_sqlite_utils_658` and
  `seed_159_hynek_structlog_759`;
- questionable: `seed_071_simonw_sqlite_utils_689`, because it overlaps a
  prior `--functions` claim and comes from a broad PR;
- rejected or deferred: candidates requiring older dependency patterns,
  optional dependencies outside the supported group, non-Python/Rust surfaces,
  broad cleanup, release maintenance, or test-infrastructure-only changes.

## cgpro decision

cgpro selected `stop_before_freeze`.

Binding decision for this block:

- no partial +2 freeze;
- no train-only, holdout-only, or "least bad remaining candidates" salvage;
- exact +4 is required for a G-6d.4-style tranche;
- the exact +4 tranche must be split +3 train / +1 holdout before scoped
  pytest outcome inspection;
- fewer than four clean candidates means stop before freeze;
- accepted-but-unfrozen candidates must be re-screened before any future
  tranche.

## Autonomous protocol recorded

The protocol name is `Evidence-Led Autonomous Block Protocol
(ELABP-2026-04-30)`.

Operational rules now recorded in `AGENTS.md`:

- cgpro is the decision channel at every substantive inflection point;
- Codex is the local control plane for reading, editing, testing, committing,
  and pushing;
- Context7 or official docs are used for library/API behavior;
- web/arXiv are used for methodology and current provider/tool facts;
- provider/API keys are accelerators only, not trust-boundary relaxations;
- no provider model is selected without same-day official docs or model-list
  refresh;
- every block records evidence, sources, hard-wall compliance, tests, and
  stop conditions.

## Accepted scope

- Add a G-6d.4 pre-freeze stop report in Markdown and JSON.
- Add a regression test pinning exact-four / no-partial-freeze semantics.
- Update product status, backlog, expansion protocol, README, agent handoff,
  Codex context, decision log, and progress.
- Keep `reports/calibration_seed/index.json` unchanged at N=14.

## Forbidden scope

- No edit to `reports/calibration_seed/index.json`.
- No G-6d.4 `selection.json`, partition freeze, feasibility report, replay
  output, or `round_trip_outputs`.
- No `--install-requirements-file`, tox requirements rescue, or clone-helper
  dependency-boundary flag.
- No runtime verifier/provider/gateway/MCP change.
- No GitHub Action default change.
- No official OIDA fusion-field unlock.
- No product, merge-readiness, production-readiness, or broad-generalisation
  claim.

## Verification

- `python -m json.tool reports\phase6_d_4_candidate_screening_stop\screening.json > $null` - passed.
- `python -m pytest tests/test_phase6_d_4_candidate_screening_stop.py -q` - passed.
- `python -m pytest tests/test_phase6_d_4_candidate_screening_stop.py tests/test_phase6_d_dependency_policy.py tests/test_phase6_d_3_stop.py -q` - passed.
- `python -m pytest tests/test_phase6_1_c_partition_discipline.py tests/test_phase6_1_i_predeclared_bootstrap.py tests/test_phase6_1_g_extras_and_groups.py tests/test_reports.py tests/test_self_audit_guard.py -q` - passed.
- `git diff --exit-code -- reports\calibration_seed\index.json scripts\clone_target_at_sha.py action.yml src\oida_code` - passed with no diff.
- `python -m ruff check src/ tests/ scripts/evaluate_shadow_formula.py scripts/real_repo_shadow_smoke.py` - passed.
- `python -m mypy src/ scripts/evaluate_shadow_formula.py scripts/real_repo_shadow_smoke.py` - passed.
- `python -m pytest -q` - passed.
- `git diff --check` - passed.
