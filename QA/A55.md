# QA/A55 - Product Strategy Reset Review

Date: 2026-04-30

## cgpro consultation

Thread: `repo-product-vision-review`

Conversation: `69f329be-0dd4-838f-8687-d68190f21e7d`

Prompt scope:

- current commit `b8bc2ad`
- ADR-73 / G-6d.3 stop state
- README / PLAN / project status tension
- Windows CLI help failure
- stale `AGENTS.md` handoff state

## cgpro executive read

cgpro's core judgement was that the repo is technically disciplined,
but product direction is drifting into an internal proof machine. The
recommended reset is to pause G-6d pinning for a short block, make the
product promise explicit, repair the user and agent front doors, then
resume G-6d with a dependency-install policy recorded before candidate
selection.

## Accepted decision

ADR-74 accepts the diagnostic-first product path:

- primary user: Python reviewer / maintainer inspecting AI-authored
  diffs;
- primary job: determine whether a named claim is supported,
  contradicted, or unsupported by executable evidence;
- active product path: CLI and composite GitHub Action diagnostic
  reports;
- gateway remains opt-in and default-false;
- official OIDA fusion fields remain blocked;
- G-6d remains open but is not the immediate product strategy.

## Required work in this block

- Add `docs/product_strategy.md`.
- Update README front door so strategy is visible before the phase
  ledger.
- Demote `PLAN.md` from active source of truth to historical /
  aspirational context when it conflicts with current status.
- Update `AGENTS.md` and `memory-bank/codexContext.md` so future
  agents do not restart G-6a or ignore ADR-73.
- Fix the Windows CLI help path by removing help-text characters that
  are not encodable under cp1252.
- Add tests that lock those surfaces.

## Explicitly not done

- No new G-6d pinning.
- No replay generation.
- No provider call other than the cgpro review.
- No official field unblock.
- No GitHub App, SaaS, MCP runtime, provider tool-calling runtime,
  default gateway, or non-Python adapter work.

## Next decision after this block

Before G-6d resumes, write an ADR for historical test-dependency
patterns:

- either exclude targets that expose tests only via
  `requirements/*.txt` / `tox.ini` before freeze;
- or add a predeclared, structurally tested support path before
  candidate selection.

Do not add that support as a rescue after a case has already failed
post-freeze.
